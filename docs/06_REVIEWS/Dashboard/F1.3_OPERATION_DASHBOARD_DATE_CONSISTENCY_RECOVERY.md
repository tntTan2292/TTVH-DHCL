# F1.3 Operation Dashboard Date Consistency Recovery

## Status

- Project: `QIS V2`
- Defect: `F1.3 Operation Dashboard date/data inconsistency`
- Baseline: latest `origin/codex/da-impl-006`
- Technical status: `PASS`
- PO runtime status: `AWAITING PO RECHECK`

## Root Cause

The Operation Dashboard accepted URL date filters before metadata was loaded. Invalid or future dates such as `2098-02-18` could remain visible as the selected period while downstream widgets used their own date fallback behavior.

Follow-up PO runtime evidence showed the previous fix still received `2098-02-18` as backend coverage because dashboard metadata used raw `MAX(ngay_do_kiem)` from `fact_f13`. The runtime DB contained tiny future-dated recovery artifacts for `2098-02-16` and `2098-02-18`, so frontend normalization treated 2098 as valid.

The BCVH ranking service also resolved selected-day analysis through the latest available date inside the selected month. That allowed selected-period ranking, month-to-date values, and D-1/D-7 deltas to display data for a date different from the visible filter. When comparison rows were missing, D-1/D-7 deltas were calculated against zero instead of being reported as unavailable.

## Changed Scope

- Dashboard metadata now exposes available data coverage with `min_date` and `max_date`.
- Dashboard metadata and latest-import freshness exclude future-dated rows relative to the runtime day.
- Dashboard page normalizes invalid, out-of-coverage, and future URL dates to the available data range before issuing selected-period requests.
- Dashboard page performs one-time stale Dashboard date/filter storage recovery for existing browsers.
- Dashboard page performs one atomic URL cleanup for date normalization, legacy `kpi`, and invalid BCVH parameters.
- Dashboard widgets do not render or fetch until metadata coverage is loaded and the browser URL has been replaced with normalized dates.
- KPI and trend requests abort or invalidate stale requests created before normalization.
- Daily trend requests use the same effective `from_date` and `to_date` as the Dashboard filter.
- BCVH selected-period ranking no longer falls back to latest monthly data for a no-data selected date.
- D-1/D-7 ranking deltas are `null` when the comparison date has no data.

## Preserved Scope

- No Dashboard redesign.
- No ranking enhancements.
- No AUTO-IMPORT behavior changes.
- No TCT, HUE session, native browser-hide, or import queue changes.

## Validation

- `node --test frontend/src/features/dashboard/dashboardDateRange.test.js frontend/src/features/dashboard/components/qualityTrendlineWindow.test.js frontend/src/features/dashboard/components/integratedTrendRiskData.test.js frontend/src/features/dashboard/components/dashboardFilterOptions.test.js frontend/src/features/dashboard/components/dashboardStaleKpiRecovery.test.js`
- `node --test backend/src/controllers/DashboardController.recovery.test.js backend/src/services/F13DashboardService.recovery.test.js`
- `npm.cmd run lint`
- `npm.cmd run build`
- Runtime metadata: `/api/f13/dashboard/meta` returned `min_date=2026-01-01`, `max_date=2026-07-22` despite future-dated `2098` artifacts.
- Runtime normal route: `/f13/dashboard` replaced to `/f13/dashboard?from_date=2026-07-22&to_date=2026-07-22`; both date inputs were `2026-07-22`; visible Command Summary, BCVH table, trend, operating pattern, and action surfaces contained no `2098`.
- Runtime 2098 route: `/f13/dashboard?from_date=2098-02-18&to_date=2098-02-18` initially rendered no widgets and no `2098` body text, then replaced to `/f13/dashboard?from_date=2026-07-22&to_date=2026-07-22`.
- Runtime no-data API check: `2026-02-17` returned zero/empty selected-period data, `available=false`, and null D-1/D-7 deltas without latest fallback.

## PO Recheck

1. Open `/f13/dashboard?from_date=2098-02-18&to_date=2098-02-18`.
2. Confirm the visible date filter normalizes to the latest available 2026 coverage date instead of showing `18/02/2098`.
3. Select a valid no-data date inside available coverage, if one exists.
4. Confirm selected-period KPI, trend, BCVH table, operating pattern, and action widgets do not show latest/previous data as the selected-period result.
5. Confirm D-1 and D-7 deltas are unavailable/null when comparison data is unavailable.
6. Rapidly change date filters and refresh the page.
7. Confirm stale responses do not overwrite the latest selected range and widgets remain aligned to the same effective `from_date`/`to_date`.
