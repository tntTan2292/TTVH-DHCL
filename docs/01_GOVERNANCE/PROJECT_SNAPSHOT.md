# PROJECT SNAPSHOT

## Table of Contents

- [1. Purpose](#1-purpose)
- [2. Current Snapshot](#2-current-snapshot)
- [3. Usage Rules](#3-usage-rules)
- [4. Continuation Notes](#4-continuation-notes)

## 1. Purpose

This document is the Governance V2 current-state snapshot for AI onboarding.

It is designed to be the shortest safe entry point for a new AI session while preserving continuity with the existing Governance V1 workflow.

## 2. Current Snapshot

| Field | Value |
| --- | --- |
| Current Phase | `F13-STANDARDIZATION-001 Date-Filter Cross-Module Remediation — IMPLEMENTED / READY FOR PO RECHECK (2026-08-13). Evidence Consolidation Phase 1 Remediation itself is PO PHASE 1 REMEDIATION RECHECK PASS / CLOSURE PAUSED (recheck passed 2026-08-13; formal closure stays paused pending this date-filter remediation's own PO recheck). Program F13-STANDARDIZATION-001 itself remains open (Phase 0 partial, Phases 1-4 of the 5-phase program plan PLANNED / NOT ACTIVE), not closed by this.` |
| Current Ticket | `F13-STANDARDIZATION-001 (existing program group) — Date-Filter Cross-Module Remediation, DATE-FILTER REMEDIATION IMPLEMENTED / READY FOR PO RECHECK. Evidence Consolidation Phase 1 Remediation: PO PHASE 1 REMEDIATION RECHECK PASS / CLOSURE PAUSED. Most recently closed delta: the Evidence/Chi tiết bưu gửi delta (CLOSED / PO RUNTIME CHECK PASS, 2026-08-11), unaffected.` |
| Next Ticket | `None self-activated. Awaiting Product Owner recheck on the date-filter remediation (manifest Section 25). Phase 1 closure (Evidence Consolidation), the 8-frozen-document governance delta, and all Phase 2 work (including the 2026-08-12 PO finding, manifest Section 24, AC-15..AC-23) remain explicitly PAUSED pending this recheck. F13-SHIPMENT-001 (stash@{0}) and F13-SURFACE-CLEANUP-PLAN remain unauthorized.` |
| Last PO Status | `PO discovered a cross-module date-filter defect on Operation Dashboard's "Bảng điều hành BCVH" table (from_date silently discarded, always single-day) and authorized a bounded fix (2026-08-13): "Bảng điều hành BCVH" now genuinely aggregates ngay_do_kiem BETWEEN from_date AND to_date; BCVH Ranking keeps its single-evaluation-day contract (from_date === to_date, explicit); Tuyến Ranking/Evidence untouched. Implemented same day; PO recheck now requested.` |
| Current Branch | `codex/da-impl-006` |
| Current Manifest | `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` (Section 25, Date-Filter Cross-Module Remediation) |
| Current Checkpoint | `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` (Section 20) — full record in the dedicated `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 16 |
| Current State | `F13-STANDARDIZATION-001 Date-Filter Cross-Module Remediation — IMPLEMENTED / READY FOR PO RECHECK` |
| Technical Status | `Root cause: DashboardController.getBcvh validated from_date but never forwarded it — getBcvhRanking/getBcvhOperationMetricsByDate/getFactByDate all queried a single exact day (ngay_do_kiem = ?). Fixed, purely parameter-driven (no branching on caller/page identity): controller now forwards both dates and validates from_date <= to_date (400 INVALID_RANGE); service getBcvhRanking(fromDate, toDate, ...) sources current-period figures from range-aware repository calls; repository getBcvhRanking uses ngay_do_kiem BETWEEN ? AND ? (both bounds inclusive), new getFactBetween added; BcvhRankingPage.jsx now explicitly sends from_date === to_date (no-op on displayed data — to_date was already the only value ever honoured). Live-database verification reproduced the PO's exact evidence (BCVH 533140: single day 2026-08-11 -> 1,820/753/986; range 2026-08-01..2026-08-11 -> 18,895/10,179/7,841). The 81-count gap confirmed as pre-existing danh_gia_2026 IS NULL rows (same unclassified category as total_unknown/Chưa xác định nguyên nhân elsewhere) — no metric/formula changed. 12 new tests (9 backend covering the PO's 7-point scenario list + 3 frontend source-level), all passing; 9 pre-existing BCVH-ranking tests updated for the new signature. Full backend sweep 209/213, full frontend sweep 283/296 — both re-verified against a freshly-confirmed true baseline (200/204 backend, 280/293 frontend) via stash+temporary test-file removal, identical pre-existing failures by name, zero regressions. oxlint clean.` |
| Runtime Status | `Not exercised by Claude Code via browser — no usable plaintext credential exists in this workspace, same precedent as prior rounds. Verification was performed via direct, live (not mocked) calls through the actual fixed DashboardController/service/repository chain against the real database.sqlite (read/aggregate only — no row inserted/updated/deleted), reproducing the PO's own reported figures exactly, plus the full automated-test suites reported precisely above. network_service_point/network_level2_route/network_level2_route_stop/network_delivery_point/fact_f13 unchanged. Data QLML/ and both stashes confirmed untouched.` |
| PO UI Check Required | `Yes — Product Owner runtime recheck of the date-filter fix: (1) Operation Dashboard's "Bảng điều hành BCVH" table now shows range-aggregated figures across the selected from_date..to_date window, matching the range evidence above; (2) BCVH Ranking (single date picker use) still shows single-day figures only, unchanged from before; (3) an invalid/reversed date range is rejected rather than silently accepted. The Phase 1 Remediation recheck (DEFECT A/B) already PASSED (2026-08-13) and is not being re-asked.` |
| PO Product Status | `F13-STANDARDIZATION-001 Evidence/Chi tiết bưu gửi delta — CLOSED / PO RUNTIME CHECK PASS (2026-08-11), unaffected. Evidence Consolidation Phase 1 Remediation — PO PHASE 1 REMEDIATION RECHECK PASS / CLOSURE PAUSED (2026-08-13). Date-Filter Cross-Module Remediation — IMPLEMENTED / READY FOR PO RECHECK (2026-08-13).` |
| Last Closed Ticket | `F13-STANDARDIZATION-001 Evidence/Chi tiết bưu gửi delta — CLOSED / PO RUNTIME CHECK PASS, 2026-08-11 (delta only; the F13-STANDARDIZATION-001 program itself remains open). Most recently closed full ticket: NETWORK-MANAGEMENT-002, 2026-08-11. Evidence Consolidation Phase 1 closure (Section 23) and the Date-Filter Remediation (Section 25) are both not yet closed — RECHECK PASS / CLOSURE PAUSED and IMPLEMENTED / READY FOR PO RECHECK respectively.` |
| Last Closed Manifest | `https://github.com/tntTan2292/TTVH-DHCL/blob/codex/da-impl-006/docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` |
| Last Reviewed Phase | `F13-STANDARDIZATION-001 Date-Filter Cross-Module Remediation (2026-08-13): IMPLEMENTED / READY FOR PO RECHECK` |
| Last Reviewed Implementation Commit | `0a15ddf4 (Date-Filter Cross-Module Remediation — /f13/ranking/bcvh honours a real range, BCVH Ranking keeps single-day contract). Prior product-code commit: d6cd022d (Evidence Consolidation Phase 1 Remediation), unaffected by this round.` |
| Phase Review Status | `NETWORK-MANAGEMENT-001: CLOSED / PO FINAL PASS, unaffected. NETWORK-MANAGEMENT-002: CLOSED / PO PASS (2026-08-11), unaffected. F13-STANDARDIZATION-001: Phase 0 (of the original 5-phase program) partially implemented (unaffected); Phases 1-4 of that program still PLANNED / NOT ACTIVE; Evidence delta (Section 19) CLOSED / PO RUNTIME CHECK PASS; Evidence Consolidation Phase 1 Remediation (Section 23) PO PHASE 1 REMEDIATION RECHECK PASS / CLOSURE PAUSED; Date-Filter Cross-Module Remediation (Section 25) IMPLEMENTED / READY FOR PO RECHECK — none of these closes the program.` |
| Next Phase Authorization | `Date-filter remediation implemented, awaiting PO recheck. Once passed: Phase 1 (Evidence Consolidation) closure may proceed; Phase 2 still separately requires (a) its own governance delta for the frozen-document amendment, and (b) implementing the 10-point search-result-presentation contract (manifest Section 24, AC-15..AC-23) — both remain locked as scope, not yet built.` |
| Governance Version | `V2 Active` |
| Last Updated | `2026-08-13` |

## 3. Usage Rules

- Read this document immediately after `README_AI.md`.
- Treat this document as the single live project-state snapshot for AI onboarding.
- Do not infer current state from chat history when this snapshot is available.
- Do not use this document to override SSOT, frozen docs, or Product Owner decisions.
- Keep workflow behavior unchanged unless a dedicated governance change is approved.
- `Last Closed Manifest` must always be a concrete GitHub Blob URL pointing to the manifest of the most recently closed ticket when Current Ticket = None.
- `Last Closed Manifest` must not contain placeholder labels or descriptive text.
- `Last PO Status` must reflect the latest authoritative Product Owner visible outcome, including explicit failure states when a ticket remains active.
- `Claude Code` reads this document as its single live-state source instead of re-reading the full `README_AI.md` chain every session; see `CLAUDE.md` for the condensed Claude Code onboarding equivalent.
- Whenever `Current Ticket` changes (a ticket closes or a new ticket activates), append exactly one new line to `PROJECT_PROGRESS.md`'s ticket history in the same update; never edit or delete prior lines. This snapshot does not itself keep historical entries.

## 4. Continuation Notes

This snapshot is intentionally narrow.

It exists to answer only the questions a fresh AI needs in order to continue:

- where the project is
- what ticket is active
- what comes next
- what branch is active
- what manifest governs the current reading scope

Current handoff: `AUTO-IMPORT-011`, an emergency remediation ticket activated `2026-08-05` for two Product Owner-reported Import symptoms, is now `CLOSED / PO RUNTIME PASS`. Symptom A (recurring `2098`-dated file import bypassing future-date validation) was root-caused, fixed, and verified with a previously-failing regression test now passing. Symptom B (HUE/TCT browser window not appearing) was discovery-only, never technically root-caused; after a Product Owner-performed server restart, both HUE and TCT opened, authenticated, and imported successfully, and the Product Owner accepted the result as `PO RUNTIME PASS`. No code fix was applied for Symptom B — see `docs/06_REVIEWS/Import/AUTO-IMPORT-011_CHECKPOINT_002.md` for the closure disposition and the standing instruction to capture live backend console output before any future restart if it recurs.

Follow-up (`AUTO-IMPORT-012`, `2026-08-05`, `COMPLETED / TECHNICAL PASS`, closed same day): the test-isolation defect found and flagged under `AUTO-IMPORT-011` (`test_dkclHueF13SyncService.js` writing into production `Data DKCL/F1.3` and `database.sqlite`, confirmed twice) is fixed. `backend/src/services/importPipeline.js` now carries the same `NODE_ENV=test`-gated isolation guard `backend/src/config/db.js` already had, requiring `QIS_TEST_DATA_ROOT` for the file-system paths the same way the DB guard requires `QIS_TEST_DB_PATH`. A new shared helper `backend/test/importTestSandbox.js` builds a fresh OS temp sandbox (full `Incoming/Processing/Processed/Error/Quarantine × HUE/TCT` layout plus an isolated SQLite file with schema applied) per test run. All four Import test files that read/write the database or file system (`test_dkclHueF13SyncService.js`, `test_importPipelineRace.js`, `test_importProcessor.js`, `test_e2e_import_engine.js`) now use it; each was run twice consecutively with stable, passing results and zero measurable impact on production (`fact_f13` remained at the authoritative `663,126` baseline throughout, zero `2098`/`AUTO002` rows, no new production files). `QIS_ALLOW_TEST_FUTURE_DATE=true` (from `AUTO-IMPORT-011`) remains set only inside these isolated test files, confirmed absent from any `.env`, launcher, or production configuration.

`NETWORK-MANAGEMENT-001` reactivated (`2026-08-05`): Product Owner issued `PO GATE 1 PASS` and explicitly authorized Phase 2 (Ba bản đồ) implementation, ending the prior `PAUSED` state. Phase 2 is now `COMPLETED / TECHNICAL PASS` — see the dedicated paragraph below. The prior `F13-STANDARDIZATION-001` program's Tuyến Ranking (Route Ranking) delta closed on `2026-08-04` with explicit Product Owner `PO PASS` (implementation commit `03ce28bacc36b49d961caa1c006a011beb804bc7`); that program itself remains open and unclosed, unaffected. `F13-DATA-2098-CLEANUP-IMPL` remains `COMPLETED / TECHNICAL PASS / CLOSED` as of `2026-08-04` — a separate, earlier occurrence of the same data pattern, database-only in scope, which could not have prevented the AUTO-IMPORT-011 recurrence since it never touched the file system or code.

Phase 2 (Ba bản đồ) implementation (`2026-08-05`): under explicit Product Owner authorization following PO Gate 1 PASS, Claude Code first re-synced from the Product Owner-supplied remote HEAD `216f16277239781bbebfdc34982fdc772f28893c` and confirmed Phase 1 unaffected. An out-of-band workspace anomaly was found and remediated before any coding: the 3 root-level reference HTML files were missing from the working tree (tracked but deleted, not committed, unrelated to any Phase 1/2 action) and were restored via `git checkout -- <file>` (a pure restore, not a destructive action), then confirmed byte-identical to `HEAD`. A precise inventory of all 3 `Data QLML/` Excel files (sheets, columns, roles) was performed before writing any code, as required, and is recorded in checkpoint Section 13 — including the discovery that `2026.08. Mang DTC2.xlsx` (new since Phase 1) contains a "TỔ CHỨC LẠI" (reorganization) block alongside a "HIỆN TRẠNG" (current-state) block, with the Product Owner's locked decision to use only the current-state network, matching the reference HTML. Real data was seeded from the actual sources into the Phase 1 schema via a one-time script (not the Phase 3 Import feature): Mạng điểm phục vụ 151 điểm (0 warnings, exact match to the locked baseline); Mạng đường thư cấp 2 28 hành trình / 148 lượt dừng / 47 mã điểm / 1,435 km (0 warnings, exact match — geometry read from the reference HTML's `MAIL_ROUTES` array since the Excel has no coordinates, per Product Owner instruction); Sơ đồ tuyến phát 143,475 điểm kept of 160,554 rows (a small, reported-not-forced discrepancy against the locked 143,467 baseline and the HTML's own stated 143,463, documented in checkpoint Section 13, not resolved). Both Phase 1 and Phase 2 SQLite migrations are now applied automatically on every backend startup (previously only run manually), verified by an actual server restart. Three real Leaflet map screens now render this data (`admin`+`viewer` read access preserved, Import still `admin`-only and disabled/not implemented), with data-fetching and Leaflet-display code kept in separate components so Phase 3 Import can swap the data source later. Sơ đồ tuyến phát enforces Ngày→BCVH→Bưu tá selection at both API and UI — verified in a real browser that the points endpoint is never called until all three are chosen. Backend 31/31 and frontend 13/13 automated tests pass, `oxlint` clean, `vite build` succeeds; `fact_f13` confirmed unchanged (666,153 rows — the correct current baseline, having legitimately grown from 663,126 via the unrelated, intervening `AUTO-IMPORT-011` ticket) before and after; all 6 source Excel/HTML files confirmed byte-identical (SHA-256) before and after; `Data QLML/` never added to git. Full evidence: `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-001_CHECKPOINT_001.md` Section 13; `docs/10_TICKETS/NETWORK-MANAGEMENT-001_MANIFEST.md` Section 17. This closes Phase 2 only — Phase 3/4 remain `PLANNED / NOT ACTIVE` and each requires its own explicit Product Owner authorization; the Import scope was not reopened.

`NETWORK-MANAGEMENT-001` activation (`2026-08-04`): Product Owner explicitly authorized this ticket, naming three independent screens — Mạng điểm phục vụ, Mạng đường thư cấp 2, Sơ đồ tuyến phát — to be brought into QIS V2 as SQLite-backed, authenticated modules (`admin`+`viewer` read; `admin`-only Import with preview, error/duplicate detection, file fingerprint, and history). A locked four-phase plan (Phase 1 Nền tảng, Phase 2 Ba bản đồ, Phase 3 Import, Phase 4 Nghiệm thu) was written into manifest `docs/10_TICKETS/NETWORK-MANAGEMENT-001_MANIFEST.md`, with only Phase 1 set to `AUTHORIZED / READY FOR IMPLEMENTATION`. The Product Owner locked the audit baseline used to scope the ticket: Mạng điểm phục vụ 151 unique mã điểm (from 260 audited Excel rows, 109 excluded as `Tạm dừng`); Mạng đường thư cấp 2 28 hành trình / 148 lượt dừng / 47 mã điểm / 1.435 km (temporary HTML-seed baseline, no business Excel source yet); Sơ đồ tuyến phát 143,467 điểm (from a Product Owner-audited tháng 06/2026 Excel, audited outside this repository). Source Excel files are not in the workspace/repository; Claude Code must not re-audit them or guess column mapping, and must request them from the Product Owner at the start of the phase that needs them. This is a documentation-only governance activation: no product code, schema, or database was changed. Checkpoint `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-001_CHECKPOINT_001.md` was created. 02 pre-existing stashes and the three source HTML files at repository root remain untouched.

Program activation (`2026-08-04`): Product Owner approved the rapid standardization plan for the F1.3 module group, program `F13-STANDARDIZATION-001`. Two documentation-only steps were executed: (1) creation of the activation package — manifest `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` and checkpoint `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md`; (2) the full five-phase plan (Phase 0 Khóa nền số liệu, Phase 1 Chuẩn hóa cấu trúc F1.3, Phase 2 Hoàn thiện điều hành, Phase 3 Pareto và Evidence, Phase 4 Regression và đóng F1.3) locked into that single manifest, with only Phase 0 set to `READY FOR IMPLEMENTATION`. Locked product decisions and locked out-of-scope items are recorded once in the manifest (Sections 7-8) and must not be duplicated elsewhere.

Phase 0 implementation (`2026-08-04`, commit `e3ca24292f39b5c59022b161b63c4603cced1949`): recommendations engine switched from the non-authoritative `ket_qua_f13` to `danh_gia_2026` (this remediates the previously-recorded `RESIDUAL-01`, superseding the candidate below); the two audited `/f13`-prefix API path defects fixed; `dd/MM/yyyy HH:mm:ss` timestamp parsing fixed. Technically validated (backend/frontend test suites, oxlint, `vite build`); not separately closed with its own Product Owner runtime confirmation.

Tuyến Ranking (Route Ranking) delta closure (`2026-08-04`, PO PASS, `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 16): Route Ranking data contract standardized (`a0d4b041`); violations classified into `Chậm nộp tiền` / `Không đạt khác` / `Chưa xác định nguyên nhân` with a corresponding `/f13/evidence-list` API contract (`a892a276`); UI/UX refined (`6e575308`); pagination `10 tuyến/trang` and default ascending `passed_rate` sort added and Product Owner-confirmed (`03ce28ba`). Scope is Tuyến Ranking and its violation drill-down only — no other F1.3 screen, and no other phase of the program, is closed by this. The Shipment Performance Center delta remains preserved in `stash@{0}`, untouched, pending Product Owner reactivation of the deferred `F13-SHIPMENT-001` ticket.

Phase 1 (Nền tảng) implementation (`2026-08-05`): under explicit Product Owner authorization to implement Phase 1, Claude Code added 5 new independent SQLite tables (`network_import_log`, `network_service_point`, `network_level2_route`, `network_level2_route_stop`, `network_delivery_point`) additively to `backend/src/db/schema.sql` and applied them to the live operational database via an idempotent migration script — zero rows seeded, `fact_f13` confirmed unchanged at 663,126 rows before/after. Authenticated API foundation mounted at `/api/network-map` (`admin`+`viewer` read; `admin`-only Import scaffolding returning `501 NOT_IMPLEMENTED`, no Excel logic); the Sơ đồ tuyến phát points endpoint rejects any query missing Ngày/BCVH/Bưu tá with `400`. Frontend: new `Quản lý mạng lưới` nav group and 3 role-gated scaffold screens (no Leaflet map, no OSRM call) proving API connectivity and gating the Import button to `admin` only. Backend tests 20/20 pass, frontend tests 12/12 pass, oxlint clean, `vite build` succeeds; 7 pre-existing backend test failures unrelated to this ticket were found and left untouched (Windows-native `koffi` scripts and pre-existing F1.3 data-snapshot/contract tests — none reference any `network_*` table or this ticket's new files). No Excel was read; no business data was fabricated; PO-confirmed baseline figures (151/28+148+47/143,467) remain unimplemented data targets for Phase 3. 02 pre-existing stashes, the 3 root-level reference HTML files, and the newly-observed `Data QLML/` Excel files were left untouched. Full evidence: `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-001_CHECKPOINT_001.md` Section 12; `docs/10_TICKETS/NETWORK-MANAGEMENT-001_MANIFEST.md` Section 16. This closes Phase 1 only — Phase 2/3/4 remain `PLANNED / NOT ACTIVE` and each requires its own explicit Product Owner authorization.

Phase 3 (Import) implementation (`2026-08-06`): following a multi-round design review (initial Recommended Design → Corrected Recommended Design resolving dedupe-rule/DB-index/edited-file-handling contradictions → final PO approval), Product Owner authorized full implementation in 3 steps, each validated before the next began. Step 1 (additive migration): `network_import_session`, `network_import_snapshot`, `network_import_log.rollback_of_import_log_id`, and a real `UNIQUE(ma_buu_gui, ngay_phat, route_po_code)` index (pre-flight-checked for zero violations) — wired into `server.js` startup alongside Phase 1/2. Step 2 (backend): Export/Preview/Confirm/History/Rollback for all 3 modules in `backend/src/services/networkMapImport/` + `NetworkImportController.js`, honoring every locked decision from PO review unchanged — điểm phục vụ upsert-by-`ma_diem` with `trang_thai` never transformed; ĐTC2 using `network_level2_route.id` directly as the stable Route ID (no separate `route_key`, confirmed sufficient since AUTOINCREMENT ids are never reused) with `Mã điểm` existence-only validation (never filtering on `trang_thai`, so "Tạm dừng" points remain valid ĐTC2 geometry sources) and per-route-selected delete-and-reinsert; tuyến phát keeping the `(ma_buu_gui, ngay_phat, route_po_code)` key unchanged with `ON CONFLICT DO UPDATE` (never `INSERT OR IGNORE`). Rollback snapshots a before-image plus explicit INSERT/UPDATE/DELETE operation type per row and refuses to run when a later, still-active import touched the same scope. Step 3 (frontend): admin-only Import/Export/History/Rollback UI wired into all 3 map pages, plus a PO-directed extension to `ServicePointsMap.jsx` — "Tạm dừng" points default-hidden with a legend toggle and a marker visually distinct from all 5 active-status colors, additive Phase 3 frontend work made necessary by Phase 3 introducing such points for the first time (Phase 2's closed PO Gate 2 deliverable is not reopened). Using the finished feature, the 5 previously-audited "Tạm dừng" points were actually imported into the live database (`network_service_point` 151→156 rows; 11 previously-orphaned ĐTC2 stop references now resolve real geometry). Two real defects (a migration silently creating no rows due to `db.run()` vs `db.exec()`; a rollback-eligibility check that could miss a same-second later import via `created_at`'s second-level resolution) were found and fixed by the test suite before shipping. 77 backend + 25 frontend automated tests pass, `oxlint` clean, `vite build` succeeds, full real-browser runtime validation performed as `admin`; `fact_f13` confirmed unchanged (669,847 rows) across three checkpoints; all 3 `Data QLML/` source Excel files confirmed byte-identical throughout. Full evidence: `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-001_CHECKPOINT_001.md` Section 14; `docs/10_TICKETS/NETWORK-MANAGEMENT-001_MANIFEST.md` Section 27. This closes Phase 3 only — Phase 4 remains `PLANNED / NOT ACTIVE` and requires its own explicit Product Owner authorization.

PO Gate 3 Runtime Remediation (`2026-08-06`): Product Owner's first PO Gate 3 runtime check returned `RUNTIME FAIL` on 3 defects against baseline `2efa6fa227d1cda4c514f8afb4f8f91144acf59d`: (1) Mạng đường thư cấp 2 (ĐTC2) rendering straight-line polylines instead of road geometry; (2) Sơ đồ tuyến phát routing lacking resilience (no per-provider timeout, could hang silently; 10 out-of-Huế-bounds coordinate rows inherited unchanged from the Phase 2 seed could break a route); (3) the Date Picker allowing July 2026 selection when only June 2026 had been imported, because the filter/calendar date used `COALESCE(ngay_nhap_phat, ngay_phat)` rather than `ngay_phat` alone. Claude Code first audited root cause only (no code changes) and confirmed none of the 3 defects originated in the Phase 3 (Import) diff itself. Product Owner then authorized remediation, completed same day: `deliveryRoutingService.js` renamed/generalized to `roadRoutingService.js` and shared by both ĐTC2 (`Level2RoutesMap.jsx`, newly wired to real road routing for the first time) and Sơ đồ tuyến phát (`DeliveryRoutesMap.jsx`), adding a 15s per-provider `AbortController` timeout, 2-provider fallback, and a Huế-bounds routing-exclusion filter (excludes only from the OSRM waypoint chain, never edits/deletes the underlying data, always names the excluded point in the UI); `backend/src/controllers/NetworkMapController.js`'s `getDeliveryRoutesMeta`/`listDeliveryPoints` now filter/aggregate strictly by `ngay_phat`, with `ngay_nhap_phat`/`thoi_gian_nhap_phat` restricted to intra-day ordering only. 31 backend + 39 frontend tests pass (new regression tests per defect), `oxlint` clean, `vite build` succeeds; real-browser runtime re-verified as `admin` on all 3 map screens after a backend restart (all 28 ĐTC2 routes render real road geometry; a real 413-record tuyến-phát route builds a real road route; a route containing the confirmed Morocco-coordinate outlier still routes its 4 valid points and shows the exact expected exclusion warning; July 2026 fully disabled, June 2026 fully enabled in the Date Picker; Mạng điểm phục vụ unaffected). `fact_f13` (`669,847`) and `network_delivery_point` (`143,475`) row counts unchanged; `Data QLML/` untouched; both stashes untouched. Full evidence: checkpoint Section 15; manifest Section 28. This remediation does **not** constitute PO Gate 3 PASS.

ĐTC2 Journey Visual Remediation (`2026-08-06`): Product Owner reported overlapping outbound/return markers on ĐTC2 revisited điểm (Tuyến 6 example: BCVH Phú Lộc lượt 1/6, Thừa Lưu lượt 3/5, Lăng Cô quay đầu lượt 4) and authorized an additive visual fix scoped to the Mạng đường thư cấp 2 selected-route detail view: new pure module `routeJourneyGeometry.js` classifies outbound/turnaround/return, same-coordinate revisits are spiderfied via zoom-invariant Leaflet pixel-space projection (real lat/lon never altered), outbound/return legs route independently through the existing shared `roadRoutingService.js`, plus a mode toggle, consolidated per-location popups, and two-way hover/click sync. 12 new unit tests pass, 51/51 full targeted frontend suite, real-browser runtime confirmed on Tuyến 6/3/8/9. No backend/DB touched (`fact_f13` 669,847, `network_level2_route` 28, `network_level2_route_stop` 148, `network_delivery_point` 143,475 all unchanged); Import/Export/History unaffected; both stashes untouched. Does not alter Phase 3's `REMEDIATED / TECHNICAL PASS` state or PO Gate 3 status and does not start Phase 4. Full evidence: checkpoint Section 16.

ĐTC2 Selected-Route Arrow Visibility Remediation (`2026-08-06`): Product Owner runtime feedback that the journey-visual remediation's direction arrows were too small to read without zooming to maximum. Follow-up fix, same selected-route-only scope: `createDirectionArrowSvg` default size raised 16px→26px with a thicker outline/drop-shadow (already zoom-invariant — this only fixes "too small," not a zoom-scaling defect); new pure `computePolylineLengthKm`/`computeArrowCount` scale arrow density to real leg length (~1 per 15km, clamped 2-8) replacing a flat per-leg constant; arrows offset 13px off the line centerline via a consistent "90° from own bearing" rule that lands outbound/return arrows on opposite physical sides and never overlaps a stop marker. 2 new unit tests, 53/53 full targeted frontend suite, real-browser runtime confirmed at the unchanged default zoom (10) on Tuyến 6 (9 arrows, none overlapping its 6 markers), Tuyến 8/171km (16 arrows, max-clamped), Tuyến 2/8km (4 arrows, min-clamped), and the unselected overview (0 arrows). No backend/DB touched; both stashes untouched. Full evidence: checkpoint Section 17.

PO Gate 3 PASS — Closure (`2026-08-06`, baseline `7da98a79eb8`): Product Owner explicitly granted `PO GATE 3 PASS`, confirming ĐTC2 road routing/outbound-return display, same-coordinate marker/turnaround/arrow fixes, Sơ đồ tuyến phát routing resilience and out-of-Huế-bounds warning, Date Picker `ngay_phat` semantics, and Import/Export/History/Rollback all with no regression (verified: no commit across the entire PO Gate 3 remediation chain touched `NetworkImportController.js`, `backend/src/services/networkMapImport/`, or the frontend `import/` directory). This closes Phase 3 (Import) as `COMPLETED / PO PASS / CLOSED`. Per this same instruction, the program moves to `READY FOR PHASE 4 / AWAITING PO AUTHORIZATION` only — Phase 4 (Nghiệm thu) is **not** started and is **not** self-activated by this closure; it requires its own explicit Product Owner authorization. A future "Bản đồ tổng thể mạng lưới" (network-wide overview map) module was named in the same instruction and is recorded as a noted future scope item only — not added to Phase 4's locked scope, not authorized, not started. Documentation-only update; no product code, schema, or database changed. Full evidence: checkpoint Section 18; manifest Section 29.

Phase 4 — Sơ đồ tuyến phát Data Contract Audit + Remediation (`2026-08-06`): Product Owner authorized starting Phase 4 with a first scoped item, audit then remediation, same day. Audit found: the real raw source (`Data QLML/2026.07.01 - BatchFile Phat thang 06.2026.xlsb`) has a true 29-column header, but the Phase 3 Import feature required an unrelated hand-built 12-column flat template — an admin could not upload the real recurring file as-is; "Biển số" (`bien_so`) has no source column anywhere and was always NULL since Phase 1's original schema; no filename-vs-content period validation existed; classify/apply/upsert was already multi-month-safe by design; no file-archive mechanism existed (multer memoryStorage discarded uploads after each request). Remediation, PO-locked: new header-name-based raw-BatchFile parser (`parseDeliveryRoutesBatchFileExcel.js`) persists only the 11 fields the map needs, accepts-but-ignores the other 18 raw columns, fails loudly on a missing required header; "Biển số" removed from Import/Export (schema column kept, nullable, untouched — no breaking migration); filename period (`thang MM.YYYY`) cross-checked against parsed `ngay_phat` with a non-blocking warning on mismatch/multi-month; classify/apply/upsert layer left unchanged; new `network_import_archive` table + `fileArchive.js` archive the original file (checksum, byte size, declared/actual period, uploader, import time) after a successful Confirm, staged to disk at Preview time so the buffer is never lost to memoryStorage — no retention/auto-delete added. 104 backend + 53 frontend tests pass (incl. a real, unmodified `Data QLML` file parse reproducing the exact 143,475-row baseline); real-API and real-UI runtime validated (duplicate-fingerprint rejection of the real production file, Preview→Confirm→Archive→checksum-verify, Rollback via the actual UI, 2 sequential months with zero cross-month interference, Export confirmed without "Biển số"); all test-injected data rolled back/removed, `network_delivery_point` (143,475) and `fact_f13` (669,847) confirmed back at exact pre-test baseline; `Data QLML/` and both stashes untouched. Does not constitute Phase 4 or program-wide PO PASS. Full evidence: checkpoint Section 19; manifest Section 30.

PO Gate 4 Recheck Fail + Fix (`2026-08-06`/`2026-08-07`): Product Owner's first Phase 4 recheck FAILED at commit `0f968b02` — the real May 2026 BatchFile (`2026.06.01 - BatchFile Phat thang 05.2026.xlsb`) Previewed as 0/0/0/0/0 despite having 29 correct headers and ~144,289 valid rows. Root cause: `parseDeliveryRoutesBatchFileExcel.js` picked the data sheet by a hardcoded `Data_Ghep` name prefix (or blind `SheetNames[0]` fallback) — the June file's sheet matched that name and worked, but the May file's data sheet is named `Data_Tong_Hop` and isn't necessarily sheet 0, so the old code silently read the wrong/empty sheet. Fixed in commit `8cac056d`: new `findDataSheet()` scans every sheet by header CONTENT (never name/position), throws a clear per-sheet-missing-headers error if none qualify. 107/107 backend tests pass (incl. 4 new tests reproducing the exact reported multi-sheet delta); fix verified live via curl against the real API (Preview succeeded on a structurally-equivalent fixture, not Confirmed to DB). The actual May file is not available in this workspace — PO must re-run the recheck with the real file. `network_delivery_point`/`fact_f13` unchanged; `Data QLML/` and both stashes untouched. Does not constitute Phase 4 or program-wide PO PASS.

PO Gate 4 Second Recheck FAIL + Fix — Real May File (`2026-08-07`, commit `d830a784`): the Product Owner supplied the actual `Data QLML/2026.06.01 - BatchFile Phat thang 05.2026.xlsb` file. Re-running Preview against the `8cac056d` fix reproduced the same 0/0/0/0/0 symptom from a **new, different root cause**: sheet detection now worked correctly (`Data_Tong_Hop` found, all headers matched), but the May file stores `QUANTITY`/`LAT`/`LON` as text-formatted cells (e.g. `"-1"`, `"16.497005"`) instead of the June file's native numbers, so the parser's strict `=== -1` / `typeof === 'number'` checks silently excluded all 164,411 rows. PO authorized a bounded fix same day: `toFiniteNumber()` normalizes both native-number and numeric-text cells (rejects blank/non-numeric/NaN/Infinity); `isValidCoordinate()` now also range-checks (lat -90..90, lon -180..180); LAT/LON stored as normalized numbers; when every row is excluded, `periodWarning` names exact per-reason counts instead of a bare message; Preview's API response gained `sourceStats`. 9 new regression tests; 113/113 targeted backend suite; frontend `NetworkMapClient` 13/13; `oxlint` clean. Live `curl` Preview against the real, unmodified May file: `added: 144284, duplicate: 5` (144,289 total, matching PO's own ~144,289-row count), `declaredPeriod`/`actualPeriodMonths` both `2026-05`, no warning — **Preview only, not Confirmed to DB**. `network_delivery_point`/`fact_f13` unchanged; May file SHA-256 checksum unchanged; `Data QLML/` and both stashes untouched. Does not constitute Phase 4 or program-wide PO PASS. Full evidence: checkpoint Section 21; manifest Section 31.

Sơ đồ tuyến phát Import — PO PASS (`2026-08-07`): Product Owner officially confirmed `PO PASS` for the Sơ đồ tuyến phát Import component (Phase 4's first scoped item), citing: real unmodified May 2026 BatchFile Preview success (144,289 valid rows: 144,284 new / 5 duplicate / 0 error); declared period = actual period = `2026-05`; Confirm Import succeeded (PO-performed, `network_import_log.id=17`); period `05/2026` now available in Sơ đồ tuyến phát; the prior 0/0/0/0/0 defect (both root causes) resolved. Documentation-only update — no product code or data changed; the May import is preserved, not rolled back. Read-only verification against the running backend confirmed `network_import_log.id=17` (`status: SUCCESS, total_records: 144289, inserted_records: 144284, skipped_records: 5, error_records: 0, file_fingerprint: 3daa53c9…5a687`) matches the PO's accepted evidence exactly, and `/api/network-map/delivery-routes/meta` now lists `2026-05-01`..`2026-05-31` alongside June. **This PASS covers Sơ đồ tuyến phát Import only** — not Phase 4 or program-wide PO PASS. `Data QLML/` and both stashes untouched. Full evidence: checkpoint Section 22; manifest Section 32.

NETWORK-MANAGEMENT-001 Resumption — AUTO-IMPORT-013 unrelated closure (`2026-08-07`, out of band): `AUTO-IMPORT-013`, an urgent TCT login-detection ticket, activated, was implemented, and closed `COMPLETED / PO RUNTIME PASS` entirely independently of `NETWORK-MANAGEMENT-001` — it paused this ticket's snapshot slot without touching its code, data, `Data QLML/`, or stashes. One side effect worth recording here for continuity: `AUTO-IMPORT-013`'s validation included a real Product Owner-performed TCT F1.3 Import, which legitimately grew `fact_f13` by 3,934 rows (a single `2026-08-06` cohort) — this explains why `fact_f13`'s Phase 3 closure baseline of `669,847` is no longer byte-identical (now `673,781`) without it being a `NETWORK-MANAGEMENT-001` regression. Full AUTO-IMPORT-013 evidence: `docs/06_REVIEWS/Import/AUTO-IMPORT-013_CHECKPOINT_003.md`.

Phase 4 Acceptance Checklist — Technical Verification Round (`2026-08-08`): resuming `NETWORK-MANAGEMENT-001` on explicit instruction, Claude Code performed a read-only technical verification of manifest §6's remaining checklist items (2, 3-4, 5, 6), using no browser automation and applying no code change (no defect was found). **Item 2 (admin-only, all 3 modules) — Technical PASS**: route wiring, auth middleware, unit tests, and a live unauthenticated `curl` sweep (401 on every Import/Export/History/Rollback endpoint across all 3 modules) all confirm; frontend `NetworkAdminSection.jsx` returns `null` (absent, not disabled) for non-admin roles across all 3 map pages. **Items 3-4 ("Tạm dừng" handling) — Technical PASS**: live API read confirms all 5 audited points (`536101, 536102, 537200, 534630, 534989`) present with `trạng_thái` preserved verbatim as `"Tạm dừng"`, and all 11 ĐTC2 stop references to them resolve real, non-null geometry; code confirms default-hidden/toggleable/visually-distinct marker logic. **Item 5 (cross-module regression) — Technical PASS**: `git diff` since the PO Gate 3 baseline (`7da98a79`) shows zero touches to any Phase 1/2 map, auth, or routing code outside the Phase 4 import files (only Phase 4 delivery-routes-import files plus the unrelated, already-closed `AUTO-IMPORT-013` files changed); 117/117 backend + 13/13 frontend tests pass. **Item 6 (data integrity) — Technical PASS with one explained baseline shift**: `network_delivery_point`/`network_service_point`/`network_level2_route`/`network_level2_route_stop` all match expected baselines; `fact_f13`'s deviation is fully explained by the unrelated `AUTO-IMPORT-013` TCT import (see paragraph above); `Data QLML/` checksums and both stashes unchanged. No code changed, no defect found, no PO PASS declared. Full evidence: checkpoint Section 23.

Exact next authorized action (for `NETWORK-MANAGEMENT-001`): `Product Owner acceptance is still needed for manifest §6 items 2 (admin-only, viewer-role spot-check), 3-4 ("Tạm dừng" point handling, visual/toggle check), and 5 (light cross-module walkthrough) — a concrete ordered checklist for this was provided in the 2026-08-08 verification report. Item 6 is technical-only and needs no further PO check. Do not declare Phase 4 or program-wide PO PASS, and do not begin the "Bản đồ tổng thể mạng lưới" module, until repository authority confirms every remaining Phase 4 component has also received PO acceptance.`

Cleanup execution (`2026-08-04`): Product Owner authorized permanent removal of year-2098 test/future data. All six date-bearing fields were scanned; zero 2098 values existed in any event timestamp, so the predicate `ngay_do_kiem LIKE '2098%'` was verified complete and precise. After a verified `VACUUM INTO` backup (`backend/src/db/backups/database.pre-2098-cleanup.2026-08-04.sqlite`, `integrity_check = ok`), 4 `fact_f13` rows and 4 `import_log` rows were deleted in a single guarded transaction. Zero 2098 rows remain; date range is now `2026-01-01`-`2026-08-03`. 2026 production data is unchanged (663,126 rows, 213 days, per-month counts identical) and the authoritative KPI `danh_gia_2026` is unchanged at `58.6233%`. `DQ-01` and `DQ-03` are closed; `DQ-07` is retracted. Evidence: `docs/06_REVIEWS/Shared/F13-DATA-2098-CLEANUP-IMPL_CHECKPOINT_001.md`.

Prior ticket: `F13-DATABASE-PRODUCT-OPPORTUNITY-AUDIT-PLAN` is `CLOSED — PO DECISIONS RECORDED` as of `2026-08-04`. The read-only audit found the product exposes only a small fraction of the 45 columns per shipment; origin-handover-to-delivery latency separates passing from failing shipments by 10.97h vs 47.68h across 595,046 complete chains and is surfaced nowhere; 10 customer accounts carry 37.5% of all failures; 46 of 154 routes are chronically failing; and three F1.3 navigation entries (Pareto/RCA, Evidence, Message Center) are placeholder screens whose backend endpoints are implemented by static inspection, with runtime behavior not verified. Evidence: `docs/06_REVIEWS/Shared/F13-DATABASE-PRODUCT-OPPORTUNITY-AUDIT_CHECKPOINT_001.md`.

Prior ticket: `F13-SHARED-NAV-FILTERS-IMPL` is `CLOSED / PO UI PASS` as of `2026-08-04`.

Implementation & Closure (`2026-08-04`): Product Owner awarded `PO UI PASS` to `F13-SHARED-NAV-FILTERS-IMPL`. Implemented parameter dual-read fallback (`bcvh_id || ma_bcvh`) across Dashboard, BCVH Ranking, and Route Ranking; updated Route Ranking title to `"Bảng xếp hạng Tuyến Bưu tá"`; replaced Route Ranking static BCVH list with dynamic metadata from `/f13/dashboard/meta`; updated `GlobalFilterBar` default prop `showKpiFilter = false`; preserved URL filter parameters (`from_date`, `to_date`, `bcvh_id`) across cross-module navigation via `urlPreservation.js`. Ticket closed. No active ticket. Repository awaits explicit Product Owner direction before opening any next scope.

Next-direction candidates — recorded as candidates only, **not authorized tickets**:

1. Formally start `F13-STANDARDIZATION-001` Phase 1 (Chuẩn hóa cấu trúc F1.3 — naming/navigation, Evidence as the official shipment detail screen, redirects, Message Center hide, legacy-page removal), the next item in the original five-phase sequence.
2. Reactivate the deferred `F13-SHIPMENT-001` (Shipment Performance Center), whose delta changes remain preserved in `stash@{0}`.
3. `F13-SURFACE-CLEANUP-PLAN` — Evidence merge, Message Center hide, Vietnamese Shipment Ranking naming, redirect behavior, and verified orphan-page removal.
4. Later: Pareto product design, which must distinguish Pareto analysis from true RCA.

`RESIDUAL-01` (recommendations engine using `ket_qua_f13`) is **remediated** as of Phase 0 implementation commit `e3ca2429`; no longer an open candidate. Evidence MERGE and Message Center HIDE remain **pending Product Owner confirmation**. No repository authority records an explicit Product Owner decision on either; approval must not be inferred.

`NETWORK-MANAGEMENT-002` activation (`2026-08-10`): Product Owner explicitly authorized a new, independent ticket — Bản đồ tích hợp Điểm phục vụ + Đường thư cấp 2 — locking scope directly in the activation prompt: one integrated read-only map screen; display data directly from the two source modules (no data duplication); independently toggleable Điểm phục vụ / ĐTC2 layers; preserve the two source screens' already-approved marker/popup/legend/routing/interaction verbatim; no new database table; no dedicated Import/Export/History/Rollback; no behavior change to the two original screens; do not fold in Shipment Detail/Evidence. Explicitly does not reopen `NETWORK-MANAGEMENT-001` (closed, untouched) and is narrower than the previously-noted, still-unauthorized "Bản đồ tổng thể mạng lưới" candidate. Claude Code performed read-only, delta-only discovery only — no product code changed. Findings: both source screens already read via existing `admin`+`viewer`-readable endpoints (`GET /api/network-map/service-points`, `GET /api/network-map/level2-routes`) through `NetworkMapClient.js`'s existing methods — zero backend/schema/migration work needed; both source Map components (`ServicePointsMap.jsx`, `Level2RoutesMap.jsx`) are self-contained, each owning its own `<MapContainer>` and all rendering JSX in one file, with no existing reusable "layer-only" component; shared pure modules (`mapStyles.js`, `roadRoutingService.js`, `routeJourneyGeometry.js`) are directly reusable regardless of implementation approach; the ĐTC2 layer's full approved interaction (real road routing + the complete selected-route journey-visual mode) is the main implementation-complexity item, not optional under the "keep approved routing/interaction" lock; nav/routing wiring (`App.jsx`, `appNavigation.jsx`) follows an established, simple pattern for a 4th `network-map` screen; a latent, currently-dead `VIEWER_ALLOWED_PATH_PREFIXES` constant was noted for defensive consistency, not an active blocker. Two implementation options identified (manifest Section 8) — Option A (extract shared layer components, touches the 2 original files, requires proven behavior-equivalence) or Option B (zero-touch to the 2 original files, some JSX duplication) — Claude Code recommends and will default to Option B absent further direction. `Data QLML/` and both stashes untouched. Full record: `docs/10_TICKETS/NETWORK-MANAGEMENT-002_MANIFEST.md`; `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-002_CHECKPOINT_001.md`.

`NETWORK-MANAGEMENT-002` implementation — READY FOR PO CHECK (`2026-08-11`): Product Owner/CTO approved Option B and authorized implementation. New `frontend/src/features/networkMap/IntegratedMap.jsx` + `IntegratedMapPage.jsx` render Mạng điểm phục vụ and Mạng đường thư cấp 2 on one shared `<MapContainer>` at `/network-map/integrated` (`admin`+`viewer`, both layers default on), re-implementing the two source screens' already-PO-approved marker/popup/legend/routing/journey-visual behavior (including full ĐTC2 route-selection/outbound-return/turnaround/spiderfy/direction-arrow rendering) on the same shared pure modules (`mapStyles.js`, `roadRoutingService.js`, `routeJourneyGeometry.js`) those screens already use — `ServicePointsMap.jsx`/`Level2RoutesMap.jsx` confirmed untouched via `git diff`. `App.jsx` (+1 route) and `appNavigation.jsx` (+1 nav entry) modified; no backend, schema, migration, or `auth/roles.js` change (the latter left untouched per explicit instruction not to edit dead configuration defensively). `oxlint` clean, `vite build` succeeds, 53/53 relevant frontend tests pass unchanged. Real-browser verification as `admin`: all 4 layer-toggle states (both on/only Điểm phục vụ/only ĐTC2/both off) independently confirmed; ĐTC2 Tuyến 6 selection reproduces the exact known-good outbound (BCVH Phú Lộc→Lộc Thủy→Thừa Lưu)/turnaround (Lăng Cô)/return (Thừa Lưu→BCVH Phú Lộc) behavior with the same-coordinate Thừa Lưu revisit spiderfied; deselect reverts cleanly; the two original screens (`ServicePointsPage`, `Level2RoutesPage`) re-verified to render identically to before (156/156 điểm, 28 hành trình/1435 km). Live viewer-role check not performed (no credential available, same precedent as the closed Phase 4 verification round) — flagged as a PO checklist item; role gating confirmed by static code instead. `network_service_point`(156)/`network_level2_route`(28)/`network_level2_route_stop`(148)/`network_delivery_point`(287,759) unchanged (zero writes performed); `Data QLML/` and both stashes untouched. Does not constitute PO PASS — Claude Code updated governance to `READY FOR PO CHECK` only. Full evidence: checkpoint Section 12; manifest Section 14.

Fresh-chat onboarding chain:

1. `README_AI.md`
2. `docs/01_GOVERNANCE/CODEX_PROMPT_STANDARD.md`
3. `docs/01_GOVERNANCE/PROJECT_SNAPSHOT.md`
4. Current Manifest: `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 24 (PO finding locked into Phase 2 scope, documentation only) — Section 23 (Phase 1 Remediation, `IMPLEMENTED / READY FOR PO RECHECK`) is the live implementation status. `NETWORK-MANAGEMENT-002` (manifest `docs/10_TICKETS/NETWORK-MANAGEMENT-002_MANIFEST.md`) remains the most recently closed full ticket, `COMPLETED / PO PASS / CLOSED` (`2026-08-11`), unaffected; read only if the Product Owner's next direction concerns that screen.
5. Current Checkpoint: `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` Section 19 — full record: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 14 (finding) and Section 13 (Phase 1 remediation); the audit it builds on: `docs/06_REVIEWS/Shared/F13-EVIDENCE-PRODUCT-VALUE-AUDIT_CHECKPOINT_001.md`.

`NETWORK-MANAGEMENT-002` — PO RUNTIME FAIL + remediation, READY FOR PO RECHECK (`2026-08-11`): Product Owner reported that with both layers on, 156 Điểm phục vụ markers + 28 ĐTC2 routes overlapped too heavily; the Điểm phục vụ layer only had a whole-layer on/off toggle, no per-Loại-điểm density control. Remediated same day, `IntegratedMap.jsx` only (confirmed via `git diff --name-only` — no other file touched, `ServicePointsMap.jsx`/`Level2RoutesMap.jsx`/backend/schema still untouched): independent per-Loại-điểm checkboxes, category list derived from real data via the existing shared `normalizeLoaiDiem()` (never a separately hand-typed list), all default checked; per-category selection state never reset by the whole-layer toggle, so turning Điểm phục vụ off then back on restores the prior selection within the session; marker filtering and the legend's `visible/total` badges share one filter helper so counts can never drift from the actual markers rendered; combines correctly with the existing status filter, "Hiện điểm Tạm dừng" toggle, search, and the ĐTC2 layer/route-selection (all unchanged). `oxlint` clean, `vite build` succeeds, 53/53 relevant tests pass unchanged. Real-browser re-verified as `admin`: all-on 151/156 (matches pre-remediation baseline exactly); 1-category-only (Văn hoá xã) 102/156 (exact); multi-category (Văn hoá xã+Giao dịch+Văn phòng) 144/156=102+35+7 (exact); all-off 0/156; category+status("Ngừng hoạt động")+Tạm dừng combined 2/156 (exact); category filters unaffected by toggling the ĐTC2 layer off/on; ĐTC2 Tuyến 6 selection/deselection re-verified working correctly alongside active category filters. `network_service_point`/`network_level2_route`/`network_level2_route_stop`/`network_delivery_point` unchanged (zero writes); `Data QLML/` and both stashes untouched. Does not constitute PO PASS — governance updated to `READY FOR PO RECHECK` only. Full evidence: checkpoint Section 13; manifest Section 15.

`NETWORK-MANAGEMENT-002` — PO PASS, Closure (`2026-08-11`): Product Owner performed the runtime recheck and explicitly confirmed `PO PASS` — the per-Loại-điểm checkbox filter (remediation round above) resolved the reported marker/route overlap density issue — and explicitly authorized closing `NETWORK-MANAGEMENT-002`. Final delivered scope, unchanged from the locked scope (manifest Section 5): one integrated read-only map screen at `/network-map/integrated` (`admin`+`viewer`), Điểm phục vụ and ĐTC2 layers independently toggleable (both default on), Điểm phục vụ further filterable per Loại điểm (real-data-derived checkboxes, default all-on, selection persists across the layer's own toggle), full ĐTC2 routing/journey-visual behavior preserved, no data duplication/new table/dedicated Import-Export-History-Rollback. The two original screens were never modified across the entire ticket (discovery, implementation, remediation) — confirmed by `git diff --name-only` at every round. Documentation-only closure; `Data QLML/` and both stashes confirmed untouched throughout. State after closure: no active ticket, `AWAITING PO DIRECTION`. Full evidence: checkpoint Section 14; manifest Section 16.

Next authorized action (superseded below by the Evidence delta closure — kept for AUTO-IMPORT/NETWORK-MANAGEMENT context): `None self-activated. AWAITING PO DIRECTION for any next ticket. NETWORK-MANAGEMENT-001 (2026-08-10) and NETWORK-MANAGEMENT-002 (2026-08-11) are both COMPLETED / PO PASS / CLOSED — do not reopen either without explicit new Product Owner authorization. AUTO-IMPORT-011/012/013/014 remain CLOSED; if a HUE/TCT session-lifecycle symptom recurs, open a new remediation ticket per the standing instructions in their respective checkpoints.`

`F13-STANDARDIZATION-001` — Evidence / Chi tiết bưu gửi Implementation, READY FOR PO CHECK (`2026-08-11`, commit `a66fa57`): following the discovery delta above, Product Owner issued a 12-point remediation decision authorizing implementation, all within this existing program group (no competing ticket opened, per explicit instruction). Delivered exactly the locked scope: `ShipmentPerformancePage.jsx` is now the canonical component rendered at `/f13/evidence` (`admin`+`viewer`, replacing the `PlaceholderPage`); `/f13/ranking/shipment` is not deleted, it redirects to `/f13/evidence` preserving the full query string via a new `LegacyShipmentRedirect` component; real BCVH/date defaults sourced from `getDashboardMeta()` and a real, BCVH-dependent Tuyến selector sourced from `getRouteRanking(..., route_type=all)` replace every previous fake fallback (`2026-06-23`/`BC_HUE01`/`R_HUE01_01`), with "Tất cả tuyến" support added via a backward-compatible backend relaxation (`FactBuuGuiRepository.getEvidenceListFacts` drops the `ma_tuyen` predicate only when route is absent/`'all'`, existing callers unaffected); the existing single-day F1.3 analysis contract is reused verbatim from Route Ranking's `resolveDefaultRouteDate` (no range-filter switch); the implicit `pageSize=1000` cap is fixed by a new `fetchAllEvidenceRows()` helper that walks every backend page (server-side pagination was already correct; the cap was frontend-only), with a bounded safety ceiling that surfaces rather than silently drops an over-ceiling condition; full drill-down context (`from_date`, `bcvh_id`, `bcvh_name`, `route_id`, `route_name`, `shipment_id`) preserved; `/evidence-list` opened from `admin`-only to `admin`+`viewer`. Dashboard/BCVH Ranking/Tuyến Ranking, `RouteViolationEvidencePage.jsx`, and `frontend/src/auth/roles.js` (including the still-dead `VIEWER_ALLOWED_PATH_PREFIXES`, left untouched per the `NETWORK-MANAGEMENT-002` precedent) all confirmed untouched via `git diff --name-only`. Validation: backend targeted suite 16/16 pass (2 new tests for the optional-route path); full backend sweep 107/111 pass, the 4 failures confirmed pre-existing on the baseline via `git stash` (unrelated live-KPI-database/timeline tests); frontend 25/25 pass (15 new tests across `shipmentPerformanceData.test.js` and a new `ShipmentPerformancePage.contract.test.js`); `oxlint` clean; `vite build` succeeds (688 modules). Runtime browser verification was not performed by Claude Code — no usable plaintext credential (admin or viewer) exists in this workspace, the same precedent already recorded for `NETWORK-MANAGEMENT-001` Phase 4; an 11-item PO/Antigravity runtime checklist is recorded instead (manifest Section 18). `network_service_point`/`network_level2_route`/`network_level2_route_stop`/`network_delivery_point`/`fact_f13` unchanged (zero database writes); `Data QLML/`, both stashes, and `.claude/` untouched. State: `READY FOR PO CHECK` — Claude Code does not self-award PO PASS. Full evidence: checkpoint Section 13; manifest Section 18.

`F13-STANDARDIZATION-001` — Evidence / Chi tiết bưu gửi, PO RUNTIME CHECK PASS, Closure (delta only) (`2026-08-11`, implementation commit `a66fa57d`, governance baseline before the check `ca170c40`): Product Owner performed the runtime recheck and reported `8/9 mục PASS`: `/f13/evidence` accepted as the canonical Evidence screen; legacy `/f13/ranking/shipment` redirect preserves query/drill-down context; single-day analysis contract accepted; real BCVH list; real Tuyến list; "Tất cả tuyến" behavior; `shipment_id` selection/synchronization; loading/empty/error states and overall UI/UX. The 9th item — the >1,000-row live-dataset check — is recorded as `NOT TESTABLE`, not `FAIL`: the real production dataset's largest matching Không đạt set found is `318` records, below the previous 1,000-row cap this delta fixed, so the Product Owner could not runtime-observe the cap actually being exceeded; the underlying `fetchAllEvidenceRows()` page-walking fix (including its over-ceiling `truncated: true` warning path) has independent automated test PASS (`shipmentPerformanceData.test.js`, 5 tests), which is not itself represented as a live-runtime PASS here. Admin+Viewer access is recorded per actual evidence only: the Product Owner report does not state that both roles were live-logged-in during this recheck, so this closure records **technical verification** (`ProtectedRoute allowedRoles={[ROLE_ADMIN, ROLE_VIEWER]}`, identical in form to the other `admin`+`viewer` F1.3 routes) plus **automated routing test PASS** (`App.role-routing.test.js`), not a live dual-role runtime PASS — any future dual-role login trace should be added as an explicit update, not inferred. Backend test status is carried forward precisely as `107/111` at commit `a66fa57d` (the same 4 pre-existing, environment-dependent failures confirmed via `git stash` before this delta touched anything) — never described as "full backend PASS". **Scope of this closure: the Evidence/Chi tiết bưu gửi delta only** (manifest Sections 17-19; checkpoint Sections 12-14). It does **not** close the `F13-STANDARDIZATION-001` program — Phase 0 remains partially implemented/not separately closed, Phases 1-4 remain `PLANNED / NOT ACTIVE`, unaffected. No next ticket is created or activated by this closure, per explicit instruction: `F13-SHIPMENT-001` (`stash@{0}`) remains `DEFERRED / PRESERVED`, not reactivated; `F13-SURFACE-CLEANUP-PLAN` remains a named-but-never-created candidate, not created. `NETWORK-MANAGEMENT-001`/`NETWORK-MANAGEMENT-002` were not reopened. Documentation-only closure — no product code changed in this round; `Data QLML/`, `.claude/`, and both stashes (`stash@{0}`, `stash@{1}`) confirmed untouched. State after closure: no active ticket, `AWAITING PO DIRECTION`. Full evidence: checkpoint Section 14; manifest Section 19.

`F13-STANDARDIZATION-001` — Evidence / Chi tiết bưu gửi Discovery Delta (`2026-08-10`, out of band, discovery + governance only, no product code changed): scoped strictly to the Evidence/Chi tiết bưu gửi (shipment detail) screen area, within this existing program group rather than a new ticket naming series; does not reopen `NETWORK-MANAGEMENT-001`/`NETWORK-MANAGEMENT-002`, does not audit the whole repository, and `Data QLML/`/`.claude/`/both stashes were left untouched. Findings, confirmed by direct file read: the nav-visible `/f13/evidence` route (`F1.3 Quality Management → Evidence`, `admin`-only) still renders `<PlaceholderPage title="Evidence List" />` — unchanged since the `2026-08-04` database/opportunity audit; a real, working shipment-detail implementation already exists at `/f13/ranking/shipment` (`ShipmentPerformancePage.jsx`, "Shipment Performance Center") but is orphaned — reachable only by direct URL, not present in `appNavigation.jsx`; both are backed by the exact same single backend contract (`GET /f13/evidence-list` → `DashboardController.getEvidence` → `f13DashboardService.getEvidenceList`), and `F13DashboardClient.js`'s `getShipmentEvidenceList()` is confirmed still a direct pass-through alias of `getEvidenceList()`; a third, narrower consumer of the same contract is `RouteViolationEvidencePage.jsx` (route-violation drill-down). This exact convergence is already recorded, unresolved, in three prior artifacts — this program's own Phase 1 ("Keep Evidence as the official shipment detail screen") and Phase 3 (both `PLANNED / NOT ACTIVE`), the `2026-08-04` `F13-DATABASE-PRODUCT-OPPORTUNITY-AUDIT-PLAN` closure's outstanding "MERGE confirmation (Evidence → Shipment Ranking)" (never PO-decided since), and the deferred `F13-SHIPMENT-001` (`stash@{0}`, deferred `2026-07-28`) — plus a fourth, named-but-never-created candidate, `F13-SURFACE-CLEANUP-PLAN`. No implementation was performed or authorized. A Product Owner decision is requested on: (1) confirming or amending the MERGE direction (fold `ShipmentPerformancePage.jsx` into `/f13/evidence`, retiring the placeholder and the orphaned route, rather than building a separate Evidence screen); (2) which of the four converging planning artifacts should carry the implementation scope, to avoid opening a fifth competing plan; (3) whether to authorize implementation at all at this time. State: `DISCOVERY COMPLETE / AWAITING PO DECISION`. Full evidence: checkpoint Section 12; manifest Section 17.

AUTO-IMPORT-014 activation (`2026-08-08`, out of band): `NETWORK-MANAGEMENT-001` was paused a second time — not touched, not rolled back — to activate `AUTO-IMPORT-014`, an emergency Phase-1-discovery-only ticket for a general HUE/TCT session-lifecycle reliability defect (multiple Chrome windows, an authenticated session not recognized by Import, closing a window causing `FAILED`, Re-update briefly bringing the window to the foreground) reported by the Product Owner after `AUTO-IMPORT-013`'s `PO RUNTIME PASS` closure. Discovery (read-only code tracing, no live browser session opened, no credentials/cookies/tokens touched) found a systemic root cause: `entry.client`/`entry.page` inside `dkclSessionPreflightService.js`'s shared `globalRegistry` is a single mutable resource accessed concurrently, with no operation-level mutex, by an unconditional 5-second frontend preflight poll (`DataImportCenter.jsx`), the interactive login flow, and the Import backfill worker for both sources — and `preflight()` contains a branch that can destructively `close()` a live, actively-in-use session on a single transient false reading. The only existing mitigation (`entry.activeOperation === 'HUE_QUEUE_RUNNING'`) is HUE-only and does not cover every exposure window; no equivalent exists for TCT. No code was changed; a remediation design was proposed but not authorized. Full evidence: `docs/06_REVIEWS/Import/AUTO-IMPORT-014_CHECKPOINT_001.md`; ticket detail: `docs/10_TICKETS/AUTO-IMPORT-014_MANIFEST.md`.

`NETWORK-MANAGEMENT-001` — 5-Point ĐTC2 Discovery Delta, PO Confirmed Closed (`2026-08-10`, out of band from the current `AUTO-IMPORT-014` pause; scoped, read-only, governance-only — does not resume Phase 4 work or the paused ticket state above): a delta-only discovery traced the 5 audited "Tạm dừng" điểm (`536101, 536102, 537200, 534630, 534989`) to their raw sources — all 5 present in `Mang_diem_phuc_vu_kem_du_lieu_ban_do.xlsx` sheet `Huế` as officially registered service points (Loại Pudo/Giao dịch/Khai thác tỉnh), 2 of 5 also in sheet `TMS- NODE`; absent from `2026.08. Mang DTC2.xlsx` entirely (their ĐTC2 route geometry instead comes from the HTML reference's `MAIL_ROUTES` array, coordinates matching exactly); DB-traced to a dedicated Import batch `network_import_log.id=10` (`bosung_5diem_tamdung.xlsx`). Product Owner confirmed: all 5 are real service points (`điểm phục vụ thật`), status `Tạm dừng` — no code/coordinate/classification change; the 3 mã (536102, 537200, 534630) whose ĐTC2 route label differs from their official `Tên ĐPV` are accepted as an operational label, not a defect; the `TMS- NODE` gap for those same 3 mã is explicitly not to be investigated further. Documentation-only — no product code, schema, or data changed at any point. `Data QLML/` and both stashes untouched. Closes this discovery delta only; does not reopen Phase 4, does not resume the ticket from its `AUTO-IMPORT-014` pause, and does not start Sơ đồ tuyến phát Import standardization or any other scope. Full evidence: checkpoint Section 24; manifest Section 34.

`NETWORK-MANAGEMENT-001` — Sơ đồ tuyến phát 29-Column Standardization, Discovery + PO Decision (`2026-08-10`, out of band from the `AUTO-IMPORT-014` pause; scoped, read-only, governance-only): a delta-only discovery mapped all 29 real BatchFile source columns, confirmed the raw-file archive mechanism already meets the "preserve original for audit" requirement (`fileArchive.js`, no retention limit), found Export currently produces its own 11-column report rather than the original 29-column structure, and measured `network_delivery_point` at 287,759 rows / ~56.2 MB with a ~22-36 MB/month growth estimate if a full 29-column detail table were added. **Product Owner decision**: do not persist all 29 columns now — keep `network_delivery_point` exactly as-is; the raw file continues to be archived for audit (already sufficient); KPI fields will be added later on a per-need basis, not as one full detail table; no backfill for the 2 already-Imported months; no migration created. Closes this discovery delta; schema/parser/export remain unchanged. `Data QLML/` and both stashes untouched. Does not resume the ticket from its paused state. Full evidence: checkpoint Section 25; manifest Section 35.

`NETWORK-MANAGEMENT-001` — Program Final Closure, PO FINAL PASS (`2026-08-10`): Product Owner confirmed having reviewed the complete evidence set and explicitly authorized closing `NETWORK-MANAGEMENT-001` in full. Governance closure synthesized already-recorded evidence — no new PO runtime/UI recheck was requested or performed. All 4 phases closed: Phase 1 PO Gate 1 PASS (`2026-08-05`); Phase 2 PO Gate 2 PASS (`2026-08-05`); Phase 3 PO Gate 3 PASS (`2026-08-06`, baseline `7da98a79eb8`); Phase 4 — Sơ đồ tuyến phát data-contract remediation, two recheck-fail/fix cycles against the real May BatchFile (sheet-detection then text-cell coercion, both fixed same-day), Sơ đồ tuyến phát Import PO PASS (`2026-08-07`, real May file: 144,289 valid rows, Confirm succeeded), manifest §6 acceptance checklist items 2/3-4/5/6 Technical PASS (`2026-08-08`), and 2 closed discovery deltas (5-point ĐTC2 classification and 29-column standardization, both `2026-08-10`). Product Owner's FINAL PASS covers manifest §6 items 2, 3-4, and 5 — previously the only items awaiting a live PO UI check — together with everything above, constituting **PO Gate 4 PASS** and **program-wide PO FINAL PASS**. Documentation-only closure: no product code, schema, UI, or database change made or needed. `Data QLML/` and both stashes confirmed untouched throughout the program's entire lifecycle. State after closure: no active ticket, `AWAITING PO DIRECTION`; the future "Bản đồ tổng thể mạng lưới" module remains noted only, not authorized, not self-activated. Full evidence: checkpoint Section 26; manifest Section 36.

Latest authoritative next action as of the Evidence delta closure (`2026-08-11`, superseded below by the Evidence Product-Value Audit — kept for context): `None self-activated. AWAITING PO DIRECTION for any next ticket.` The most recently closed unit of work is the `F13-STANDARDIZATION-001` Evidence/Chi tiết bưu gửi delta — `CLOSED / PO RUNTIME CHECK PASS` (`2026-08-11`, implementation commit `a66fa57d`); this closes the delta only, **not** the `F13-STANDARDIZATION-001` program, whose Phase 0-4 status is unchanged (Phase 0 partial, Phases 1-4 `PLANNED / NOT ACTIVE`). Do not reopen `NETWORK-MANAGEMENT-001`, `NETWORK-MANAGEMENT-002`, or the Evidence delta without new explicit Product Owner authorization. `F13-SHIPMENT-001` (`stash@{0}`) remains `DEFERRED / PRESERVED`; `F13-SURFACE-CLEANUP-PLAN` remains a named-but-never-created candidate — neither is activated by this closure. `AUTO-IMPORT-011/012/013/014` remain `CLOSED`; if a HUE/TCT session-lifecycle symptom recurs, open a new remediation ticket per the standing instructions in their respective checkpoints.

`F13-STANDARDIZATION-001` — Evidence Product-Value Audit (`2026-08-11`, discovery/planning only, no product code changed): Product Owner opened this audit explicitly because the `PO RUNTIME CHECK PASS` closure above (commit `a66fa57d`) proved only that `/f13/evidence` works technically, not that it delivers product value. Traced end to end: `Tuyến Ranking → current shipment detail → /f13/evidence`. **Central finding**: the frozen `EVIDENCE_CENTER_INFORMATION_ARCHITECTURE.md`/`EVIDENCE_CENTER_WIDGET_SPECIFICATION.md` define Evidence Center as a distinct verification stage explicitly forbidden from "duplicating Shipment Performance Center" or "carrying Recommendation content" — yet the Product Owner-accepted canonical `/f13/evidence` today runs `ShipmentPerformancePage.jsx` (the Shipment Performance Center component itself, including a `ShipmentRecommendation` widget), exactly what the frozen documents forbid; this is escalated to the Product Owner rather than resolved unilaterally, since changing a frozen document requires explicit authority. Other findings: Tuyến Ranking's "Mở chi tiết bưu gửi vi phạm" button still targets a separate, already-`PO PASS`-closed screen (`RouteViolationEvidencePage.jsx`, `/f13/ranking/route/violations`, closed `2026-08-04` under the Tuyến Ranking delta) because that screen predates and was never reconciled with the later Evidence canonicalization (`2026-08-11`) — not an oversight, a genuine unreconciled design lineage; that screen has violation-reason classification (Chậm nộp tiền/Không đạt khác/Chưa xác định) that Evidence completely lacks, but also still has the exact 1,000-row pagination cap this program already fixed in Evidence, unfixed there; a `date` vs `from_date`/`to_date` URL-param mismatch between the two screens would silently corrupt the analysis date if a future round merged them without translating params — flagged before it becomes a live defect. Widget-by-widget audit of all 7 `Shipment*` widgets: only `ShipmentEvidenceSummary` is substantively real; `ShipmentExecutiveBrief` is real but redundant; `ShipmentImpactOverview`/`ShipmentTimeline`/`ShipmentDrilldown` are decorative or fully redundant with the table; `ShipmentRootCause` never surfaces the one thing it should (`violation_reason`, already returned by the API); `ShipmentRecommendation` just echoes the delay number, applies no real threshold logic, and independently violates the frozen Evidence boundary. Denominators between Tuyến Ranking and Evidence are guaranteed identical by construction (same repository query) given identical date/BCVH/route — confirmed by code trace, not re-measured live. Proposed target flow: Tuyến Ranking → violation list (merge onto `RouteViolationEvidencePage.jsx`'s already-real violation-reason structure, carrying over Evidence's route selector/viewer access/pagination fix) → select one shipment → real single-shipment cause/timeline panel → explicit, honestly-labeled hand-off to Action Center. Full KEEP/REMOVE/MERGE/REDESIGN classification, a no-code wireframe, and 8 reconciliation-checkable acceptance criteria are recorded in the dedicated checkpoint. 5 Product Owner decisions requested (full text: manifest Section 20 / dedicated checkpoint Section 12): (1) frozen-architecture path — amend the frozen docs to match the real product, or build toward the frozen verification/RCA spec as separate new scope; (2) confirm/amend the merge direction; (3) `ShipmentRecommendation` — remove or redesign; (4) final route/URL for the merged screen; (5) sequencing/authorization. No implementation performed or authorized. `F13-SHIPMENT-001` not opened; Dashboard/BCVH Ranking/`Data QLML/` not touched; `NETWORK-MANAGEMENT-001`/`002` not reopened; `.claude/` and both stashes confirmed untouched. State: `AUDIT COMPLETE / AWAITING PO DECISION`. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-PRODUCT-VALUE-AUDIT_CHECKPOINT_001.md`; checkpoint Section 15; manifest Section 20.

Next action as of the audit (`2026-08-11`, superseded below by the Evidence Consolidation plan — kept for context): `None self-activated. AWAITING PO DECISION on the 5 items above before any Evidence/Tuyến Ranking redesign implementation proceeds.` `F13-STANDARDIZATION-001` remains open as a program (Phase 0 partial, Phases 1-4 `PLANNED / NOT ACTIVE`); its Evidence delta is separately `CLOSED / PO RUNTIME CHECK PASS`. Do not reopen `NETWORK-MANAGEMENT-001`, `NETWORK-MANAGEMENT-002`, or `F13-SHIPMENT-001` (`stash@{0}`) without new explicit Product Owner authorization. `AUTO-IMPORT-011/012/013/014` remain `CLOSED`; if a HUE/TCT session-lifecycle symptom recurs, open a new remediation ticket per the standing instructions in their respective checkpoints.

`F13-STANDARDIZATION-001` — Evidence Consolidation Plan (`2026-08-11`, planning only, no product code changed, no frozen document edited; baseline `e2c32178`): Product Owner decided the audit above — keep `/f13/evidence` as the single shared violation-detail screen; Tuyến Ranking must lead into it rather than maintaining a separate shipment-detail flow; streamline Evidence per the audit; remove Recommendation in this scope; planning (not executing) a controlled amendment of the frozen architecture documents is permitted. **Three new defects were found while building the plan.** (F-1, blocking) `f13DashboardService.getEvidenceList()`'s mapper returns only six fields and discards `ma_tuyen`/`ten_tuyen`/`ma_bcvh`/`ten_bcvh`, even though the repository's `SELECT *` already returns them — so in "Tất cả tuyến" mode every row silently falls back to the URL parameter, **every shipment displays "Tất cả tuyến" as its route**, and route-name search matches nothing; the previous round's headline capability cannot actually attribute a violation to a route. Fixed by an additive pass-through in plan Phase 1. (F-2, latent crash) `RoutePerformancePage.jsx:262` references an out-of-scope `row` (`route.failed ?? row.total_failed`), throwing a `ReferenceError` whenever `route.failed` is nullish, currently masked only because the backend always populates it. (F-3, baseline correction) the true full frontend suite at `e2c32178` is **256 pass / 13 fail of 269**, not the "25/25" reported in earlier rounds for a narrow targeted subset; all 13 are proven pre-existing (`git diff --name-only b83900af HEAD` over the route/dashboard/pages directories returns empty), and one of them — `RoutePerformancePage.dateResolution.test.js` asserting a drill-down button label that does not match the code — sits directly in the path of plan Phase 3. Plan contents: an exact URL/data/reconciliation contract (Tuyến Ranking sends `from_date` and `to_date` with the same resolved analysis day, so Evidence never silently falls back to the newest imported day); a no-code wireframe with three regions (context/filter, violation list with server-sourced group tabs, evidence detail panel) plus desktop/mobile/loading/empty/error behaviour; a KEEP/REMOVE/MERGE/REDESIGN disposition for every widget (removing Impact Overview, Recommendation, Drilldown and the stale "shell" disclaimer); handling for the old `/f13/ranking/route/violations` screen as a **translating** redirect (`date` → both `from_date` and `to_date`) opened to `admin`+`viewer` so old bookmarks land on the correct day, with the component retired only after acceptance; 14 acceptance criteria (AC-1..AC-5 count reconciliation, AC-6..AC-9 date-parameter protection, AC-10 requiring a real dual-role login as evidence); a list of 8 frozen/architecture documents needing amendment, **none edited**, each requiring separate explicit Product Owner approval; and a 4-phase implementation plan with test plan and file scope. No implementation authorized. `F13-SHIPMENT-001` not opened; Dashboard/BCVH Ranking/`Data QLML/`/`NETWORK-MANAGEMENT` untouched and unexpanded; `.claude/` and both stashes untouched. State: `PLAN COMPLETE / AWAITING PO APPROVAL`. Full plan: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md`; checkpoint Section 16; manifest Section 21.

Next action as of plan approval (`2026-08-11`, superseded below by the Phase 1 implementation record — kept for context): `None self-activated. AWAITING PO APPROVAL of the Evidence Consolidation plan (manifest Section 21) before any implementation phase starts.` The frozen-document amendment list (plan Section 8) requires its own separate explicit approval before plan Phase 2. `F13-STANDARDIZATION-001` remains open as a program (Phase 0 partial, Phases 1-4 `PLANNED / NOT ACTIVE`). Do not reopen `NETWORK-MANAGEMENT-001`, `NETWORK-MANAGEMENT-002`, or `F13-SHIPMENT-001` (`stash@{0}`) without new explicit Product Owner authorization. `AUTO-IMPORT-011/012/013/014` remain `CLOSED`.

`F13-STANDARDIZATION-001` — Evidence Consolidation Phase 1 Implementation (`2026-08-11`, backend-only, additive-only; implementation commit `b147df7c`, plan approved at `34f42c57`): Product Owner approved the Evidence Consolidation plan and authorized Phase 1 only, together with 5 additional decisions — screen name **"Evidence — Chi tiết bưu gửi vi phạm"** (for Phase 2/3); arriving from Tuyến Ranking keeps the exact violation group clicked, and clicking the total `Không đạt` figure opens "Tất cả không đạt" (for Phase 3); no Action Center button until a real hand-off flow exists (for Phase 2); the frozen-document amendment is approved in principle but must not be mixed into any implementation commit, requiring its own separate governance delta before Phase 2; and the three violation groups may only be asserted to sum to the total after proving mutual exclusion and exhaustiveness over the unique `ma_bg` set, not from the arithmetic sum alone. Decisions 2-5 are recorded as locked for Phase 2/3 and were **not implemented this round** — only Phase 1 was, per the explicit "no Giai đoạn 2-4" instruction. **Implementation**: `backend/src/services/f13DashboardService.js`'s `getEvidenceList()` row mapper now additionally returns `ma_tuyen`/`ten_tuyen`/`ma_bcvh`/`ten_bcvh`, fixing F-1 (the repository's `SELECT *` already returned these four fields; the mapper was the only place discarding them, so "Tất cả tuyến" mode could not attribute any row to its real route). **No frontend file was touched** — direct code read confirmed both existing consumers already prefer a real API value over their own URL-parameter fallback via `||`, so the display/search fix took effect with zero additional frontend change once the backend stopped discarding the fields. 4 new backend tests: 2 prove route/BCVH pass-through (including a direct regression guard that two rows in "Tất cả tuyến" mode resolve two different real routes, never a shared fallback value); 2 prove `violation_reason` classification is a true partition — mutually exclusive and exhaustive over the unique `ma_bg` set — before trusting the numeric summary sum, implementing the Product Owner's exact reconciliation instruction. Validation, reported precisely per explicit instruction not to overstate scope: targeted evidence-list suite **20/20 pass**; full backend sweep **111/115 pass** (the same 4 pre-existing failures already on record, unchanged, unrelated to this diff); full frontend sweep re-run despite zero frontend files touched, to rule out incidental regression: **256/269 pass**, identical to the plan's already-recorded F-3 baseline — reported as the true full-suite figure, never narrowed to a targeted subset. No Phase 2-4 work performed; no frozen document edited; `F13-SHIPMENT-001` not opened; Dashboard/BCVH Ranking/`Data QLML/`/`NETWORK-MANAGEMENT` untouched and unexpanded; `.claude/` and both stashes confirmed untouched. State: `PHASE 1 IMPLEMENTED / READY FOR PO CHECK`. Claude Code does not self-award PO PASS. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 12; checkpoint Section 17; manifest Section 22.

Next action as of Phase 1 implementation (`2026-08-11`, superseded below by the Phase 1 remediation record — kept for context): `None self-activated. AWAITING PO CHECK on Evidence Consolidation Phase 1 (manifest Section 22) before Phase 2 starts.` Phase 2 additionally requires its own separate governance delta for the frozen-document amendment before it may start — that amendment must not be mixed into any implementation commit. `F13-STANDARDIZATION-001` remains open as a program (Phase 0 partial, Phases 1-4 of the original 5-phase program plan `PLANNED / NOT ACTIVE`). Do not reopen `NETWORK-MANAGEMENT-001`, `NETWORK-MANAGEMENT-002`, or `F13-SHIPMENT-001` (`stash@{0}`) without new explicit Product Owner authorization. `AUTO-IMPORT-011/012/013/014` remain `CLOSED`.

`F13-STANDARDIZATION-001` — Evidence Consolidation Phase 1 Remediation (`2026-08-11`, frontend-only, no widget consolidation, no frozen document; baseline `6e3bda0d`): Product Owner ran Phase 1 and reported 2 additional defects. **DEFECT A** — Vietnamese IME input (UniKey/EVKey) corrupted the shared `GlobalFilterBar` search box. Root cause traced through the actual event flow: every keystroke's `onChange` synchronously called `setSearchParams` (a React Router navigation re-rendering the whole page tree), heavy enough to land mid-composition and corrupt text (e.g. "phía" → "pịa"); no `compositionstart`/`compositionend` handling existed at all. Fixed in the one shared component where the `<input>` DOM element lives (not duplicated per-consumer): new `frontend/src/components/shared/searchCommitController.js`, a composition-aware debounced commit controller — never commits mid-composition, commits the final composed value immediately on `compositionend`, debounces (300ms) plain typing/paste/delete so a keystroke burst coalesces into one trailing commit; `SharedLayout.jsx`'s `GlobalFilterBar` search field now uses a local, uncontrolled-while-typing `<input>` wired to this controller, syncing from the external value only when not composing. Diacritic-insensitive search fallback also added (`stripVietnameseDiacritics`/`matchesSearchQuery` in `shipmentPerformanceData.js`) — exact (with-diacritics) match tried first, diacritic-stripped fallback only widens matching, never breaks exact route-code search (codes are digits, stripping is a no-op on them). **DEFECT B** — empty state didn't distinguish "route genuinely has no violations" from "keyword didn't match" (reported case: Tuyến 535790 - Hương Phong, BCVH A Lưới, 2026-08-10). Re-verification performed *before any code change*, per explicit instruction: a direct, read-only (`OPEN_READONLY`) query against the real operational database for `ma_bcvh='535790'`, `ngay_do_kiem='2026-08-10'` — ground truth for Tuyến `53579015` ("535790 - Hương Phong"): exactly 2 real shipments that day, both `Đạt`, zero `Không đạt`. Conclusion: **the filter was correct — real data, not a filter defect**; only the empty-state messaging needed remediation, exactly as the Product Owner's own contingency instruction anticipated. Dropdown contract also verified: `FactBuuGuiRepository.getRouteRanking()` groups directly from `fact_f13` for the exact date+BCVH, so a route can only appear in the dropdown if it had real activity that day — no change made to dropdown population, already correct by construction. `ShipmentPerformancePage.jsx`'s empty state, previously one generic message shown unconditionally (including a "chọn 'Tất cả tuyến'" suggestion even while already in that mode), is now `emptyStateContent` with 3 distinguished branches, keyword checked first per explicit instruction: (1) keyword active and unmatched → names the keyword, offers "Xóa từ khóa"; (2) no keyword, specific Tuyến selected, zero rows → names the route/ngày/BCVH, states explicitly this is a real result not a filter error, offers "Xem Tất cả tuyến"; (3) no keyword, "Tất cả tuyến" selected, zero rows anywhere → states the whole context has no Evidence, no route suggestion (already there). Loading/error states unchanged. 24 new tests across 4 files (`searchCommitController.test.js` ×8, `shipmentPerformanceData.test.js` +7, `SharedLayout.searchInput.test.js` ×4, `ShipmentPerformancePage.remediation.test.js` ×5) covering IME composition, paste, delete, fast typing, and all 3 empty-state branches. Validation: full frontend sweep **280/293 pass** (same 13 pre-existing failures already on record, unchanged — confirmed via `git status` that zero backend files were touched this round); backend sanity re-run **111/115**, identical to the Phase 1 baseline; `oxlint` clean on all 8 changed/new files. No Phase 2-4 work, no widget consolidation, no frozen document edited; `F13-SHIPMENT-001` not opened; Dashboard/BCVH Ranking/`Data QLML/`/`NETWORK-MANAGEMENT` untouched (the shared search-input fix changes correctness for those screens' existing search boxes only, no new feature/scope); `.claude/` and both stashes confirmed untouched; the DEFECT B database query was read-only, zero rows written. State: `PHASE 1 REMEDIATION IMPLEMENTED / READY FOR PO RECHECK`. Claude Code does not self-award PO PASS. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 13; checkpoint Section 18; manifest Section 23.

Next action as of the Phase 1 remediation (`2026-08-11`, superseded below by the PO finding locked into Phase 2 — kept for context): `None self-activated. AWAITING PO RECHECK on the Phase 1 remediation (manifest Section 23) — specifically Vietnamese IME search input behavior and the 3-branch empty state — before Phase 2 starts.` Phase 2 additionally requires its own separate governance delta for the frozen-document amendment before it may start — that amendment must not be mixed into any implementation commit. `F13-STANDARDIZATION-001` remains open as a program (Phase 0 partial, Phases 1-4 of the original 5-phase program plan `PLANNED / NOT ACTIVE`). Do not reopen `NETWORK-MANAGEMENT-001`, `NETWORK-MANAGEMENT-002`, or `F13-SHIPMENT-001` (`stash@{0}`) without new explicit Product Owner authorization. `AUTO-IMPORT-011/012/013/014` remain `CLOSED`.

`F13-STANDARDIZATION-001` — Evidence Consolidation, PO Finding Locked Into Phase 2 (`2026-08-12`, documentation only — no product code, route, component, schema, or frozen document changed): Product Owner confirmed the `2026-08-11` search remediation filters correctly, but its presentation misleads — keyword "hồng th" matched 9 rows; `ShipmentExecutiveBrief` (already locked `MERGE`, plan Section 5) auto-displayed one representative shipment/route; other matching routes were never shown; the "Evidence Runtime" KPI kept showing the pre-search total (`30`) with no results list rendered near the search box. A 10-point contract is now locked into the not-yet-started Phase 2: (1) typing a keyword only filters, never auto-selects a representative row; (2) show "Tìm thấy [n] thuộc [m] tuyến cho '[keyword]'" directly below the filter; (3) show every match grouped by real route (`[mã] - [tên tuyến]` + count), expandable to individual bưu gửi; (4) every matching/near-matching route appears, not only the first; (5) the detail panel updates only on explicit shipment selection; (6) pre-search context total, post-search count, and selected shipment stay visibly distinct; (7) the Tuyến dropdown remains its own independent filter; (8) explicit 0/1/n states, a clear-keyword control, and correct desktop/mobile behavior; (9) reconciliation by real `ma_bg`/`ma_tuyen`, never route-name text alone; (10) no interim patch to `ShipmentExecutiveBrief` — its disposition (merge into the context header) is already locked and unchanged. 9 new acceptance criteria (AC-15 to AC-23) added to the plan's Section 7. This updates the plan's wireframe (Section 4, a new "SEARCH RESULT SUMMARY" region and grouped-by-route active-search table state), widget disposition (Section 5, reaffirms `ShipmentExecutiveBrief`'s locked disposition), and acceptance criteria only — **no implementation was performed**, and Phase 2 remains blocked on its own prerequisite (the frozen-document governance delta) regardless of this addition. Does not reopen or amend the `2026-08-11` Phase 1/Phase 1-remediation closures. `F13-SHIPMENT-001` not opened; Dashboard, BCVH Ranking, `Data QLML/`, `NETWORK-MANAGEMENT` untouched; `.claude/` and both stashes confirmed untouched. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 14; checkpoint Section 19; manifest Section 24.

Next action as of `2026-08-12` (superseded below — kept for context): `None self-activated. AWAITING PO RECHECK on the Phase 1 remediation (manifest Section 23), and Phase 2 remains blocked on both (a) that recheck and (b) its own separate governance delta for the frozen-document amendment.` The 10-point search-result-presentation contract (manifest Section 24, AC-15..AC-23) is locked as Phase 2 scope but not yet implemented.

`F13-STANDARDIZATION-001` — Date-Filter Cross-Module Diagnosis + PO STOP (`2026-08-12`, read-only diagnosis, no code changed): before any recheck of the Phase 1 remediation was recorded, the Product Owner discovered a possible cross-module date-filter defect on Operation Dashboard and instructed Claude Code to STOP all current governance work (Phase 1 closure, the 8-frozen-document delta, all Phase 2 work) and perform a bounded, read-only diagnosis. Diagnosis confirmed: `DashboardController.getBcvh` validated `from_date` as present but never forwarded it to the service/repository layer; `f13DashboardService.getBcvhRanking`/`FactBuuGuiRepository.getBcvhRanking`/`getBcvhOperationMetricsByDate`/`getFactByDate` all queried a single exact day (`ngay_do_kiem = ?`), silently discarding `from_date`. Live evidence (BCVH Thuận Hóa `533140`): `to_date=2026-08-11` alone and `from_date=2026-08-01/to_date=2026-08-11` both returned the identical `{1820, 753, 986}`; a true range aggregate would be `{18895, 10179, 7841}`. Operation Dashboard's "Bảng điều hành BCVH" table was affected and internally contradicted its own page's correctly-ranged KPI cards; BCVH Ranking hit the same endpoint but was UI-self-consistent (single-day labeled throughout). Tuyến Ranking, Evidence, Pareto/RCA: not affected. Recorded as a continuity checkpoint (no governance-state change) in `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 15, per explicit instruction not to commit/push incomplete work.

`F13-STANDARDIZATION-001` — Evidence Consolidation Phase 1 Remediation, PO Recheck PASS (`2026-08-13`): Product Owner ran the `2026-08-11` remediation (DEFECT A Vietnamese IME search, DEFECT B empty-state distinction) and passed the recheck. State corrected from `IMPLEMENTED / READY FOR PO RECHECK` to `PO PHASE 1 REMEDIATION RECHECK PASS / CLOSURE PAUSED` — the recheck itself passed; formal Phase 1 closure stays paused only because it is sequenced behind the date-filter remediation below, per explicit PO instruction.

`F13-STANDARDIZATION-001` — Date-Filter Cross-Module Remediation (`2026-08-13`, bounded fix, PO-authorized): Product Owner accepted the Section-15 diagnosis and authorized a bounded fix under a 3-point contract — Operation Dashboard's "Bảng điều hành BCVH" table genuinely aggregates `ngay_do_kiem BETWEEN from_date AND to_date` (inclusive); BCVH Ranking keeps its single-evaluation-day contract, its request now explicitly sending `from_date === to_date`; Tuyến Ranking and Evidence untouched. Fixed purely by parameter wiring — no branching on caller/page identity anywhere: `DashboardController.getBcvh` forwards both dates and validates `from_date <= to_date` (`400 INVALID_RANGE` otherwise); `f13DashboardService.getBcvhRanking(fromDate, toDate, ...)` sources the current-period figures from range-aware repository calls (`getBcvhOperationMetricsBetween`, new `getFactBetween`) instead of single-day ones; `FactBuuGuiRepository.getBcvhRanking` uses `ngay_do_kiem BETWEEN ? AND ?`; `BcvhRankingPage.jsx`'s request now sends `from_date: toDate` (a no-op on displayed data, since `to_date` was already the only value ever honoured). Live-database verification (not mocked, run through the actual fixed code) reproduced the Product Owner's own reported evidence exactly: BCVH `533140` single day `2026-08-11` → `1,820/753/986`; range `2026-08-01`–`2026-08-11` → `18,895/10,179/7,841`. The 81-count gap on `2026-08-11` was confirmed (direct read-only query) to be pre-existing `danh_gia_2026 IS NULL` rows, the same unclassified category already modeled elsewhere (`total_unknown`, `Chưa xác định nguyên nhân`) — no metric or formula was changed. 12 new tests (9 backend covering the PO's 7-point required-scenario list, 3 frontend source-level), all passing; 9 pre-existing BCVH-ranking tests updated for the new function signature. Full backend sweep **209/213 pass**, full frontend sweep **283/296 pass** — both re-verified against a freshly-confirmed true baseline (**200/204 backend**, **280/293 frontend**, obtained by stashing all tracked changes and temporarily removing this round's new test files, not merely diffed from memory) with identical pre-existing failures by name; zero regressions. `oxlint` clean. No frozen document touched; no Phase 2 work; the 8-frozen-document governance delta not started; `F13-SHIPMENT-001` not opened; `Data QLML/`/`NETWORK-MANAGEMENT` untouched; `.claude/` and both stashes confirmed untouched throughout (re-verified after every stash/pop cycle used for baseline comparison). State: `DATE-FILTER REMEDIATION IMPLEMENTED / READY FOR PO RECHECK`. Claude Code does not self-award PO PASS. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 16; checkpoint Section 20; manifest Section 25.

Latest authoritative next action (`2026-08-13`, supersedes every earlier "Next authorized action"/"next action" note in this document): `None self-activated. AWAITING PO RECHECK on the date-filter remediation (manifest Section 25).` Once that recheck passes, Phase 1 (Evidence Consolidation) closure may proceed — its own recheck already passed (`2026-08-13`). Phase 2 remains blocked on both (a) this date-filter recheck and (b) its own separate governance delta for the frozen-document amendment; the 10-point search-result-presentation contract (manifest Section 24, AC-15..AC-23) is locked as Phase 2 scope but not yet implemented. `F13-STANDARDIZATION-001` remains open as a program (Phase 0 partial, Phases 1-4 of the original 5-phase program plan `PLANNED / NOT ACTIVE`). Do not reopen `NETWORK-MANAGEMENT-001`, `NETWORK-MANAGEMENT-002`, or `F13-SHIPMENT-001` (`stash@{0}`) without new explicit Product Owner authorization. `AUTO-IMPORT-011/012/013/014` remain `CLOSED`.
