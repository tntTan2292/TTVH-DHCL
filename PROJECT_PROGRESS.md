# Project Progress

## Current Phase

- `F1.3 UI Audit and Standardization Planning`

## Completion By Phase

- Business Discovery: `100%`
- Freeze Business: `100%`
- Information Architecture: `100%`
- Cross-Center Interaction: `100%`
- Widget Specification: `100%`
- UX Design Principles: `100%`
- Design System: `100%`
- Screen Architecture: `100%`
- Architecture Freeze: `100%`
- UX Architecture: `100%`
- Route UX Architecture: `100%`
- Shipment UX Architecture: `100%`
- Evidence UX Architecture: `100%`
- Action UX Architecture: `100%`
- UX Freeze: `100%`
- Implementation Architecture: `PASS`
- Release Planning: `PASS`
- Epic Planning: `PASS`
- Feature Planning: `PASS`
- Development Backlog: `PASS`
- Shipment Shell: `PASS`
- TODAY-001 Import Delivery: `MODULE COMPLETED / PO PASS`
- Leadership Dashboard Delivery: `Completed through DASHBOARD-AUDIT-001 PO PASS`
- Smart Leadership Dashboard Implementation: `COMPLETED / PO PASS`
- TODAY-002-R1 KPI 2026 Source Column Recovery: `Closed`
- TODAY-002-R2 KPI 2026 Dashboard Consistency Recovery: `Completed`
- DASHBOARD-AUDIT-001: `Completed / PO PASS`
- DA-IMPL-001: `Completed / PO PASS`
- DA-IMPL-002: `Completed / PO PASS`
- DA-IMPL-003: `Completed / PO PASS`
- DA-IMPL-004: `Completed / PO PASS`
- DA-IMPL-005: `Completed / PO PASS`
- AUTO-IMPORT-001: `Completed / Handoff`; `Atomic importer claim - Completed / Verified`
- AUTO-IMPORT-002: `Completed / PO PASS`; `Live end-to-end verification passed for 2026-07-16`
- AUTO-IMPORT-003: `Completed / PO PASS`
- AUTO-IMPORT-004: `Completed / PO PASS`
- AUTO-IMPORT-005: `Completed / PO PASS`
- AUTO-IMPORT-006: `Completed through TCT unfinished bulk-selection PO PASS`; Technical Status `PASS`; Runtime Status `PO PASS`; Last accepted commit `313b16a2f0e3259562681d26a581e5c9f2bba960`
- AUTO-IMPORT-007: `ACTIVE / WAVE 1 IMPLEMENTED`; Technical Status `WAVE 1 TECHNICAL PASS`; Runtime Status `NOT STARTED`; PO Product Status `NOT READY`; Current Phase `ACCELERATED DELIVERY WAVE 1`; next action `PO Wave 2 authorization`
- DOC-GOV-CLEANUP-001: `COMPLETED / TECHNICAL PASS`; Technical Status `PASS`; Runtime Status `NOT APPLICABLE`; Evidence `306 before / 306 after; no delete/move/merge; link check PASS; authority conflict check PASS; git diff --check PASS`
- DA-IMPL-006: `Completed / PO PASS`
- DA-IMPL-007: `Completed / PO PASS`
- F13-BCVH-RANKING-REDESIGN-PLAN: `Completed / Handoff`
- F13-BCVH-RANKING-REDESIGN-IMPL: `Completed / PO PASS / Closed`; latest verified implementation commit `a6235b2fc99fd662971a7c0fc9d7f43190b133b4`
- QIS-LAN-DEPLOY-001: `COMPLETED / PO PASS / CLOSED`; latest accepted runtime remediation commit `99c865e92b840a587dc9a889294c535fecc68816`
- F13-UI-AUDIT-PLAN: `READY FOR PO UI/UX PLANNING`
- TICKET-0102: `Deferred / Inactive`
- AUTO-IMPORT-010: `COMPLETED / PO RUNTIME PASS / CLOSED`; Dashboard/HUE/TCT accepted 2026-07-31; HUE first-click browser-open residual recorded as `KNOWN RESIDUAL / DEFERRED / NON-BLOCKING` by explicit Product Owner decision
- F13-UI-AUDIT-PLAN: `READY FOR DISCOVERY/PLANNING / NO IMPLEMENTATION` (reactivated as Current Ticket 2026-07-31 after AUTO-IMPORT-010 closure; Phase 1-3 PO PASS retained, Phase 4 not authorized)
- CORRECTION (2026-07-31): the line above understated Phase 4. Verified evidence (`235b69d0aa1a5b776b3398fde50c60172f7e4181` is an ancestor of branch HEAD; `docs/06_REVIEWS/UI/F13_UI_AUDIT_PLAN_CHECKPOINT_001.md` independently records it) confirms Phase 4 (Observation Group Viewport Optimization) is already `COMPLETED / PENDING PO REVIEW`, not `NOT YET DISPATCHED`. Corrected state: `F13-UI-AUDIT-PLAN: PHASE 4 COMPLETED / PENDING PO REVIEW / NO NEW IMPLEMENTATION`. See `docs/01_GOVERNANCE/PROJECT_SNAPSHOT.md` Section 4 for full evidence.
- F13-UI-AUDIT-PLAN: `COMPLETED / PO PASS (PHASE 1-4) / CLOSED` (2026-08-03). Phase 4 final acceptance: heatmap shows month-cumulative rank in the `TB THÁNG` cell, backend restarted to load the fix, Product Owner confirmed runtime result; latest accepted commit `cdb9eab246415a3835210dd70329996e6ef6521c`. `Current Ticket = None`; future sequence (BCVH Ranking, Route Ranking, shared nav/filters, Login/system states, final consistency review) not authorized or self-activated.
- F13-SHARED-NAV-FILTERS-PLAN: `COMPLETED / PO PLAN PASS / CLOSED` (2026-08-03). Authorized for DISCOVERY AND PLANNING ONLY / NO IMPLEMENTATION. Audit completed; Checkpoint Revision 2 awarded PO PLAN PASS.
- F13-SHARED-NAV-FILTERS-IMPL: `COMPLETED / PO UI PASS / CLOSED` (2026-08-04). Authorized under PO PLAN PASS. Parameter dual-read & write synchronization (`bcvh_id`), cross-module URL parameter preservation (`from_date`, `to_date`, `bcvh_id` via `urlPreservation.js`), Route Ranking title update ("Bảng xếp hạng Tuyến Bưu tá"), dynamic BCVH metadata loading, and GlobalFilterBar prop cleanup implemented and awarded PO UI PASS. Final implementation commit `e4c57e0d`.
- AUTO-IMPORT-011: `ACTIVE / SYMPTOM A FIXED / SYMPTOM B BLOCKED ON PO` (2026-08-05, emergency). Symptom A (recurring `2098`-dated future-file import, "Source unresolved" UI message) root-caused and fixed: `importProcessor.js` unconditionally exempted `2098`-prefixed dates from future-date rejection in production; fixed, verified via a previously-failing regression test now passing. Secondary test-isolation defect found and flagged (`test_dkclHueF13SyncService.js` writes fixtures into real production `Data DKCL` folders — separate follow-up ticket needed); self-inflicted DB pollution from this ticket's own validation run found and cleaned up (`fact_f13` restored to `663,126` rows). Symptom B (HUE/TCT browser window not appearing) discovery-only, not reproduced, blocked on Product Owner/live-runtime data. `NETWORK-MANAGEMENT-001` (previous Current Ticket) paused, unaffected, Phase 1 remains `COMPLETED / TECHNICAL PASS`.
- AUTO-IMPORT-012: `COMPLETED / TECHNICAL PASS` (2026-08-05, emergency follow-up to AUTO-IMPORT-011). Fixed the confirmed test-isolation defect: `importPipeline.js` now has the same `NODE_ENV=test` isolation guard `db.js` already had, requiring `QIS_TEST_DATA_ROOT` for file-system paths; new shared helper `test/importTestSandbox.js`; all four Import test files that touch the database or file system (`test_dkclHueF13SyncService.js`, `test_importPipelineRace.js`, `test_importProcessor.js`, `test_e2e_import_engine.js`) now run in an isolated sandbox. Each suite run twice consecutively, stable and passing; production `fact_f13` confirmed unchanged at `663,126` rows throughout, zero `2098`/`AUTO002` rows, no new production files. `AUTO-IMPORT-011` remains the tracking ticket (Symptom B still open); `NETWORK-MANAGEMENT-001` unaffected.
- NETWORK-MANAGEMENT-001: Phase 2 (Ba bản đồ) `COMPLETED / PO PASS / CLOSED` (2026-08-05). Product Owner visually rechecked and officially confirmed PO ROUTE VISUAL RECHECK PASS — LEGEND AND ROAD ROUTES ACCEPTED across all 3 map screens (Mạng điểm phục vụ, Sơ đồ ĐTC2, Sơ đồ tuyến phát with Calendar Date Picker, OSRM road network routing, and interactive Legend Box). PO Gate 2 officially closed as PASS. Phase 3 (Import) is `PLANNED / NOT ACTIVE`, awaiting explicit Product Owner direction.





## Frozen Documents

- `docs/PROJECT_SSOT.md`
- `PROJECT_STATUS.md`
- `docs/02_ARCHITECTURE/QIS_V2_ARCHITECTURE.md`
- `docs/02_ARCHITECTURE/BCVH/BCVH_PERFORMANCE_CENTER_INFORMATION_ARCHITECTURE.md`
- `docs/02_ARCHITECTURE/ROUTE/ROUTE_PERFORMANCE_CENTER_INFORMATION_ARCHITECTURE.md`
- `docs/02_ARCHITECTURE/SHIPMENT/SHIPMENT_PERFORMANCE_CENTER_INFORMATION_ARCHITECTURE.md`
- `docs/02_ARCHITECTURE/EVIDENCE/EVIDENCE_CENTER_INFORMATION_ARCHITECTURE.md`
- `docs/02_ARCHITECTURE/ACTION/ACTION_CENTER_INFORMATION_ARCHITECTURE.md`
- `docs/02_ARCHITECTURE/CROSS_CENTER_INTERACTION_ARCHITECTURE.md`
- `docs/BCVH_PERFORMANCE_CENTER_WIDGET_SPECIFICATION.md`
- `docs/ROUTE_PERFORMANCE_CENTER_WIDGET_SPECIFICATION.md`
- `docs/SHIPMENT_PERFORMANCE_CENTER_WIDGET_SPECIFICATION.md`
- `docs/EVIDENCE_CENTER_WIDGET_SPECIFICATION.md`
- `docs/ACTION_CENTER_WIDGET_SPECIFICATION.md`
- `docs/03_UX/shared/QIS_UX_DESIGN_PRINCIPLES.md`
- `docs/03_UX/shared/QIS_DESIGN_SYSTEM.md`
- `docs/02_ARCHITECTURE/BCVH/BCVH_PERFORMANCE_CENTER_SCREEN_ARCHITECTURE.md`
- `docs/02_ARCHITECTURE/ROUTE/ROUTE_PERFORMANCE_CENTER_SCREEN_ARCHITECTURE.md`
- `docs/02_ARCHITECTURE/SHIPMENT/SHIPMENT_PERFORMANCE_CENTER_SCREEN_ARCHITECTURE.md`
- `docs/02_ARCHITECTURE/EVIDENCE/EVIDENCE_CENTER_SCREEN_ARCHITECTURE.md`
- `docs/02_ARCHITECTURE/ACTION/ACTION_CENTER_SCREEN_ARCHITECTURE.md`

**Controlled amendment record (2026-08-13):** the 6 documents below (all in this list) plus 2 further documents outside this list's original scope (`docs/03_UX/evidence/EVIDENCE_CENTER_UX_ARCHITECTURE.md`, `docs/03_UX/shipment/SHIPMENT_PERFORMANCE_CENTER_UX_ARCHITECTURE.md`) received a Product Owner-authorized controlled amendment ("PO DATE-FILTER RUNTIME RECHECK PASS" instruction, executing the `2026-08-11` approval-in-principle from `F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 8) reflecting the merged Evidence Center / Shipment Performance Center screen (`/f13/evidence`). Each document gained a `## 0. GOVERNANCE AMENDMENT NOTICE (2026-08-13)` section at its top recording the change; original content is retained below each notice as the historical record, not deleted:

- `docs/02_ARCHITECTURE/EVIDENCE/EVIDENCE_CENTER_INFORMATION_ARCHITECTURE.md` — redefined as the merged violation-detail stage; drill-down chain collapsed to `Dashboard → BCVH → Tuyến → Evidence → Action`.
- `docs/02_ARCHITECTURE/EVIDENCE/EVIDENCE_CENTER_SCREEN_ARCHITECTURE.md` — eight-zone list replaced with the three real regions (context/filter bar, violation list, evidence-detail panel).
- `docs/EVIDENCE_CENTER_WIDGET_SPECIFICATION.md` — re-specified against the real widget set; retired widgets (Coverage, Scan History, Rule Validation-as-widget, Supporting Evidence, RCA Evidence-as-widget, Decision Support) marked "no data source available," not outstanding debt.
- `docs/02_ARCHITECTURE/SHIPMENT/SHIPMENT_PERFORMANCE_CENTER_INFORMATION_ARCHITECTURE.md`, `docs/02_ARCHITECTURE/SHIPMENT/SHIPMENT_PERFORMANCE_CENTER_SCREEN_ARCHITECTURE.md`, `docs/SHIPMENT_PERFORMANCE_CENTER_WIDGET_SPECIFICATION.md` — marked fully `SUPERSEDED` (the standalone Shipment Performance Center stage no longer exists; merged into Evidence Center).
- `docs/03_UX/evidence/EVIDENCE_CENTER_UX_ARCHITECTURE.md` — journeys/wireflow aligned with the merged screen's entry, journey, and wireflow.
- `docs/03_UX/shipment/SHIPMENT_PERFORMANCE_CENTER_UX_ARCHITECTURE.md` — marked fully `SUPERSEDED`.

Full record: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 17 (governance delta execution record).

## Business Decisions Frozen

- QIS V2 is a Decision Support System.
- EIDAF framework is locked.
- Operation Table SSOT is locked.
- KPI 2026 is locked.
- National Ranking must be grounded in imported nationwide data.
- Dashboard Runtime Rule is locked.
- Import Rule is locked.
- No mock data is accepted as final acceptance source.
- No business rule changes unless explicitly approved by Product Owner.

## Outstanding Decisions

- `F13-UI-AUDIT-PLAN` closed `2026-08-03` with Phase 1-4 PO PASS. No open decision remains on this ticket.
- Next scope is an open decision: Product Owner has not authorized any item from the future sequence (`BCVH Ranking`, `Route Ranking`, shared navigation/filters, Login/system states, final consistency review). `Current Ticket = None` until that direction is given.

## Open Issues

- Untracked HTML files remain in the working tree and are unrelated to the current governed ticket:
  - `Ban_do_mang_diem_phuc_vu_BDTP_Hue.html`
  - `Ban_do_mang_diem_phuc_vu_tich_hop_Duong_thu_cap_2.html`
  - `ban_do_duong_giao_thong_bcvh_postman_06_2026.html`

## Current Position

| Field | Value |
| --- | --- |
| Current Ticket | `None` |
| Current Commit | `3b605beb7ed2deeae239dbb050cf9b03fbad9c43` |
| Current Phase | `Awaiting Product Owner Direction` |
| Last Closed Ticket | `F13-DATA-2098-CLEANUP-IMPL` |
| Last Reviewed Phase | `F13-DATA-2098-CLEANUP-IMPL CLOSURE / CTO TECHNICAL PASS` |
| Last Reviewed Commit | `3b605beb7ed2deeae239dbb050cf9b03fbad9c43` |
| Phase Review Status | `COMPLETED / TECHNICAL PASS / CLOSED` |
| Next Phase Authorization | `NONE` |
| Next Milestone | `Product Owner selects the next scope; candidates are recorded but not self-activated` |
| PO UI Check Required | `No` |
| PO Product Status | `Cleanup closed` |
| Confirmed Open Defects | `FOUR — DQ-02, DQ-05, DQ-06, DQ-08` |

## F13-BCVH-RANKING-REDESIGN-IMPL Closure

- Ticket status: `COMPLETED / PO PASS / CLOSED`.
  - Dashboard BCVH table remains the original compact overview surface.
  - `/f13/ranking/bcvh` remains the detailed independent ranking surface.
  - `D-1` and `D-7` each show `Sản lượng`, `Tỷ lệ`, `SS SL`, and `SS Tỷ lệ`.
  - comparison-rank and rank-movement columns are not rendered.
  - table block order remains `Đơn vị -> Kết quả ngày đánh giá -> Chậm nộp tiền -> So sánh D-1 -> So sánh D-7 -> Phân bổ tuyến -> Hành động`.
  - KPI 2026 labels remain `Tốt / Cần chú ý / Cảnh báo / Rủi ro cao`.
  - route-distribution labels remain `Tốt / Khá / Trung bình / Kém`.
- Delayed-cash SSOT accepted:
  - denominator includes selected-day canonical BCVH facts with `danh_gia_2026 != Đạt`
  - delayed only when valid PTC and cash-handover timestamps exist and the gap is strictly greater than `3` hours
  - missing or invalid timestamps remain in the denominator but are not delayed
  - zero denominator publishes `0%`
- Accepted runtime evidence for `2026-07-28`: numerator `334`, denominator `1536`, rate `21.7%`.

## DOC-GOV-CLEANUP-001 Activation

- Ticket name: `Toi gian va chuan hoa he thong tai lieu du an`.
- State: `COMPLETED / TECHNICAL PASS`.
- Scope: inventory `docs`, identify at most `5` active onboarding documents, convert old documents to conditional reference or archive status, correct wrong status entries in `DOCUMENT_INDEX`, preserve history.
- Restrictions: no functional code changes, no code audit, no business SSOT changes, no history deletion.
- Result: `DOCUMENT_INDEX` now caps fresh onboarding at five steps and classifies non-onboarding docs as Conditional Reference or Archive; docs file count remained `306`.
- Closure evidence: `306` files before, `306` after; no delete/move/merge; link check `PASS`; authority conflict check `PASS`; `git diff --check` `PASS`.
- Queued next ticket: `AUTO-IMPORT-007`.

## AUTO-IMPORT-007 Plan

- Ticket name: `Chuan hoa va nang cap kien truc Import`.
- State: `ACTIVE / PLAN ONLY`.
- Authority: discovery and planning consolidation are complete; Wave 1 implementation is authorized and complete; further implementation is not authorized.
- Plan locks: shared DKCL lifecycle SSOT `SOURCE_SELECTED` -> `SESSION_CHECK` -> `OPENING_BROWSER` -> `WAITING_FOR_LOGIN` -> `AUTHENTICATED` -> `F13_OPENING` -> `F13_READY`; HUE/TCT share the lifecycle contract while keeping separate account, `profileDir`, Registry entry, PID tree, HWND, and session; branch only after `F13_READY` into `HueF13Adapter` and `TctF13Adapter`; standardize queue, retry, stop, progress, error, and import history; metadata must distinguish source even when original filenames match; architecture must extend to `F1.1`, `F1.2`, and `F4.1`; reuse code already `PASS` and do not plan a full rewrite.
- Completed discovery inputs: `Codex code/data analysis`; `Antigravity runtime/UI analysis`.
- Product Owner decisions: HUE and TCT use the same operator-visible stages `OPENING_BROWSER` -> `WAITING_FOR_LOGIN` -> `F13_OPENING` -> `F13_READY`; manual login timeout default remains `4` minutes; operator errors use concise actionable Vietnamese; technical details remain in logs.
- Approved six-phase plan: shared lifecycle contract/state standardization; shared source/session registry and profile ownership hardening; operator-visible lifecycle/runtime behavior alignment; shared queue/retry/stop/progress/error/history contract standardization; post-`F13_READY` adapter extraction; metadata/source identity completion and extension readiness.
- Wave 1 implementation result: shared lifecycle contract/state standardization by Codex, with no frontend visual or database behavior change; final TCT `LOGIN_IN_PROGRESS` compatibility regression closure PASS; further implementation requires Product Owner Wave 2 authorization.

## Pre-DA-IMPL-007 Regression Closure

- Focused Import Center and Dashboard regression remediation: `COMPLETED / PO PASS` on `2026-07-21`.
- Accepted commits: `f32afc3`, `43dc587`, `5d44b69`, and `de8bcbd27470e521d4c52be1d16b2be01fb73dc8`.
- Closure evidence and protected contract boundary: `docs/06_REVIEWS/Import/PRE_DA_IMPL_007_REGRESSION_PO_PASS.md`.
- `DA-IMPL-007` is completed and PO PASS.

## DA-IMPL-004 Unified BCVH Analysis Table Status

- Ticket status: `COMPLETED / PO PASS`.
- Product Owner accepted Unified BCVH analysis table, date context synchronization, Chuyển hoàn reconciliation, and detail navigation.
- Boundaries preserved: no SSOT changes; no KPI formula changes; no new BCVH mapping rules; no new business thresholds; no TCT; no AUTO-IMPORT changes; no Architecture Freeze changes; no broad UI redesign outside the unified BCVH analysis table.

## DA-IMPL-005 Operating Pattern Tabs Status

- Ticket status: `COMPLETED / PO PASS`.
- Product Owner accepted tab order/default, `Theo tháng` management view, `Theo thứ` combo view, Heatmap management view and month separation, and data contracts/filter context.
- Accepted UI/UX follow-up: Heatmap responsive layout at 100% desktop zoom, month block adaptation, controlled scrolling or compact cell sizing, non-overlapping chart legends/labels, improved spacing, typography, information density, and desktop usability without browser zoom changes were completed, absorbed, and accepted within `DA-IMPL-007`.

## AUTO-IMPORT-001 DKCL Sync Status

- `Atomic importer claim`: `COMPLETED / VERIFIED`.
- Root cause: multiple backend/watcher instances could process the same `Incoming` file.
- Fix: atomic move from `Incoming` to `Processing`; only the winning process continues.
- Real verification: `F1.3-2026.02.01.xlsx` imported `2374` rows with `2374` distinct shipment codes, exactly `1 SUCCESS` log, `0` error/skipped rows, and no duplicate or trailing `FAILED` log.
- Follow-on status: Hue manual missing-date/backfill was completed under `AUTO-IMPORT-003`; TCT source discovery and controlled one-date import were completed under `AUTO-IMPORT-004`; TCT manual backfill and shared DKCL background operations were completed under `AUTO-IMPORT-005`.

## AUTO-IMPORT-002 Hue F1.3 Acquisition Status

- Backend/manual-trigger engine: `IMPLEMENTED`.
- Persistent Hue browser profile: `IMPLEMENTED`.
- One automatic username/password/fixed-HRM login attempt: `IMPLEMENTED`.
- Portal export cleanup: `IMPLEMENTED`.
- API added: `POST /api/import/dkcl/hue/f13/sync`.
- Status endpoint added: `GET /api/import/dkcl/hue/f13/sync/:runId`.
- Controlled live verification: `PASSED for 2026-07-16`.
- Visible metric/detail population, workbook rows, imported database rows, distinct shipment codes, and Dashboard backend `total_bg`: `3941`.
- Import logs: exactly `1 SUCCESS`; skipped/error rows: `0`.
- Product Owner decision: `COMPLETED / PO PASS`.
- Out of scope remains inactive: Data Import Center UI, daily scheduler, TCT workflow, KPI business-rule changes.

## AUTO-IMPORT-003 Operations UI Status

- Ticket status: `COMPLETED / PO PASS`.
- Scope: extend Data Import Center for Hue F1.3 missing-date scan, manual backfill, sequential queue, retry/stop, progress/status monitoring, and safe import/export/validation/error evidence.
- Accepted operational condition: manual Hue F1.3 backfill requires a valid DKCL authenticated session. The operator does not need to log in for every `Update` while the session remains valid. If the DKCL session is expired or invalid, queue creation is blocked before `RUNNING` and the operator is instructed to re-authenticate.
- Not included: automatic login, credential storage, or additional DKCL session persistence.

## AUTO-IMPORT-004 TCT Source Discovery and Nationwide Ranking Contract Status

- Product Owner activation decision recorded on `2026-07-20`.
- Ticket status: `COMPLETED / PO PASS`.
- Checkpoint 001 status: `COMPLETED - DISCOVERY / CONTRACT DEFINED`.
- Checkpoint 002 status: `COMPLETED - CONTROLLED IMPORT VALIDATED`.
- Accepted evidence for `2026-07-19`: ranked population `34`; Hue volume `2,399`; Hue pass `1,261`; Hue KPI `52.56%`; Hue nationwide rank `24/34`; TCT workbook downloaded, imported, and deleted successfully; Dashboard result accepted by Product Owner.
- Boundaries: no TCT importer implementation in Checkpoint 001; no Dashboard UI changes; no KPI formula changes; no ranking-rule changes; no SSOT/schema changes; no AUTO-IMPORT-002/003 behavior changes; no BCVH mapping changes; no Architecture Freeze changes; no scheduling or force replacement.
- Evidence: `docs/06_REVIEWS/Import/AUTO-IMPORT-004_CHECKPOINT_001.md`, `docs/06_REVIEWS/Import/AUTO-IMPORT-004_CHECKPOINT_002.md`, `docs/06_REVIEWS/Import/AUTO-IMPORT-004_PO_ACCEPTANCE.md`.
- Manifest: `docs/10_TICKETS/AUTO-IMPORT-004_MANIFEST.md`.

## AUTO-IMPORT-005 TCT Manual Backfill and Shared DKCL Background Operations Status

- Product Owner activation decision recorded on `2026-07-20`.
- Ticket status: `COMPLETED / PO PASS`.
- Checkpoint 001 status: `COMPLETED - DISCOVERY / CONTRACT DEFINED`.
- Checkpoint 002 status: `COMPLETED - TECHNICAL PASS`.
- Checkpoint 003 status: `PO ACCEPTED`.
- Scope: shared Hue/TCT DKCL browser/session workflow, interactive authentication mode, background operation mode, TCT F1.3 manual backfill in Data Import Center, TCT coverage/missing-date scan, sequential in-memory queue, graceful Stop, eligible Retry, workbook lifecycle cleanup, and evidence contract.
- Product Owner accepted TCT `Update`, one-active-queue sequential in-memory processing, pre-queue TCT session validation, explicit interactive TCT re-authentication action, graceful Stop, eligible Retry, background portal workflow, workbook validation, 34-unit national import handoff, queue/item evidence, temp workbook cleanup, Hue/TCT source separation, and cumulative range-based nationwide ranking.
- Boundaries: no unattended scheduling; no automatic all-missing-date import; no credential storage; no automatic credential-based login; no SSOT/KPI/ranking/population changes; no Dashboard UI changes except Data Import Center TCT controls/session status; no broad schema, BCVH, or AUTO-IMPORT-002/003 behavior changes.
- Evidence: `docs/06_REVIEWS/Import/AUTO-IMPORT-005_CHECKPOINT_001.md`; `docs/06_REVIEWS/Import/AUTO-IMPORT-005_CHECKPOINT_002.md`; `docs/06_REVIEWS/Import/AUTO-IMPORT-005_CHECKPOINT_003.md`.
- Manifest: `docs/10_TICKETS/AUTO-IMPORT-005_MANIFEST.md`.

## DA-IMPL-006 Unified Action Center Status

- Product Owner activation decision recorded on `2026-07-20`.
- Ticket status: `COMPLETED / PO PASS`.
- Checkpoint 001 status: `COMPLETED - DISCOVERY / CONTRACT DEFINED`.
- Checkpoint 002 status: `COMPLETED - TECHNICAL PASS`.
- Checkpoint 003 status: `COMPLETED - PO ACCEPTED`.
- Final PO remediation removed the Dashboard `BCVH nổi bật và cần cải thiện` / Top 2 area and removed `Tin điều hành` / `Tin báo cáo` message drafts from Dashboard.
- Dashboard retains Unified Action Center recommendations/issues, KPI context, evidence, and follow-up only.
- Future message building/viewing/management belongs in a future governed `BCVH Ranking` module ticket.
- Manifest: `docs/10_TICKETS/DA-IMPL-006_MANIFEST.md`.
- Next active ticket: `None`.

## DA-IMPL-007 Smart Dashboard Final Assembly Status

- Ticket status: `COMPLETED / PO PASS`.
- Primary executor: `Antigravity`.
- Code commit range: `670927a81716732137ac535dc13a6f418503e247..f9a62f5` (remediated up to `f9a62f5`).
- PO decision: `FINAL PO PASS`.

## AUTO-IMPORT-006 Unified DKCL Authentication Lifecycle Recovery Status

- Ticket status: `COMPLETED THROUGH TCT UNFINISHED BULK-SELECTION PO PASS`.
- Technical Status: `PASS`.
- Runtime Status: `TCT UNFINISHED BULK-SELECTION PO PASS`.
- PO UI Check Required: `No for completed TCT bulk-selection`.
- PO Product Status: `NOT READY`.
- Current Phase: `REMEDIATION-006 / CLOSED FOR AUTO-IMPORT-007 PLAN ACTIVATION`.
- Last Reviewed Phase: `TCT unfinished bulk-selection`.
- Last Reviewed Commit: `313b16a2f0e3259562681d26a581e5c9f2bba960`.
- Phase Review Status: `PO PASS`.
- Next Phase Authorization: `AUTO-IMPORT-007 ACTIVE / PLAN ONLY, discovery only`.
- Primary executor: `Antigravity`.
- Scope: separate HUE login trigger, block automatic interactive login in HUE queue check, resolve TCT interactive client factory configuration omission, CDP best-effort minimize, and browser manual closed listener/Registry.
- Technical evidence: all unit tests passed for sync and backfill service components, oxlint passed with zero errors, production bundle built successfully.
- Remediation 001: Resolved initialization crash on `/import` due to lexical declaration order of `preflightHueSession` and `preflightTctSession`. Added frontend source-contract test validation.
- Remediation 002: Implemented responsive layout, restored Re-Update for complete dates (HUE/TCT), and cleaned up duplicate TCT login buttons. Phase changes were not separated atomically (see Checkpoint 004). PO runtime verification has not yet been performed.
- Remediation 003 / R2.1 Remediation A: initial attempt `7ac4fb3` was `REVIEW FAIL`; actual root cause was stale local backend returning `selectable:false`; follow-up commit `3e3309bf959582c681f4a81f319f7128fcde7f87` wired production to shared Hue selection helpers; runtime DOM smoke confirmed checkbox selection and `Re-Update (1)`; no portal login or submission was performed.
- Remediation 003 / R2.2 TCT Login Lifecycle: root cause was preflight collapsing an in-progress interactive session into expired/auth-required handling; commit `220123d7defa040d340d39750b37b6cba3950301` adds `LOGIN_IN_PROGRESS`; controller maps it to HTTP `202`; polling preserves the client; duplicate auth uses one `openingPromise`; frontend disables the button and displays `Đang mở đăng nhập...`; code review passed; stable headed-browser behavior remains pending PO runtime verification.
- Earlier PO runtime recheck authorization for login lifecycle is not the current next action; native HUE/TCT window-hide runtime validation remains deferred until re-authentication is required.
- Historical login-lifecycle recheck details are retained in the AUTO-IMPORT-006 manifest/checkpoint history and must not be treated as newly authorized work without explicit PO direction.
- Responsive recheck must verify 100% zoom, visible and usable controls, and no page-level clipping.
- Remediation 004 / R4.1: commit `8c22374...` was `REVIEW FAIL`.
- Remediation 004 / R4.1A: commit `dd0d9f94...` was `REVIEW FAIL`.
- Remediation 004 / R4.1B: commit `58fb723e9c5eeb82f17b75d14b7662c3503ee262` was `REVIEW PASS`. Browser profile handling follows the five-state profile classification contract; `interactiveAuthenticate()` and `recover()` use `_classifyLockState()`; neither function calls `terminateProcessTree()`; cleanup is allowed only for `STALE_CONFIRMED`.
- Remediation 005A: commit `52d25e5310550631a8211aead577442994687787` was `REVIEW FAIL` because it treated CDP `Browser.getWindowForTarget().windowId` as a native Windows `HWND`.
- Remediation 005B: replaces the blocked PowerShell native-window bridge with direct Win32 calls through minimal prebuilt Node FFI dependency `koffi`; authority remains `exact --user-data-dir` -> `owned PID tree` -> `owned HWND`; controlled non-portal headed Chromium smoke proved hide to `IsWindowVisible=false`, browser/page usability after hide, and restore to `IsWindowVisible=true`.
- Historical AUTO-IMPORT-006 archive chain is `README_AI.md` -> `docs/01_GOVERNANCE/PROJECT_SNAPSHOT.md` -> `docs/10_TICKETS/AUTO-IMPORT-006_MANIFEST.md` -> `docs/06_REVIEWS/Import/AUTO-IMPORT-006_CHECKPOINT_009.md`; current fresh-chat onboarding is owned by `AUTO-IMPORT-007`.
- Latest PO PASS: TCT unfinished bulk-selection at commit `313b16a2f0e3259562681d26a581e5c9f2bba960`; no backend/import execution/Dashboard/KPI/authentication changes were made for that defect; no next action is authorized after this PO PASS.

## F13-ROUTE-RANKING-REDESIGN-PLAN Activation

- Ticket status: `ACTIVE / DISCOVERY-PLANNING ONLY / NO IMPLEMENTATION AUTHORITY`.
- Activation date: `2026-08-03`. Baseline: `7fd33ce130227a0c2b24d3b36aa0980bf8fc9ad3`.
- Authority: explicit Product Owner authorization for discovery/planning only, following closure of `F13-UI-AUDIT-PLAN` with `Current Ticket = None`.
- Scope: delta-only discovery — verified baseline, minimum templates/rules required to activate the ticket, and Route Ranking source files with direct dependencies. Dashboard, BCVH Ranking, Import, and closed tickets were not reopened or inspected.
- Result: manifest `docs/10_TICKETS/F13-ROUTE-RANKING-REDESIGN-PLAN_MANIFEST.md` records the current live Route Ranking implementation inventory (frontend `RoutePerformancePage.jsx` at `/f13/ranking/route`, `F13DashboardClient.getRouteRanking`, backend `DashboardController`/`F13DashboardService`/`FactBuuGuiRepository` chain) and the accepted filter/classification contract carried forward from `F13-INTERNAL-ROUTE-AUDIT_MANIFEST.md`.
- No product code was changed.

## F13-ROUTE-RANKING-REDESIGN-PLAN Objective Confirmed

- Date: `2026-08-03`.
- Product Owner confirmed the redesign objective: an operational decision-support tool identifying which routes need intervention, priority level, root cause, responsible postman, and next action.
- Product Owner reconfirmed the full accepted filter/classification contract (`Tuyến bưu tá | Tất cả`, Hue `53%` scope, 7 confirmed internal-counter routes) must be preserved unchanged.
- Route -> Shipment drill-down is design-only for this redesign; runtime implementation remains not authorized pending its own separate validation/authorization.
- No design plan was produced, no product code was changed, and no implementation ticket was created or activated by this update.

## F13-ROUTE-RANKING-REDESIGN-PLAN Closure and F13-ROUTE-RANKING-REDESIGN-IMPL Activation

- Date: `2026-08-03`.
- Design plan basis: static code inspection by Antigravity (`docs/F13_ROUTE_RANKING_EVIDENCE_HANDOFF.md`, browser subagent `RESOURCE_EXHAUSTED`, no runtime evidence) plus targeted read-only data discovery by Claude Code-Opus against `backend/src/db/database.sqlite` (659,454 rows in `fact_f13`).
- Key data findings: no postman/employee field or route-to-postman mapping exists anywhere in the database (6 tables total); `ket_qua_f13` is only `{Dat, Khong dat, NULL}` and is not a root-cause field; unevaluated shipments are 5.03% overall but 93.78% concentrated in the 7 catalog routes and 0.49% in postman routes; `sys_kpi_thresholds` exists but is read by no backend code and its seed rows are duplicated 3x; `loai_tuyen_phat` is 100% populated, 1 value per route; max routes per BCVH per day across all history is 35 (page_size=1000 confirmed safe for client-side sort); frontend default date `2026-06-23` is 41 days stale versus latest valid data `2026-08-02`.
- Product Owner approved the design plan; ChatGPT/CTO finalized implementation scope, explicitly cancelling all inferred priority-tier/threshold logic (no `sys_kpi_thresholds` usage, no color tiers/priority labels, no `<60%` intervention threshold, no `<=20%` unevaluated-exclusion rule). Locked decisions: default sort `Ty le dat DESC`; full column set `Tong BG / Dat / Khong dat / Chua danh gia / Ty le dat`; KPI row = so tuyen phat sinh khong dat, ty le dat toan BCVH (`SUM dat / SUM tong BG`), tong BG khong dat, tong so tuyen; new filter `Chi tuyen co buu gui khong dat`; no Shipment drill-down UI in any form (not even disabled) in MVP; default date resolves to latest valid date <= current date excluding future-dated garbage rows; full filter/classification contract and `routeRankingFilters.js` unchanged.
- Deferred (insufficient data, explicit): buu ta phu trach (no data source), root cause (no reason field exists), Shipment drill-down runtime (needs separate authorization), date-range/trend (backend accepts single date only).
- Known defects noted but explicitly out of remediation scope for this ticket: `sys_kpi_thresholds` 3x-duplicated seed rows; 4 garbage future-dated rows in `fact_f13` (up to 2098-02-18) — only the minimum date filtering needed for this screen's default is in scope.
- `docs/10_TICKETS/F13-ROUTE-RANKING-REDESIGN-PLAN_MANIFEST.md` closed `COMPLETED / CLOSED / PO APPROVED / CTO FINALIZED`. Locked scope recorded in `docs/06_REVIEWS/Route/F13_ROUTE_RANKING_REDESIGN_PLAN_CHECKPOINT_001.md`.
- `docs/10_TICKETS/F13-ROUTE-RANKING-REDESIGN-IMPL_MANIFEST.md` created and activated, executor `Claude Code-Sonnet`, `PO UI Check Required = Yes` before closure.
- No product code was changed by this governance update.

## F13-ROUTE-RANKING-REDESIGN-IMPL Closure

- Date: `2026-08-03`. Ticket status: `COMPLETED / PO PASS / CLOSED`. Final implementation commit: `43819b9272910fcaddb9c058c97913648b10654d`.
- PO PASS confirmed on all 12 items across the ticket's lifecycle: default sort `Ty le dat DESC`, full column set, 4-card KPI row, only-failed filter, two-column layout, no forbidden inference (Items 1, 3-9, commit `ee73feed`); `BLACK` = `Chuyen hoan` naming/meaning correction (Item 2, commit `6133a46`); delayed-cash table columns and selected-route panel (Item 10, commit `62753c0`, numbers corrected via a targeted scope-extension request then a shared-engine SSOT sync, re-confirmed PO PASS on commit `4e80fdfd`); `BG CHAM NOP TIEN` KPI widget bound strictly to the backend aggregate (Item 11, commit `2a0a06d`); BCVH Ranking default-date fix so its filter resolves to the latest date with real data instead of a hardcoded stale date (Item 12, commit `43819b9`).
- Key technical corrections along the way: a stale (never-restarted) backend process caused an apparent all-zero runtime failure that was diagnosed as operational, not a code defect (no product code changed for that fix); the delayed-cash denominator was corrected to exclude `Chuyen hoan` (BLACK) and this correction was synced from a Route-Ranking-local filter into the shared `RuleF13302`/`RuleRegistry` engine so BCVH Ranking and Route Ranking now compute delayed-cash identically (BCVH Ranking's own numbers changed too, confirmed via real HTTP with zero regression to its existing test suite).
- Deferred items remain open, no next ticket authorized: buu ta phu trach (no data source in `fact_f13`, no mapping table anywhere in the database), root cause/nguyen nhan F1.3 (no reason field exists), Route -> Shipment drill-down runtime (design-only per PO, needs separate authorization), date-range/trend comparison (backend accepts a single evaluation date only).
- Full closure detail: `docs/10_TICKETS/F13-ROUTE-RANKING-REDESIGN-IMPL_MANIFEST.md` Section 16 Closure; `docs/06_REVIEWS/Route/F13_ROUTE_RANKING_REDESIGN_PLAN_CHECKPOINT_001.md` Section 18.
- No product code was changed by this governance closure update itself.
- Date: `2026-08-04`. Ticket `F13-DATABASE-PRODUCT-OPPORTUNITY-AUDIT-PLAN` activated by PO planning authorization and completed as a read-only audit. Status: `READY FOR PO DATABASE AUDIT REVIEW`. Audited `backend/src/db/database.sqlite` in `OPEN_READONLY` mode (5 tables; `fact_f13` 663,130 rows x 45 columns; usable history `2026-01-01`-`2026-08-03`, 213 days; `fact_f13_national` 6,766 rows across 34 provinces), the full API surface (33 endpoints across `/api/auth`, `/api/f13`, `/api/import`), and all 7 F1.3 screens. Key findings: origin-handover-to-delivery latency averages `10.97h` on passing vs `47.68h` on failing shipments across 595,046 complete chains and is exposed on no screen; 10 of 17,012 customer accounts carry 78,091 of 208,121 failures (37.5%); 46 of 154 routes failed above 40% on 20+ of the last 60 days; route type spreads failure 12.66%-63.05%; Pareto/RCA, Evidence, and Message Center render `PlaceholderPage` despite working backends. Recommendations: BUILD Pareto/RCA, MERGE Evidence into Shipment Ranking, HIDE Message Center (no message lifecycle state exists in the database), REMOVE 5 orphaned `pages/F13*.jsx` files, KEEP the 3 working ranking/dashboard surfaces. 18 opportunities ranked (`OPP-01`-`OPP-18`), 8 data-quality defects catalogued (`DQ-01`-`DQ-08`, including a duration column stored as TEXT whose MIN/MAX return silently wrong values), 1 latent API path defect found (`API-01`: `getKpi` and `getPareto` omit the `/f13` prefix and would 404), and 12 missing-data items registered (`MD-01`-`MD-12`) for PO decision rather than inferred. No schema, data, index, or product code was changed by this ticket. No implementation ticket opened; three PO decisions gate the proposed Wave 1. Detail: `docs/06_REVIEWS/Shared/F13-DATABASE-PRODUCT-OPPORTUNITY-AUDIT_CHECKPOINT_001.md`; manifest `docs/10_TICKETS/F13-DATABASE-PRODUCT-OPPORTUNITY-AUDIT-PLAN_MANIFEST.md`.
- Date: `2026-08-04`. Ticket `F13-DATABASE-PRODUCT-OPPORTUNITY-AUDIT-PLAN` CLOSED on Product Owner decisions. PO issued four authoritative rulings: (1) `danh_gia_2026` is the authoritative F1.3 result field, FINAL — `ket_qua_f13` is a technical/reference field only, production KPI logic must not be switched to it, and `MD-01` is closed and not to be reopened; (2) the import rule requiring records sharing the authoritative business key to be overwritten/upserted rather than appended is already decided and not reopened; (3) permanent removal of year-2098 test/future data is authorized; (4) the duplicate count is a technical validation item, not a PO decision. Duplicate revalidation performed as required: the audit query grouped by `ma_bg` alone, which is NOT the business key — the real key is `UNIQUE(ngay_do_kiem, ma_bg)`, declared in the `fact_f13` DDL and enforced as `sqlite_autoindex_fact_f13_1`, and matched by `INSERT OR IGNORE` in `importProcessor.js` with date-level replacement. On the real key there are ZERO duplicates and ZERO exact full-row duplicates; the 9,348 repeated `ma_bg` are single shipments evaluated across 2 dates (6,008) or 3 dates (3,340) — legitimate multiple operational records. The 12,688-row duplicate claim was false and `DQ-07` is RETRACTED; no genuine duplicates exist despite the overwrite rule, so no cause report or row modification was required. Numbering note: the PO instruction referenced "DQ-06", but the duplicate finding is `DQ-07`; `DQ-06` is the separate weight-unit item and remains open and untouched. `DQ-04` resolved by decision; `MD-01`, `MD-05`, `MD-06` closed. Confirmed open defects drop from eight to six. Audit surface recommendations were not amended; MERGE (Evidence) and HIDE (Message Center) confirmations remain outstanding.
- Date: `2026-08-04`. Ticket `F13-DATA-2098-CLEANUP-IMPL` activated under PO authorization and executed. Status: `READY FOR PO DATA CLEANUP RECHECK`. All six date-bearing fields in `fact_f13` were scanned for year 2098 (`ngay_do_kiem` in ISO form plus five `dd/MM/yyyy` event timestamps); zero 2098 values existed in any event timestamp, proving the predicate `ngay_do_kiem LIKE '2098%'` both complete (0 rows missed) and precise. Backup taken via `VACUUM INTO` (backend was live, so a file copy risked a torn page) to `backend/src/db/backups/database.pre-2098-cleanup.2026-08-04.sqlite`, 543,047,680 bytes, `integrity_check = ok`, row counts verified against live state before any write. Deletion ran in a single `BEGIN IMMEDIATE` transaction with two pre-commit guards: 4 rows from `fact_f13` and 4 rows from `import_log`, transaction COMMITTED. Post-delete verification: zero 2098 rows in `fact_f13`, `import_log`, and `fact_f13_national`; zero `BCVH TEST` rows (all four matched the 2098 predicate and none existed outside it, satisfying the scope restriction); date range now `2026-01-01`-`2026-08-03`; 2026 rows 663,126 and 213 days both unchanged with every month's count identical to the audit baseline; `integrity_check = ok`; zero orphaned fact rows; zero duplicates on the business key. Authoritative KPI `danh_gia_2026` unchanged to four decimals at `58.6233%` (637,445 evaluated, 373,691 Đạt) and representative BCVH ranking intact. Side effect: `DQ-03` BCVH code/name collision closed (distinct names 10 -> 9, matching 9 codes); `DQ-01` closed. No product code, frontend surface, `fact_f13_national`, `sys_kpi_thresholds`, or `system_config` was modified, and no deduplication was performed. Two residuals reported but NOT acted on as out of scope: `RESIDUAL-01` (high) — `ruleEngineService.js` computes live recommendation KPIs on the non-authoritative `ket_qua_f13`, making `GET /f13/recommendations` systematically optimistic by up to 5.58 points per BCVH (province-wide 63.4988% vs authoritative 58.6233%); the divergence pre-existed the PO decision and needs a separate authorized ticket. `RESIDUAL-02` (low) — `FactBuuGuiRepository.js` inserts `session_id` and `extended_data` columns that do not exist in the `fact_f13` schema. Detail: `docs/06_REVIEWS/Shared/F13-DATA-2098-CLEANUP-IMPL_CHECKPOINT_001.md`; manifest `docs/10_TICKETS/F13-DATA-2098-CLEANUP-IMPL_MANIFEST.md`.
- Date: `2026-08-04`. Ticket `F13-DATA-2098-CLEANUP-IMPL` CLOSED as `COMPLETED / TECHNICAL PASS / CLOSED` by CTO review; reviewed implementation commit `3b605beb7ed2deeae239dbb050cf9b03fbad9c43`. Authoritative closure result: 4 `fact_f13` rows deleted; 4 `import_log` rows deleted; zero year-2098 rows remain; zero `BCVH TEST` rows remain; 2026 remains unchanged at 663,126 rows / 213 days; authoritative `danh_gia_2026` KPI remains `58.6233%`; the pre-cleanup backup at `backend/src/db/backups/database.pre-2098-cleanup.2026-08-04.sqlite` is retained and must not be deleted. Duplicate finding confirmed as `DQ-07` and RETRACTED because `ma_bg` alone is not the business key; the real enforced key is `UNIQUE(ngay_do_kiem, ma_bg)` and zero duplicates exist on it. `DQ-06` is the weight-unit item and remains open and untouched. Defect state after cleanup: `DQ-01` CLOSED by the 2098 cleanup; `DQ-02` OPEN; `DQ-03` CLOSED because `BCVH TEST` was removed; `DQ-04` RESOLVED by the Product Owner decision that `danh_gia_2026` is authoritative; `DQ-05` OPEN; `DQ-06` OPEN; `DQ-07` RETRACTED; `DQ-08` OPEN. **The confirmed open defect count is FOUR: `DQ-02`, `DQ-05`, `DQ-06`, `DQ-08`** — this figure supersedes the "eight to six" count recorded in the preceding audit-closure entry, which described the state before this cleanup. This closure is documentation-only: no operational database, backup database, schema, backend, frontend, or product runtime was modified. No ticket is activated. Repository state returns to `NO ACTIVE TICKET / AWAITING PRODUCT OWNER DIRECTION`. Next-direction candidates recorded as candidates only, not authorized: (1) remediate `RESIDUAL-01`, because the live recommendations path uses the non-authoritative `ket_qua_f13` and may display KPI values inconsistent with `danh_gia_2026`; (2) `F13-SURFACE-CLEANUP-PLAN` covering Evidence merge, Message Center hide, Vietnamese Shipment Ranking naming, redirect behavior, and verified orphan-page removal; (3) Pareto product design later, distinguishing Pareto analysis from true RCA. Evidence MERGE and Message Center HIDE remain pending explicit Product Owner confirmation and must not be inferred. Detail: `docs/10_TICKETS/F13-DATA-2098-CLEANUP-IMPL_MANIFEST.md` Section 16; `docs/06_REVIEWS/Shared/F13-DATA-2098-CLEANUP-IMPL_CHECKPOINT_001.md` Section 11.

## F13-STANDARDIZATION-001 Program Activation

- Date: `2026-08-04`. Program `F13-STANDARDIZATION-001` activated under explicit Product Owner authorization to standardize the F1.3 module group. This is a documentation-only activation: no product code, database, or runtime was changed. Program state set to `ACTIVE / AUTHORIZED`. A single locked five-phase plan was written into one manifest, `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md`: Phase 0 Khóa nền số liệu (`AUTHORIZED / READY FOR IMPLEMENTATION` — recommend the standard KPI field `danh_gia_2026`, verify/correct only already-audited data/API obstacles serving this program, lock one unified KPI source); Phase 1 Chuẩn hóa cấu trúc F1.3 (naming/navigation standardization, Evidence kept as the official shipment detail screen, old-path redirects, Message Center temporarily hidden, legacy pages removed/isolated, PO Gate 1 after close); Phase 2 Hoàn thiện điều hành (Operation Dashboard/BCVH Ranking/Route Ranking — audited-data views only, BCVH small-sample warning, chronic route-failure identification, route-type comparison, cross-module consistency, PO Gate 2 after close); Phase 3 Pareto và Evidence (Pareto across verified dimensions, Evidence as the single verification lookup, filter preservation, explicit non-RCA disclaimer); Phase 4 Regression và đóng F1.3 (cross-module/API/nav/access/performance/responsive checks, final PO acceptance, governance closure, PO Gate 3 after close). Phases 1-4 are `PLANNED / NOT ACTIVE`; only Phase 0 is open, and no Phase 0 work was performed under this activation step. Locked product decisions and locked out-of-scope items are recorded once in the manifest (Sections 7-8): Evidence is the official shipment detail screen; Message Center hidden and deferred; Pareto is not RCA; no feature without reliable data; no expansion beyond F1.3; Data Import reconciliation excluded; sequential phases within one program; no new ticket required per Phase if exit criteria are met; only PO may change scope. `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` was created as the current checkpoint. `docs/01_GOVERNANCE/PROJECT_SNAPSHOT.md`, `docs/01_GOVERNANCE/DOCUMENT_INDEX.md`, and `README_AI.md` were synchronized to point to this program as the active ticket. Exact next authorized action: `Begin bounded delta-only discovery for Phase 0. Do not implement until onboarding and Phase 0 scope confirmation are complete.` Prior tickets `F13-DATA-2098-CLEANUP-IMPL`, `F13-DATABASE-PRODUCT-OPPORTUNITY-AUDIT-PLAN`, and `F13-SHARED-NAV-FILTERS-IMPL` remain `CLOSED` and were not reopened.

## F13-STANDARDIZATION-001 — Tuyến Ranking (Route Ranking) Delta Closure

- Date: `2026-08-04`. Following Phase 0 discovery and implementation (commit `e3ca24292f39b5c59022b161b63c4603cced1949`: `danh_gia_2026` KPI-field standardization in the recommendations engine, remediating the previously-recorded `RESIDUAL-01`; the two audited `/f13`-prefix API path fixes; `dd/MM/yyyy HH:mm:ss` timestamp-parsing fix), the Product Owner authorized a bounded delta scope covering only the Tuyến Ranking (Route Ranking) screen and its violation drill-down, executed as an in-branch delta under the active `F13-STANDARDIZATION-001` program rather than as a separately named ticket. Implementation chain: `a0d4b041573798b08eb2992698bdc9cc20031083` (Route Ranking data-contract standardization: date/BCVH parameter-mismatch fix, `date`/`ma_bcvh`/`ten_bcvh`/`buu_ta` fields, first violation drill-down); `a892a276310705920cb298264ebfeb2db3ae64da` (violation-reason classification into `Chậm nộp tiền` / `Không đạt khác` / `Chưa xác định nguyên nhân`, reusing the existing `RULE_F13_302` SSOT threshold, plus the corresponding `/f13/evidence-list` API contract — `violation_reason`, `meta.violation_summary`, `reason` filter); `6e5753089ccda7b4f90706c32ed1482be3aadb12` (Antigravity UI/UX refinement of the Tuyến Ranking table and violation drill-down detail window); `03ce28bacc36b49d961caa1c006a011beb804bc7` (frontend pagination `10 tuyến/trang` and default ascending `passed_rate` sort). Product Owner runtime-tested Tuyến Ranking and its violation drill-down and confirmed: pagination `10 tuyến/trang` correct; default sort ascending by `Tỷ lệ đạt` correct, weakest-performing route ranked first; page navigation correct; reconciliation (đối soát) table correct. Result: `PO PASS / CLOSED`, latest PO-tested implementation commit `03ce28bacc36b49d961caa1c006a011beb804bc7`. Scope discipline: this closure covers only Tuyến Ranking and its violation drill-down — it does not close Operation Dashboard, BCVH Ranking, Pareto/RCA, Evidence, Message Center, or Shipment Performance Center, and it does not close Phase 0, Phase 1, Phase 2 in full, Phase 3, Phase 4, or the `F13-STANDARDIZATION-001` program as a whole; Phase 0's foundational items remain implemented and technically validated but not separately PO-runtime-confirmed as their own closure. The Shipment Performance Center delta remains preserved untouched in git stash (`F13-SHIPMENT-001: preserved Shipment Performance Center delay/status changes`), pending Product Owner reactivation of the deferred `F13-SHIPMENT-001` ticket; that stash and the separate pre-existing-HTML stash were both left untouched by this closure. No next ticket or Phase is activated by this closure. Repository state returns to `NO ACTIVE TICKET / AWAITING PRODUCT OWNER DIRECTION`. Detail: `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 16; `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` Section 11.

## NETWORK-MANAGEMENT-001 Program Activation

- Date: `2026-08-04`. Ticket `NETWORK-MANAGEMENT-001` (Quản lý mạng lưới) activated under explicit Product Owner authorization naming three independent screens — Mạng điểm phục vụ, Mạng đường thư cấp 2, Sơ đồ tuyến phát — to become SQLite-backed, authenticated QIS V2 modules (`admin`+`viewer` read access; `admin`-only Import at both API and UI, with preview, error/duplicate validation, file fingerprint, and persisted history). This is a documentation-only activation: no product code, database, or runtime was changed. Program state set to `ACTIVE / AUTHORIZED / READY FOR PHASE 1 IMPLEMENTATION`. A locked four-phase plan was written into one manifest, `docs/10_TICKETS/NETWORK-MANAGEMENT-001_MANIFEST.md`: Phase 1 Nền tảng (`AUTHORIZED / READY FOR IMPLEMENTATION` — SQLite schema for the three independent modules, authenticated API foundation reusing existing `requireAuth`/`requireRole` middleware); Phase 2 Ba bản đồ (the three map screens, each opened from its own entry, Sơ đồ tuyến phát querying only after Ngày+BCVH+Bưu tá are all selected, no bulk-loading a full month client-side, no public HTML data exposure); Phase 3 Import (admin-only Excel import per module, each with its own update rule, sequential-by-month append-only for Sơ đồ tuyến phát); Phase 4 Nghiệm thu (cross-module regression and final PO acceptance). Phases 2-4 are `PLANNED / NOT ACTIVE`; only Phase 1 is open, and no Phase 1 work was performed under this activation step. The Product Owner locked the audit baseline used to scope the ticket, recorded once in the manifest (Section 7) and not duplicated elsewhere: Mạng điểm phục vụ 151 unique mã điểm from 260 audited Excel rows (109 excluded as `Tạm dừng`), source Excel to be supplied by the Product Owner for column-mapping verification before Phase 3 import logic is built; Mạng đường thư cấp 2 28 hành trình / 148 lượt dừng / 47 mã điểm / tổng cự ly 1.435 km, seeded temporarily from the existing reference HTML with no business Excel source yet; Sơ đồ tuyến phát 143,467 điểm after prioritizing records with valid coordinates, from a tháng 06/2026 Excel already audited by the Product Owner outside this repository, to be returned to the workspace when Phase 3 needs it. Claude Code must not re-audit these Excel sources or guess column mapping in any future execution step under this ticket. `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-001_CHECKPOINT_001.md` was created as the current checkpoint. `docs/01_GOVERNANCE/PROJECT_SNAPSHOT.md`, `docs/01_GOVERNANCE/DOCUMENT_INDEX.md`, and `README_AI.md` were synchronized to point to this ticket as the active ticket. Exact next authorized action: `Begin NETWORK-MANAGEMENT-001 Phase 1 (Nền tảng) implementation. Do not begin Phase 2 or Phase 3 until Phase 1 closes.` The prior `F13-STANDARDIZATION-001` program (Tuyến Ranking delta `PO PASS / CLOSED`; Phase 0 implemented, not separately closed; Phases 1-4 `PLANNED / NOT ACTIVE`) remains untouched and unrelated to this activation. 02 pre-existing git stashes (`F13-SHIPMENT-001` deferred delta; pre-existing HTML maps) and the three source HTML files at repository root (`Ban_do_mang_diem_phuc_vu_BDTP_Hue.html`, `Ban_do_mang_diem_phuc_vu_tich_hop_Duong_thu_cap_2.html`, `ban_do_duong_giao_thong_bcvh_postman_06_2026.html`) remain untouched.

## NETWORK-MANAGEMENT-001 Phase 1 (Nền tảng) Implementation

- Date: `2026-08-05`. Under explicit Product Owner authorization to implement `NETWORK-MANAGEMENT-001` Phase 1 (Nền tảng), Claude Code added a SQLite schema foundation and an authenticated API foundation for the three independent Quản lý mạng lưới screens (Mạng điểm phục vụ, Mạng đường thư cấp 2, Sơ đồ tuyến phát). Status: `COMPLETED / TECHNICAL PASS — READY FOR PO GATE 1`. Backend: `backend/src/db/schema.sql` extended additively with 5 new tables — `network_import_log` (shared import history, `UNIQUE(module, file_fingerprint)`), `network_service_point`, `network_level2_route` + `network_level2_route_stop`, `network_delivery_point` — applied to the live operational database via the idempotent `backend/migrate_network_management_001_phase1_schema.js`; zero business rows seeded, and `fact_f13` confirmed unchanged at 663,126 rows before and after. `backend/src/controllers/NetworkMapController.js` + `backend/src/routes/networkMapRoutes.js` mounted at `/api/network-map` in `backend/server.js`: read endpoints gated `admin`+`viewer`, three `POST .../import` endpoints gated `admin`-only and returning `501 NOT_IMPLEMENTED` (Excel logic deferred to Phase 3), and `GET /delivery-routes/points` hard-rejects any query missing Ngày/BCVH/Bưu tá with `400 MISSING_REQUIRED_FILTER` so no full-month scan is possible. Frontend: new `Quản lý mạng lưới` nav group (`frontend/src/navigation/appNavigation.jsx`) with 3 subItems visible to both roles; 3 new routes in `frontend/src/App.jsx` under `/network-map/*` (`ProtectedRoute allowedRoles={[ROLE_ADMIN, ROLE_VIEWER]}`) plus the same 3 paths added to `VIEWER_ALLOWED_PATH_PREFIXES`; `frontend/src/api/NetworkMapClient.js`; `frontend/src/features/networkMap/` scaffold screens (`ServicePointsPage`, `Level2RoutesPage`, `DeliveryRoutesPage` sharing `NetworkFoundationPage`) that call the read API to prove connectivity and show an admin-only disabled "Import (Phase 3)" button via `useAuth()`/`isAdminRole()` — no Leaflet map, no Excel import, no OSRM call, matching the manifest's mandatory Phase 1 exclusions. Validation: `node --test` 20/20 pass on backend (schema migration idempotency/zero-row assertions, controller auth/role/required-filter enforcement, route-wiring contract) and 12/12 pass on frontend (role path allow-list, nav contract, App route contract, API client contract including local rejection of incomplete tuyến-phát queries before any network call); `oxlint` clean on all new/changed frontend files; `vite build` succeeds. A broader pre-existing backend regression sweep surfaced 7 failures confirmed unrelated to this ticket (`test_enum.js`/`test_hide.js`/`test_hide2.js` are Windows-native `koffi`/`user32.dll` window-automation scripts; `test_dashboardNationalRankRange.js`, `test_bcvhMonthToDateContract.js`, `test_importProcessor.js` fail on pre-existing data-snapshot/source-contract assertions in F1.3 code this ticket never touched; `test_e2e_kpi.js` passes in isolation and only fails under batched cross-file execution, a pre-existing suite characteristic) — none reference any `network_*` table or this ticket's files, none fixed under this ticket per the manifest's do-not-touch-unrelated-modules constraint. Scope discipline confirmed: no Excel read or column-mapping guessed; no business data seeded (151 mã điểm / 28 hành trình / 143,467 điểm baselines remain unimplemented Phase 3 targets); no Leaflet map built; no OSRM call added; Phase 2/3/4 untouched; 02 pre-existing git stashes, the 3 root-level reference HTML files, and the newly-observed `Data QLML/` Excel files were left untouched and unopened. Full evidence: `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-001_CHECKPOINT_001.md` Section 12; `docs/10_TICKETS/NETWORK-MANAGEMENT-001_MANIFEST.md` Section 16. This closes Phase 1 only; Phase 2 (Ba bản đồ), Phase 3 (Import), and Phase 4 (Nghiệm thu) remain `PLANNED / NOT ACTIVE` and each requires its own explicit Product Owner authorization — none is self-activated by this closure.

## NETWORK-MANAGEMENT-001 Phase 2 (Ba bản đồ) Implementation

- Date: `2026-08-05`. Under explicit Product Owner authorization following PO Gate 1 PASS, Claude Code implemented `NETWORK-MANAGEMENT-001` Phase 2 (Ba bản đồ). Status: `COMPLETED / TECHNICAL PASS — READY FOR PO GATE 2`. Pre-work: re-synced from the Product Owner-supplied remote HEAD `216f16277239781bbebfdc34982fdc772f28893c`, confirmed Phase 1 unaffected, and found/restored an out-of-band workspace anomaly (the 3 root-level reference HTML files were tracked-but-deleted from the working tree, unrelated to any Phase 1/2 action; restored via `git checkout --` and confirmed byte-identical to `HEAD`). A precise inventory of all 3 `Data QLML/` Excel files (sheets, columns, roles) was performed before any code, per requirement, including the discovery that the newly-supplied `2026.08. Mang DTC2.xlsx` contains both a "TỔ CHỨC LẠI" (reorganization, excluded) block and a "HIỆN TRẠNG" (current-state) block. Real data was seeded (one-time script, not the Phase 3 Import feature) from the actual sources: Mạng điểm phục vụ 151 điểm (0 warnings, exact match to the locked baseline, from `Data QLML/Mang_diem_phuc_vu_kem_du_lieu_ban_do.xlsx`); Mạng đường thư cấp 2 28 hành trình / 148 lượt dừng / 47 mã điểm / 1,435 km (0 warnings, exact match — geometry read from the reference HTML's `MAIL_ROUTES` array since the Excel has no coordinates, per Product Owner instruction, current network only); Sơ đồ tuyến phát 143,475 điểm kept of 160,554 rows from `Data QLML/2026.07.01 - BatchFile Phat thang 06.2026.xlsb` (a small, reported-not-forced discrepancy against the locked 143,467 baseline and the HTML's own stated 143,463, documented for Product Owner awareness, not resolved). `backend/migrate_network_management_001_phase2_schema.js` added one additive column (`network_delivery_point.route_po_code`); both Phase 1 and Phase 2 migrations are now applied automatically on every backend startup (`server.js`), not manually on one machine, verified by an actual server restart. `leaflet`+`react-leaflet` added as new frontend dependencies. Three real map screens now render the seeded data via the authenticated `/api/network-map` API, each split into a data-fetching page component and a pure presentational Leaflet map component so Phase 3 Import can swap the data source without rewriting any map (`frontend/src/features/networkMap/`: `ServicePointsPage`/`ServicePointsMap`, `Level2RoutesPage`/`Level2RoutesMap`, `DeliveryRoutesPage`/`DeliveryRoutesMap`, plus shared `MapStateBanner` for loading/empty/error/warning states and `ImportPendingButton` for the unchanged admin-only, still-disabled Import gate). `NetworkMapController.getDeliveryRoutesMeta` was extended (additive) to cascade Ngày→BCVH→Bưu tá; the points endpoint still requires all three and was verified in a real browser to never be called prematurely. Validation: 31/31 backend tests pass, 13/13 frontend tests pass, `oxlint` clean, `vite build` succeeds; real-browser runtime testing as `admin` confirmed all 3 maps render correctly (151 points with 5-category legend; 28 routes with correct names/km/frequency; tuyến phát cascading dropdowns resolving to real BCVH/Bưu tá codes and rendering "30 bưu gửi" for one real selection, points endpoint called exactly once and only after all three filters were set) and that unauthenticated access redirects to `/login`; viewer role was verified against the exact production `requireAuth`/`requireRole` middleware in automated tests since the real viewer password is a one-way hash and not recoverable for a live browser session (documented residual, not a gap in the authorization logic itself). `fact_f13` confirmed unchanged at `666,153` rows (the correct current baseline, having legitimately grown from the Phase-1-era `663,126` via the unrelated, intervening `AUTO-IMPORT-011` ticket) before and after the seed. All 6 source Excel/HTML files confirmed byte-identical via SHA-256 before and after; `Data QLML/` was never added to git, renamed, or moved. No F1.3 Import/Dashboard/Ranking code was touched; no Phase 3 Import feature was built; no bulk OSRM calls were made. 02 pre-existing git stashes remain untouched. Full evidence: `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-001_CHECKPOINT_001.md` Section 13; `docs/10_TICKETS/NETWORK-MANAGEMENT-001_MANIFEST.md` Section 17. This closes Phase 2 only; Phase 3 (Import) and Phase 4 (Nghiệm thu) remain `PLANNED / NOT ACTIVE` and each requires its own explicit Product Owner authorization — none is self-activated by this closure.

## NETWORK-MANAGEMENT-001 Phase 3 (Import) Implementation

- Date: `2026-08-06`. Following a multi-round design review (Recommended Design → Corrected Recommended Design resolving dedupe-rule/DB-index/edited-file-handling contradictions → PO approval), Product Owner authorized `NETWORK-MANAGEMENT-001` Phase 3 (Import) implementation across 3 steps, each validated to Technical PASS before the next began. Status: `COMPLETED / TECHNICAL PASS — READY FOR PO GATE 3`. Step 1 (additive migration, `backend/migrate_network_management_001_phase3_schema.js`): `network_import_session` (server-side preview cache), `network_import_snapshot` (before-image + INSERT/UPDATE/DELETE operation type per affected row), `network_import_log.rollback_of_import_log_id`, and a real `UNIQUE(ma_buu_gui, ngay_phat, route_po_code)` index (pre-flight-checked for zero existing violations before creation) — wired into `server.js` startup alongside Phase 1/2. Step 2 (backend Export/Preview/Confirm/History/Rollback, `backend/src/services/networkMapImport/` + `NetworkImportController.js`): every PO-locked design decision honored unchanged — Mạng điểm phục vụ upserts by `ma_diem` (`INSERT ... ON CONFLICT DO UPDATE`), never deletes absent points, `trang_thai` copied verbatim (never defaulted/transformed); Đường thư cấp 2 uses `network_level2_route.id` directly as the stable "Route ID" for Export/reimport (no separate `route_key` column — confirmed technically sufficient since SQLite never reuses a freed AUTOINCREMENT id), validates each stop's `Mã điểm` for existence only against `network_service_point` (never filtering on `trang_thai`, so "Tạm dừng" points remain valid geometry sources per explicit PO instruction), and replaces stops only for admin-selected routes (delete-and-reinsert scoped to that route only); Sơ đồ tuyến phát keeps the locked `(ma_buu_gui, ngay_phat, route_po_code)` key unchanged (explicitly not switched to `LADING_CODE`-only, per PO instruction that the prior discrepancy investigation was informational only), writes via `INSERT ... ON CONFLICT DO UPDATE` (never `INSERT OR IGNORE`, so an edited-and-reimported row's changes are preserved), and allows a same-month, different-fingerprint file to reimport (only exact-fingerprint duplicates are rejected). Hard-error scope matches the locked design: any error row blocks Confirm for the whole file for Mạng điểm phục vụ and Sơ đồ tuyến phát; for Đường thư cấp 2 only the specific invalid route is blocked, other valid routes in the same file remain individually selectable. Rollback restores by operation type (`INSERT`→delete, `UPDATE`→restore before-image, `DELETE`→reinsert before-image) and refuses to run when a later, still-active import touched an overlapping row-key scope, enforced via monotonic `id` ordering. Step 3 (frontend, `frontend/src/features/networkMap/import/`): admin-only (`isAdminRole()`-gated, and independently re-checked server-side via `requireRole(['admin'])` on every new route) Import/Export/History/Rollback UI wired into all 3 map pages; `ServicePointsMap.jsx`/`mapStyles.js` extended per explicit PO instruction so "Tạm dừng" points are hidden by default (data always fetched/kept — display-only toggle), with a dedicated legend checkbox and a marker (`createTamDungMarkerSvg`) visually distinct from all 5 active `loai_diem` colors — additive Phase 3 frontend work, not a reopening of Phase 2's closed PO Gate 2 deliverable. Using the finished feature, the 5 previously-audited "Tạm dừng" points (`536101`, `536102`, `537200`, `534630`, `534989`) were actually imported into the live database through a real Preview→Confirm cycle: `network_service_point` grew from 151 to 156 rows, and 11 previously-orphaned ĐTC2 stop references now resolve real geometry via the `network_service_point` join. Two real defects were found and fixed by the test suite before shipping: a migration whose multi-statement `CREATE TABLE` block silently created nothing because `db.run()` only executes a SQL string's first statement (fixed via `db.exec()`); a rollback-eligibility check comparing `created_at` timestamps, which have only second-level resolution in SQLite and could not reliably distinguish two imports in the same second (fixed by comparing monotonic `id` instead) — plus a third bug where rolling back an already-rolled-back import silently "succeeded" again (fixed via an explicit `ALREADY_ROLLED_BACK` check). Validation: 77 backend + 25 frontend automated tests pass, `oxlint` clean, `vite build` succeeds; full real-browser runtime validation performed as `admin` (156 points rendered with the Tạm dừng toggle correctly default-off, admin panels functional on all 3 pages, tuyến-phát Export row-count preview returning the exact live count of 143,475, unauthenticated access correctly blocked with `401`/redirect-to-login on every new endpoint); `fact_f13` confirmed unchanged (669,847 rows) across three checkpoints (before Step 1's migration, after Step 1, after the live 5-point Import); all 3 `Data QLML/` source Excel files confirmed byte-identical via SHA-256 throughout; `Data QLML/` never added to git, renamed, or moved; 02 pre-existing git stashes untouched. Full evidence: `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-001_CHECKPOINT_001.md` Section 14; `docs/10_TICKETS/NETWORK-MANAGEMENT-001_MANIFEST.md` Section 27. This closes Phase 3 only; Phase 4 (Nghiệm thu) remains `PLANNED / NOT ACTIVE` and requires its own explicit Product Owner authorization — not self-activated by this closure.

## NETWORK-MANAGEMENT-001 Phase 3 — PO Gate 3 Runtime Remediation

- Date: `2026-08-06`. Product Owner's first PO Gate 3 runtime check (baseline `2efa6fa227d1cda4c514f8afb4f8f91144acf59d`) returned `RUNTIME FAIL` on 3 defects: (1) Mạng đường thư cấp 2 (ĐTC2) rendering straight-line polylines instead of road geometry; (2) Sơ đồ tuyến phát routing not resiliently building road routes through actual delivery coordinates; (3) the Date Picker allowing a July 2026 selection when only June 2026 had been imported. Claude Code first ran an audit-only root-cause investigation (no code/DB changes) and reported: none of the 3 defects originated in the Phase 3 (Import) diff itself. (1) `Level2RoutesMap.jsx` had never called any routing service since its original Phase 2 implementation — the reference source HTML's own `mailLoadRoute` OSRM logic for ĐTC2 was never ported. (2) `deliveryRoutingService.js`'s `fetchChunkRoadRoute` had no request timeout, so an unresponsive OSRM provider could hang indefinitely with the UI silently stuck on an unstyled placeholder; live-DB query also found 10 out-of-Huế-bounds coordinate rows (8 in Morocco, 2 near Hải Phòng) inherited unchanged from the Phase 2 `.xlsb` seed, capable of breaking any route that included them. (3) `getDeliveryRoutesMeta`/`listDeliveryPoints` used `COALESCE(ngay_nhap_phat, ngay_phat)` as the filter/calendar date; 8 real rows have `ngay_phat` in June but `ngay_nhap_phat` (actual delivery-scan time) genuinely drifted into July — real data, never a hardcoded month, and never locked by any prior PO decision.
- Product Owner authorized remediation of all 3, same day, still inside Phase 3 scope (no Import/Export/History/Rollback logic touched, no Phase 4 work started). PO-locked decisions honored: ĐTC2 shares a routing helper with Sơ đồ tuyến phát; Route ID/stop order/`network_service_point` coordinate source unchanged; each OSRM provider call bounded by a 15s `AbortController` timeout with 2-provider fallback; if both fail, an explicit non-silent failure state is shown (never a straight line disguised as road geometry); out-of-Huế-bounds coordinates are excluded from routing calculations only (never edited/deleted in `Data QLML/` or the DB), one bad coordinate never breaks the rest of a valid route, and the UI names each excluded point explicitly (`ma_buu_gui` + coordinates); `ngay_phat` is now the sole business date for calendar availability/filters, with `ngay_nhap_phat`/`thoi_gian_nhap_phat` restricted to intra-day ordering only.
- Implementation: `frontend/src/features/networkMap/deliveryRoutingService.js` renamed (git-tracked) to `roadRoutingService.js` and generalized — adds `HUE_ROUTING_BOUNDS`/`isWithinHueRoutingBounds` (sized from live coordinate ranges to exclude both confirmed artifact clusters while containing every legitimate Huế point), a 15s per-provider timeout in `fetchChunkRoadRoute`, and the new `fetchRoadRoute` entry point (`fetchDeliveryRoadRoute` kept as a backward-compatible alias) which splits routable vs. excluded locations before chunking. `Level2RoutesMap.jsx` now fetches per-route road geometry for all 28 ĐTC2 routes independently (one route's failure never blocks another's), rendering one Polyline per segment in 3 visually distinct styles (real road / confirmed fallback / loading placeholder) plus a map-overlay banner and per-route ⚠️ markers for any route that failed to route. `DeliveryRoutesMap.jsx` switched to the shared helper, combines the routing-failure warning with an explicit excluded-points warning, and restyled its pre-fetch loading placeholder to neutral gray (previously visually identical to the confirmed-fallback state). `backend/src/controllers/NetworkMapController.js`'s `getDeliveryRoutesMeta`/`listDeliveryPoints` now filter/aggregate strictly by `ngay_phat` (ordering by `thoi_gian_nhap_phat`/`status_time` unchanged, intra-day only).
- Validation: 31/31 backend tests pass (`NetworkMapController.test.js` incl. 2 new PO Gate 3 regression tests for the June-`ngay_phat`/July-`ngay_nhap_phat` case) and 39/39 frontend tests pass (`networkMapRemediation.test.js` incl. new tests for provider-timeout-then-fallback, both-providers-fail explicit state, bounds-exclusion not breaking the rest of a route, chunk-boundary continuity, and ĐTC2 real-road-geometry) — zero regressions in the pre-existing suites. `oxlint` clean, `vite build` succeeds. Real-browser runtime re-verified as `admin` on all 3 map screens after a real backend restart: all 28 ĐTC2 routes confirmed rendering solid (non-dashed) real road geometry via direct DOM inspection; a real 413-record/18-location tuyến-phát route (22/06/2026, BCVH 533140, Bưu tá 53A141) built a solid real road route with no warning; the route containing the confirmed Morocco outlier (03/06/2026, BCVH 536250, Bưu tá 53T022) still built a real road route for its 4 valid locations and displayed the exact expected exclusion warning naming the excluded parcel and coordinates; the Date Picker showed all 31 days of July 2026 disabled and all 30 days of June 2026 enabled; Mạng điểm phục vụ confirmed unaffected (156 points, Tạm dừng toggle unchanged). `fact_f13` (`669,847`) and `network_delivery_point` (`143,475`) row counts unchanged (query-logic-only backend change, zero data mutation); all `Data QLML/` source files confirmed byte-identical (2 cross-checked directly against their recorded `network_import_log.file_fingerprint` values); `Data QLML/` never added to git; both pre-existing stashes untouched. Full evidence: `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-001_CHECKPOINT_001.md` Section 15; `docs/10_TICKETS/NETWORK-MANAGEMENT-001_MANIFEST.md` Section 28.
- This remediation does **not** constitute PO Gate 3 PASS — Phase 3 returns to `TECHNICAL PASS`, awaiting a Product Owner runtime recheck. Phase 4 (Nghiệm thu) remains `PLANNED / NOT ACTIVE` and is not self-activated by this remediation.

## NETWORK-MANAGEMENT-001 — ĐTC2 Journey Visual Remediation

- Date: `2026-08-06`. Product Owner reported that ĐTC2 journeys revisiting the same điểm on outbound/return (e.g. Tuyến 6: BCVH Phú Lộc lượt 1/6, Thừa Lưu lượt 3/5, Lăng Cô as quay đầu lượt 4) rendered as overlapping markers, making the map look like nodes were missing, and authorized an additive visual remediation scoped to the selected-route detail view of Mạng đường thư cấp 2 only. New pure module `frontend/src/features/networkMap/routeJourneyGeometry.js` classifies stops outbound/turnaround/return via farthest-distance-from-start (verified against the exact live Tuyến 6 coordinates and spot-checked on Tuyến 3/8/9); same-coordinate revisits are spiderfied into a zoom-invariant pixel-space fan (Leaflet `latLngToLayerPoint`/`layerPointToLatLng`) without ever altering real `lat`/`lon` data; outbound/return legs are fetched as two independent calls to the existing shared `roadRoutingService.js` (all PO Gate 3 timeout/fallback/bounds-exclusion guarantees carry over unchanged) and pixel-offset apart when shown together so a coinciding out-and-back road stays visible as two lines; direction arrows sampled along each leg. `mapStyles.js` gained direction/turnaround color+icon helpers; `Level2RoutesMap.jsx` gained a `SelectedRouteJourneyLayer` (Toàn hành trình/Chiều đi/Chiều về mode toggle, consolidated per-location popup listing every visit, two-way hover/click sync between markers/lines/sidebar list) — the unselected 28-route overview is otherwise untouched. Validation: 12 new unit tests (all pass) plus 51/51 across the full targeted frontend suite, `oxlint` clean, `vite build` succeeds; real-browser runtime confirmed Tuyến 6 reproduces the PO's exact example (Quay đầu = Lăng Cô; 6/6 markers rendered and visibly separated; mode toggle correctly filters to 4/3 markers; consolidated popup and hover sync both verified), plus Tuyến 3 (7 stops), Tuyến 8 (9 stops), Tuyến 9 (9 stops, symmetric) all render every stop with no missing nodes and no routing warnings; Import/Export/History panel confirmed unaffected. `fact_f13` (669,847), `network_level2_route` (28), `network_level2_route_stop` (148), `network_delivery_point` (143,475) unchanged — no backend/DB code touched. Both stashes untouched. Does not alter Phase 3's `REMEDIATED / TECHNICAL PASS` status or PO Gate 3 (still awaiting Product Owner runtime recheck) and does not start Phase 4. Full evidence: `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-001_CHECKPOINT_001.md` Section 16.

## NETWORK-MANAGEMENT-001 — ĐTC2 Selected-Route Arrow Visibility Remediation

- Date: `2026-08-06`. Product Owner runtime feedback: direction arrows added in the journey visual remediation were too small to read without zooming to maximum. Follow-up fix, same selected-route-only scope: `createDirectionArrowSvg` default size raised 16px→26px with a thicker outline + drop-shadow (fixed screen-pixel size, was already zoom-invariant, so this only fixes "too small" not any zoom-scaling bug); new pure `computePolylineLengthKm`/`computeArrowCount` scale arrow density to real leg length (one per ~15km, clamped 2-8) replacing a flat 3-per-leg constant; arrows offset 13px off the line centerline via a consistent "90° from own bearing" rule that lands outbound/return arrows on opposite physical sides for a there-and-back leg and never overlaps a stop marker. 2 new unit tests (14/14 in the geometry test file, 53/53 full targeted frontend suite), `oxlint` clean, `vite build` succeeds. Real-browser runtime confirmed at the map's unchanged default zoom (10): Tuyến 6 → 9 arrows at 26×26px none overlapping its 6 markers; Tuyến 8 (171km) → 16 arrows (max-clamped); Tuyến 2 (8km) → 4 arrows (min-clamped); unselected overview → 0 arrows. No backend/DB touched; both stashes untouched. Does not alter Phase 3's `REMEDIATED / TECHNICAL PASS` state or PO Gate 3 status and does not start Phase 4. Full evidence: checkpoint Section 17.

## NETWORK-MANAGEMENT-001 — PO Gate 3 PASS — Phase 3 Closed

- Date: `2026-08-06`, baseline `7da98a79eb8` (branch `codex/da-impl-006`). Product Owner explicitly granted `PO GATE 3 PASS`, confirming with no regression: ĐTC2 road routing and full outbound/return journey display; same-coordinate marker overlap resolution, quay đầu (turnaround) identification, and direction arrows; Sơ đồ tuyến phát routing resilience (timeout/2-provider fallback/non-silent failure) and out-of-Huế-bounds coordinate exclusion/warning; Date Picker/filter semantics keyed to `ngay_phat`; and Import, Export, History, and Rollback across all 3 modules. Verified (`git diff --name-only 2efa6fa2..7da98a79 | grep -i import` → no matches) that no commit across the entire PO Gate 3 remediation chain touched `NetworkImportController.js`, `backend/src/services/networkMapImport/`, or the frontend `import/` directory. This closes Phase 3 (Import) as `COMPLETED / PO PASS / CLOSED`. Per this same Product Owner instruction, the program moves to `Phase 4 (Nghiệm thu): READY FOR PHASE 4 / AWAITING PO AUTHORIZATION` only — Phase 4 implementation is **not** started and is **not** self-activated by this closure; it requires its own explicit Product Owner authorization. A future "Bản đồ tổng thể mạng lưới" (network-wide overview map) module was named in the same instruction and is recorded as a noted future scope item only — not added to Phase 4's locked scope, not authorized, not started. Documentation-only update: no product code, schema, database, or test file changed. Full evidence: `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-001_CHECKPOINT_001.md` Section 18; `docs/10_TICKETS/NETWORK-MANAGEMENT-001_MANIFEST.md` Section 29.

## NETWORK-MANAGEMENT-001 Phase 4 — Sơ đồ tuyến phát Data Contract Audit + Remediation

- Date: `2026-08-06`. Product Owner authorized starting Phase 4 (Nghiệm thu) with a first scoped item: an audit (discovery-only) of the Sơ đồ tuyến phát data pipeline, followed by a same-day PO-approved remediation. Audit findings: the real raw source (`Data QLML/2026.07.01 - BatchFile Phat thang 06.2026.xlsb`) has a true 29-column header (read directly to confirm), but the Phase 3 Import feature instead required an unrelated hand-built 12-column flat template ("Tuyến phát Import") sharing no header names/order/count with the real recurring monthly file — an admin could not upload the real file as-is; "Biển số" (`bien_so`) traced to have no source column anywhere (not the raw file, not the reference HTML) and was always `NULL` in all 143,475 live rows, added into Phase 1's original schema with no documented origin; no filename-vs-content period validation existed anywhere; the existing classify/apply/upsert logic (locked row key, `ON CONFLICT DO UPDATE`) was confirmed already multi-month-safe by design; no file-archive mechanism existed (`multer.memoryStorage()` discarded uploads after each request).
- Remediation, PO-locked decisions honored: Import now reads the original unmodified 29-column raw BatchFile directly, zero manual reformatting; new parser (`parseDeliveryRoutesBatchFileExcel.js`) resolves columns by header name (resilient to reordering, fails loudly on a missing required header), persists only the 11 fields the map needs, accepts-but-ignores the other 18 raw columns; "Biển số" removed from Import/Export and documentation (schema column kept, nullable, untouched — no breaking migration); filename period (`YYYY.MM.DD - BatchFile Phat thang MM.YYYY.xlsb`, prefix = export date ignored, `thang MM.YYYY` = declared period) cross-checked against every parsed row's `ngay_phat` with an explicit warning on mismatch/multi-month, **never a hard block**; classify/apply/upsert layer (`deliveryRoutesImport.js`) left **unchanged**, keeping sequential-month imports additive/safe; after a successful Confirm, the original file is archived to disk (new `network_import_archive` table + `fileArchive.js`) with checksum, byte size, declared/actual period, uploader, and import time — **no retention/auto-delete added**; the raw buffer is staged to disk immediately at Preview time (independent of multer's request-scoped memory) and only promoted to the permanent archive after Confirm's transaction has already committed.
- Implementation: new `parseDeliveryRoutesBatchFileExcel.js`, `fileArchive.js`; new migration `migrate_network_management_001_phase4_schema.js` (`network_import_archive`, wired into `server.js`/`schema.sql`); `NetworkImportController.js` delivery-route Preview/Confirm rewired; `exportBuilders.js` now defines its own 11-column header (no "Biển số"); retired `parseDeliveryRoutesImportExcel.js` (+ test) removed; `FlatImportPanel.jsx` gained period-warning display and `.xlsb` file-picker support.
- Validation: `parseDeliveryRoutesBatchFileExcel.test.js` (13 tests, incl. one parsing the **real, unmodified** `Data QLML` BatchFile end-to-end and reproducing the exact known-good baseline of 143,475 non-duplicate rows), `fileArchive.test.js` (5), `migrate_network_management_001_phase4_schema.test.js` (3); `NetworkImportController.test.js` +7 Phase 4 tests; `exportBuilders.test.js` asserts "Biển số" absent. 104/104 full targeted backend suite, 53/53 frontend, `oxlint` clean, `vite build` succeeds. Real-browser + real-API runtime validated as `admin` after a backend restart: the real production BatchFile Previewed and was correctly rejected `409 DUPLICATE_FILE` against its historical Phase 2 seed fingerprint; a structurally-identical fixture Preview→Confirm→Archived via direct API with `bien_so` staying `null` and archive checksum matching; a further fixture imported through the **real UI** (file genuinely dispatched into `FlatImportPanel`'s input) showed the correct period-match text and Confirmed, then Rolled Back via the real UI button; two sequential-month imports confirmed June's 143,475 rows were never altered; Export downloaded and confirmed to no longer contain "Biển số"; Import History panel confirmed correct throughout. All test-injected rows/archived files rolled back/removed after validation — `network_delivery_point` (143,475) and `fact_f13` (669,847) confirmed back at the exact pre-test baseline; `Data QLML/` and both stashes untouched.
- Does not constitute Phase 4 or program-wide PO PASS — awaiting Product Owner runtime recheck; further Phase 4 scope items (per manifest Section 6's enriched checklist) remain outstanding and unauthorized. Full evidence: `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-001_CHECKPOINT_001.md` Section 19; `docs/10_TICKETS/NETWORK-MANAGEMENT-001_MANIFEST.md` Section 30.

## NETWORK-MANAGEMENT-001 Phase 4 — PO Gate 4 Recheck FAIL + Fix (Sheet Auto-Detection)

- Date: `2026-08-06`/`2026-08-07`. Product Owner's first Phase 4 runtime recheck (against commit `0f968b02`) FAILED: the real May 2026 BatchFile (`2026.06.01 - BatchFile Phat thang 05.2026.xlsb`, sheet `Data_Tong_Hop`, 29 correct headers, ~144,289 valid rows per PO's own direct inspection) Previewed as `0 added/0 changed/0 unchanged/0 duplicate/0 error` with "Không có dòng dữ liệu hợp lệ nào để đối chiếu kỳ" — a false-clean zero-row result, not an error. Root cause: `parseDeliveryRoutesBatchFileExcel.js`'s sheet-picking logic hardcoded a `Data_Ghep` name-prefix match (matching June's `Data_Ghep_<suffix>` sheet) and otherwise blindly used `workbook.SheetNames[0]`; the May file's data sheet, named `Data_Tong_Hop`, is not necessarily sheet 0, so the old code silently read the wrong/empty sheet instead of throwing. Fixed same day (commit `8cac056d`): new `findDataSheet()` scans every sheet in the workbook and selects the first whose header row contains the full required-header set, by content — never by name or position; if no sheet qualifies, throws one error naming every sheet checked and that sheet's specific missing headers, so a 0/0/0/0/0 silent result can never recur. 4 new/updated tests reproduce the exact reported multi-sheet delta and pass; the existing real-June-file test (143,475-row baseline) still passes unchanged; 107/107 across the full targeted backend suite. Fix verified live via `curl` against the real running API with a structurally-equivalent multi-sheet fixture (cover sheet + `Data_Tong_Hop` data sheet) — Preview returned `added:1`, correct `declaredPeriod:2026-05`, no warning — **not Confirmed to DB** (Preview-only, per explicit PO instruction not to write data before validation is complete). The actual May BatchFile is not present in this workspace's `Data QLML/` (only the June file is), so the real file itself could not be replayed byte-for-byte. `network_delivery_point` (143,475, June unaffected) and `fact_f13` (669,847) unchanged; `Data QLML/` and both stashes untouched. Does **not** constitute Phase 4 or program-wide PO PASS — awaiting a fresh Product Owner recheck with the real May file. Commit `8cac056d` pushed to `codex/da-impl-006`.

## NETWORK-MANAGEMENT-001 Phase 4 — PO Gate 4 Second Recheck FAIL + Fix (Text-Cell Type Coercion, Real May File)

- Date: `2026-08-07`. Product Owner supplied the real `Data QLML/2026.06.01 - BatchFile Phat thang 05.2026.xlsb` file (SHA-256 `3daa53c9…5a687`) and authorized a bounded fix after a second recheck FAIL against commit `8cac056d`. Preview against the real file reproduced the same 0/0/0/0/0 symptom, but from a **different, new root cause**: sheet auto-detection worked correctly this time (`Data_Tong_Hop` matched, all 12 required headers present), but the May file stores `QUANTITY`, `LAT`, and `LON` as text-formatted cells (e.g. `"-1"`, `"16.497005"`) instead of the June file's native numbers — confirmed via direct `typeof` inspection of all 164,411 rows. The parser's strict `row[idx.QUANTITY] === -1` and `typeof value === 'number'` checks silently excluded every row, again producing a false-clean 0-row result with no diagnostic.
- Fix, commit `d830a784`: new `toFiniteNumber()` normalizes both native-number and numeric-text cells, rejecting blank/non-numeric/NaN/Infinity; `isQuantityExcluded()`/`isValidCoordinate()` (now range-checked, lat -90..90 / lon -180..180) use it; `LAT`/`LON` are normalized to real numbers before being stored in records; when every row is excluded, `periodWarning` now names exact per-reason counts (`excluded_quantity_minus_1`/`excluded_invalid_coordinates`/`excluded_invalid_status_date`) instead of a bare "no valid rows" message, and the Preview API response gained a `sourceStats` field surfacing the same breakdown — so a silent unexplained 0/0/0/0/0 can never recur for either root cause. `deliveryRoutesImport.js` classify/apply/upsert layer left unchanged.
- Validation: 9 new regression tests (text-cell May-shape parse, mixed text/number `QUANTITY`+`LAT`+`LON` in one file, out-of-range/blank/non-numeric coordinates, `QUANTITY -1` in both text and numeric forms, diagnostic-message breakdown) — 22/22 in the parser test file, 113/113 across the full targeted backend suite (parser, controller, export, archive, migration, seed-parser tests, incl. the existing real-June-file 143,475-row baseline test unchanged); frontend `NetworkMapClient` 13/13 pass (unaffected, backend-only change); `oxlint` clean (one pre-existing, untouched warning in an unrelated test helper). Fix verified live via `curl` against the real, unmodified May file (not a fixture): Preview returned `added: 144284, changed: 0, unchanged: 0, duplicate: 5, error: 0` (144,289 total rows, matching the Product Owner's own direct inspection of ~144,289 valid rows), `declaredPeriod: "2026-05"`, `actualPeriodMonths: ["2026-05"]`, no warning, `hasBlockingError: false`; `sourceStats` breakdown: `total_rows 164411, excluded_quantity_minus_1 913, excluded_invalid_coordinates 19209, kept_points 144289`. **Preview only — not Confirmed to DB**, per explicit PO instruction. `/api/network-map/delivery-routes/meta` confirmed unchanged (June dates only) before/after; May file SHA-256 checksum unchanged; `network_delivery_point` (143,475) and `fact_f13` (669,847) unchanged; `Data QLML/` and both stashes untouched. Does **not** constitute Phase 4 or program-wide PO PASS — Confirm (writing the real May data to production) has not been run and requires its own explicit Product Owner approval. Commit `d830a784` pushed to `codex/da-impl-006`.

## NETWORK-MANAGEMENT-001 Phase 4 — Sơ đồ tuyến phát Import PO PASS

- Date: `2026-08-07`. Product Owner officially confirmed `PO PASS` for the Sơ đồ tuyến phát Import component (Phase 4's first scoped item, covering the raw-BatchFile data-contract audit/remediation and both recheck-fail fixes). Accepted evidence: the real, unmodified May 2026 BatchFile Preview succeeded (144,289 valid rows: 144,284 new, 5 duplicate, 0 error); declared and actual period both `2026-05`; Confirm Import succeeded (performed by the Product Owner outside this session); period `05/2026` is now available in Sơ đồ tuyến phát; the previous 0/0/0/0/0 defect (both root causes — sheet detection and text-cell type coercion) is resolved.
- Documentation-only governance update — no product code or data changed. Read-only verification against the live running backend confirmed `network_import_log.id = 17` (`module: delivery_route, status: SUCCESS, file_fingerprint: 3daa53c9a744c87533b550248b21124d43a728c60f8efaf5ddc93153da15a687, total_records: 144289, inserted_records: 144284, skipped_records: 5, error_records: 0, created_at: 2026-08-07 07:20:25, uploaded_by: admin`) matches the Product Owner's accepted evidence exactly, and `/api/network-map/delivery-routes/meta` lists `2026-05-01` through `2026-05-31` alongside the pre-existing June dates. The May import is preserved — not rolled back. `Data QLML/` and both stashes untouched.
- **Scope of this PASS: Sơ đồ tuyến phát Import only.** Does not constitute Phase 4 or program-wide PO PASS. Remaining Phase 4 acceptance checklist items (manifest §6) without recorded Product Owner acceptance: item 2 (admin-only enforcement re-verified independently at Phase 4), items 3-4 ("Tạm dừng" point handling and the 5 audited points), item 5 (cross-module regression across all 3 modules), item 6 (Phase-4-level data-integrity reconfirmation). Per manifest §6 item 7, Phase 4 may only close once all checklist items have Product Owner acceptance. Full evidence: `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-001_CHECKPOINT_001.md` Section 22; `docs/10_TICKETS/NETWORK-MANAGEMENT-001_MANIFEST.md` Section 32.

## AUTO-IMPORT-013 — Urgent TCT Login Stall, Discovery Only

- Date: `2026-08-07`. Product Owner priority directive: TCT changed its login mechanism; the Product Owner still enters credentials manually in the browser; HUE (new account) logs in and imports successfully as an unmodified control baseline; TCT's manual login succeeds in the browser but QIS stays stuck at `WAITING_FOR_LOGIN`/"Đang mở trình duyệt", and the UI's "cửa sổ đăng nhập TCT không xuất hiện hoặc đã bị đóng" message appears — the process never advances past "Chờ đăng nhập thủ công" to open F1.3. `NETWORK-MANAGEMENT-001` was explicitly paused at its current state (not touched, not closed, not rolled back — its Sơ đồ tuyến phát Import `PO PASS` stands). `AUTO-IMPORT-011` was explicitly not reopened, as this is a materially different, freshly reproduced symptom.
- Discovery only, per explicit instruction: a live, already-in-progress TCT stall was found and inspected directly (not separately triggered). Evidence: backend PID `29508` running continuously since `08:45:43` (no restart); `Data DKCL/BrowserProfiles/TCT.lock` held since `15:46` (`HUE.lock` absent, consistent with a normal completed/released HUE session); a live TCT Chromium process tree (main PID `23140`, child of the backend, launched `15:46:02`, two renderer processes) still alive ~28 minutes past the 4-minute default interactive-auth wait window; the TCT browser window open and visible, title `"Quản trị nội dung - Google Chrome for Testing"` (read via OS-level `EnumWindows`/`GetWindowText` only — no page content, no credentials, at any point).
- Root-cause hypothesis (evidence-backed, not yet fully confirmed): TCT's manual login genuinely completes, but the shared, unmodified `isAuthenticated()` in `backend/src/services/dkclHueF13PortalClient.js` only recognizes success via a hardcoded set of Vietnamese phrases (`Quan ly tep`, `Quản lý tệp`, `Tra cứu thông tin bưu gửi`, `Dang xuat`/`Đăng xuất`, `Logout`, `tantn.bdtth`, a mojibake `Thá»‘ng kÃª`), none of which match the observed post-login page (title `Quản trị nội dung`). No per-source override exists — the same detection code runs for both HUE and TCT. `waitForManualAuthentication()` therefore polls until its wait window elapses without ever detecting success, and the frontend's `tctLoginStuck` flag (`DataImportCenter.jsx`) — derived purely from backend status, not an independent window check — then shows a misleading "window did not appear or was closed" message even though the window is demonstrably open. Page-content-level (body text/URL) confirmation of the mismatch remains outstanding, as does whether the two observed renderer processes indicate an untracked second tab/popup; both would require a fresh, instrumented reproduction. No commit since `1ca7eee1` (`AUTO-IMPORT-010`, 2026-07-31) touches this code, ruling out a recent QIS-side regression.
- No code changed. No credentials requested, read, logged, or stored. HUE not modified. `Data QLML/` and both pre-existing git stashes untouched. Proposed minimum fix scope (not yet authorized): broaden/replace TCT's post-login detection based on a fresh instrumented reproduction; preserve manual credential entry exactly as-is; add an explicit bounded timeout → `FAILED` state transition with a specific diagnostic message instead of an indefinite/misleading stuck state; no change to HUE's detection path unless unavoidable, in which case HUE must be regression-tested. Ticket remains `DISCOVERY COMPLETE`, awaiting Product Owner/CTO decision before any implementation. Full evidence: `docs/06_REVIEWS/Import/AUTO-IMPORT-013_CHECKPOINT_001.md`; scope and proposed fix: `docs/10_TICKETS/AUTO-IMPORT-013_MANIFEST.md`.

## AUTO-IMPORT-013 — Phase 2: PO-Authorized Bounded Implementation

- Date: `2026-08-07`. Product Owner reviewed the Phase 1 discovery at governance commit `d380ddd9` and authorized bounded implementation across 8 numbered scope items: safe instrumentation; no automated credential entry; detector fix based on fresh evidence; a bounded timeout; HUE preserved as control baseline; mandatory tests; no interference with the then-live TCT session until safe evidence was exhausted; production DB/May import/`NETWORK-MANAGEMENT-001`/`Data QLML/`/both stashes preserved.
- Implemented: (1) `captureLoginDiagnostics()` in `dkclHueF13PortalClient.js` — logs URL, page title, page/tab count, body-text length, and boolean marker matches only (never raw body text, form values, cookies, or credentials), wired into `waitForManualAuthentication()` (start/success/timeout) and `openF13Report()`. (4) A new `LOGIN_TIMEOUT` terminal state (`dkclLifecycleContract.js`, `dkclSessionPreflightService.js`): code-read confirmed `waitInteractiveAuthentication()` throws `AUTHENTICATION_REQUIRED` from exactly one place — the manual-login wait window elapsing — and the interactive flow never automates credential entry, so this is unambiguous; on this timeout the session now releases the browser context and profile lock and reports a specific message, instead of the prior behavior of parking `WAITING_FOR_LOGIN` indefinitely with the lock held (exactly matching the reported symptom's mechanism). `SOURCE_PAGE_REQUIRED` (authenticated but F1.3 not ready) is unchanged. A related, independently discovered bug was also fixed in `DataImportCenter.jsx`: `tctLoginStuck`/`hueLoginStuck` previously fired on the very first `LOGIN_IN_PROGRESS` poll of a perfectly normal login — before this fix, the UI could show "cửa sổ đăng nhập không xuất hiện hoặc đã bị đóng" within seconds of a login that was working correctly. Both flags now key off the new `LOGIN_TIMEOUT` status and `SESSION_CHECK_FAILED` only.
- Deferred (item 3, by explicit PO sequencing — "trước khi sửa detector, báo ngắn evidence instrumentation thực tế"): `isAuthenticated()`'s hardcoded marker regex is unchanged. The Phase 1 live incident was exhausted as an evidence source before this round (one more safe attempt — `PrintWindow`/full-screen capture — found the window not present on the physically capturable display; no interaction with the window was attempted) and could not be converted into a fresh instrumented capture. Next action: Product Owner performs one real TCT manual login now that the instrumented code is deployed; the resulting evidence in `backend.log` (`[AUTO-IMPORT-013]` prefix) will be reported before any marker change.
- Validation: `test_dkclSessionPreflightService.js` (all 26 named blocks, incl. `SOURCE_PAGE_REQUIRED` cases unaffected), `test_dkclHueF13SyncService.js` (129/129, exercises the real portal client with the new diagnostics wired in), `test_dkclHueF13BackfillService.js` (39/39), `test_tctF13BackfillService.js` (all pass), `test_dkclHueBrowserBroker.js` (pass) — all unchanged from baseline. Frontend `dataImportTctScan.test.js`, `dataImportWave3Ui.test.js`, `dataImportHueSelection.test.js`, `importDashboardReconciliation.test.js` (4/4), `NetworkMapClient.test.js` (13/13) all pass; `dataImportBackfillQueue.test.js` fails on an unrelated, confirmed pre-existing `api/client.js` assertion (reproduced identically on `HEAD` via `git stash`/rerun/`git stash pop`), not touched by this ticket. `oxlint` on all 4 changed files shows no new warnings versus the pre-change baseline.
- Live-system cleanup (item 7, explicitly authorized): the TCT session stuck since Phase 1 was still live. Backend PID `29508` was stopped (its own launcher, `TTVH_ControlCenter.ps1`, does not monitor/auto-restart it); the child TCT Chromium process exited on its own once its `--remote-debugging-pipe` connection closed — no force-kill of a still-running process was needed; `TCT.lock` was removed only after the owning process was confirmed gone. Backend restarted with the same invocation the Product Owner's control-center script uses (new PID `11000`), now running the instrumented code. `HUE.lock` was already absent (no live HUE session) and remains untouched. Production DB, the May-2026 delivery-routes import, `NETWORK-MANAGEMENT-001` (still paused unchanged), `Data QLML/`, and both git stashes confirmed untouched throughout; no credentials requested, read, logged, or stored at any point.
- Ticket remains open, `NOT PASS`. Awaiting the Product Owner's fresh TCT login for detector evidence, then a full runtime recheck (TCT login → F1.3 → import; HUE regression) before any `PO PASS`. Full evidence: `docs/06_REVIEWS/Import/AUTO-IMPORT-013_CHECKPOINT_002.md`; updated scope/status: `docs/10_TICKETS/AUTO-IMPORT-013_MANIFEST.md`.

## AUTO-IMPORT-013 — COMPLETED / PO RUNTIME PASS / CLOSED

- Date: `2026-08-07`. Product Owner performed a real manual TCT login and confirmed: TCT manual login succeeded; QIS recognized the session and proceeded correctly through the flow; TCT Import succeeded; no longer stuck at "Đang mở trình duyệt"; the manual username/password entry mechanism is accepted as permanent (no automated login requested or built); HUE continues to work normally (control baseline unaffected).
- Real diagnostic evidence captured by the Phase 2 instrumentation (`backend/backend.log`, `[AUTO-IMPORT-013]` prefix) during that actual login shows `isAuthenticated()`'s existing marker regex correctly matched TCT's post-login page (`has_quan_ly_tep: true`, `has_login_input: false`) and `openF13Report()` reached the real F1.3 report page cleanly (correct title, non-trivial body length, no login input). This confirms the Phase 1 window-title observation ("Quản trị nội dung") was not representative of the actual detection signal, and that **the detector never needed broadening** — the reported symptom's actual cause was the frontend false-positive warning and the missing bounded timeout, both already fixed in commit `f7a74d4f`. No further code change was made in this closure round.
- Governance-only closure: no code changed. `NETWORK-MANAGEMENT-001` / Module QLML not touched, not resumed, not restored — remains `PAUSED` exactly as previously recorded. Production DB, `Data QLML/`, and both git stashes untouched throughout. No credentials requested, read, logged, or stored at any point across the entire ticket. Full evidence: `docs/06_REVIEWS/Import/AUTO-IMPORT-013_CHECKPOINT_003.md`; final ticket status: `docs/10_TICKETS/AUTO-IMPORT-013_MANIFEST.md`.

## NETWORK-MANAGEMENT-001 — Resumed; Phase 4 Acceptance Checklist Technical Verification Round

- Date: `2026-08-08`. `NETWORK-MANAGEMENT-001` resumed from its pause (caused solely by the unrelated, now-closed `AUTO-IMPORT-013` ticket) on explicit instruction, at its exact prior state: Phase 4, Sơ đồ tuyến phát Import component `CLOSED / PO PASS` (2026-08-07), manifest §6 checklist items 2, 3-4, 5, 6 outstanding.
- Claude Code performed a read-only technical verification of items 2, 3-4, 5, 6, with no browser automation and no code change (no defect was found). **Item 2 (admin-only, all 3 modules) — Technical PASS**: static route wiring (`allowAdminOnly` on every Import/Export/History/Rollback endpoint, all 3 modules) + passing unit tests (`requireAuth`/`requireRole` return 401/403 correctly) + live unauthenticated `curl` sweep (401 on every one of those endpoints across all 3 modules) + frontend `NetworkAdminSection.jsx` returning `null` (absent, not disabled) for non-admin roles on all 3 map pages. **Items 3-4 ("Tạm dừng" handling) — Technical PASS**: live API read confirms all 5 audited points (`536101, 536102, 537200, 534630, 534989`) present among 156 total service points with `trạng_thái` preserved verbatim as `"Tạm dừng"`, and all 11 ĐTC2 stop references to them resolve real, non-null geometry; code confirms default-hidden/toggleable/visually-distinct marker logic. **Item 5 (cross-module regression) — Technical PASS**: `git diff` since the PO Gate 3 baseline (`7da98a79`) shows zero touches to any Phase 1/2 map, auth, or routing code outside the Phase 4 import files — only Phase 4 delivery-routes-import files plus the unrelated, already-closed `AUTO-IMPORT-013` files changed; `deliveryRoutesImport.js`'s diff is dead-code removal only; 117/117 backend + 13/13 frontend tests pass. **Item 6 (data integrity) — Technical PASS with one explained baseline shift**: `network_delivery_point` (287,759 = 143,475 June + 144,284 May), `network_service_point` (156), `network_level2_route` (28), `network_level2_route_stop` (148) all match expected baselines; `fact_f13` (673,781 vs. the Phase 3 closure baseline 669,847, +3,934) traced exactly to a single `2026-08-06` cohort matching the real, unrelated, already-PO-confirmed TCT F1.3 Import performed during `AUTO-IMPORT-013` — not a `NETWORK-MANAGEMENT-001` regression (same pattern as the previously-accepted `fact_f13` growth from `AUTO-IMPORT-011`); `Data QLML/` checksums and both stashes unchanged.
- Does not constitute Phase 4 or program-wide PO PASS. A concrete ordered Product Owner checklist for items 2, 3-4, and 5 (item 6 needs no UI check) was provided in the accompanying chat report. Full evidence: `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-001_CHECKPOINT_001.md` Section 23; `docs/10_TICKETS/NETWORK-MANAGEMENT-001_MANIFEST.md` Section 33.

## AUTO-IMPORT-014 — HUE/TCT Session Reliability Hardening, Phase 1 Discovery Only

- Date: `2026-08-08`. Product Owner reported new runtime evidence after `AUTO-IMPORT-013`'s `PO RUNTIME PASS` closure: HUE initially reported an invalid session; TCT logged in, auto-hid, and imported `2026-08-07` successfully; returning to HUE produced multiple Chrome windows (one authenticated but not recognized by Import, another still requesting login); closing the extra window caused the HUE task to go `FAILED`; "Thử lại đăng nhập" recovered and imported `2026-08-07` successfully; Re-updating HUE for `2026-08-06` brought Chrome to the foreground again, hiding only after Re-import finished. Product Owner explicitly framed this as a general HUE/TCT session-lifecycle reliability defect (`AUTO-IMPORT-014`), not a per-symptom patch, and authorized Phase 1 discovery only — no code change, no further PO testing requested this phase. `NETWORK-MANAGEMENT-001` was paused again at its exact current state, untouched, not rolled back. Does not reopen `AUTO-IMPORT-013`; its `LOGIN_TIMEOUT` fix (commit `f7a74d4f`) remains correct and untouched.
- Read-only code trace of `dkclSessionPreflightService.js`, `dkclHueF13PortalClient.js`, `browserProcessManager.js`, `dkclHueF13BackfillService.js`, `tctF13BackfillService.js`, and `DataImportCenter.jsx`'s polling found a systemic root cause: `entry.client`/`entry.page`, held in the module-level `globalRegistry` Map shared by every consumer regardless of how many `DkclSessionPreflightService` instances exist, is a single mutable resource accessed concurrently by three independent paths with no operation-level mutex — an unconditional 5-second frontend preflight poll for both sources (`DataImportCenter.jsx`, not gated on active-queue state), the interactive login flow, and the Import backfill worker (`getInteractiveClient(source)`, identical pattern for HUE and TCT). `preflight()` contains a destructive branch: once the session reaches `F13_READY`, every 5-second poll re-probes the *live* page via `isF13ReportReady()`/`isAuthenticated()`, and if both report false at that instant — plausible mid-navigation while an Import operation is actively using the same page — it calls `oldClient.close()` and discards the session, based on a single transient reading rather than a real failure.
- The only existing mitigation, `entry.activeOperation === 'HUE_QUEUE_RUNNING'`, is HUE-specific, set only for the duration of a full backfill queue (not during the pre-queue authentication check or any single non-queue "Update" action), and has **no TCT equivalent at all** (confirmed by search — no `activeOperation`/`TCT_QUEUE_RUNNING` reference exists in `tctF13BackfillService.js`). TCT's one clean run in the reported sequence is not evidence of structural immunity — both sources share the identical exposure; TCT does have one narrowly-scoped, non-destructive recovery method HUE lacks (`retryWindowHideOnly()`, re-hides an already-authenticated window without forcing re-login), evidence a "window failed to hide" issue was previously patched for TCT specifically, not evidence of a general race-safety mechanism.
- Every reported symptom maps to this one mechanism: duplicate windows (a new `launchPersistentContext()` call after a destructive close, racing Chromium's own profile-singleton delegation behavior, which is not detected or reconciled against anywhere in this codebase); an authenticated window not recognized by Import (the same race, from the Import worker's perspective); closing a window causing `FAILED` (disconnecting the context mid-navigation throws an unclassified error that surfaces as a generic queue/task failure); Re-update's foreground-then-late-hide (a forced fresh re-authentication is visible for its entire duration because the post-login hide step only runs once, right after a *fresh* login's background task reaches `F13_READY`). `git log` confirms none of this is a recent regression — the last commit touching this code is `f7a74d4f` (`AUTO-IMPORT-013`, adds diagnostics/timeout only, does not touch any of the above).
- A remediation design was proposed (single per-source mutex/serialization; rebind to the authenticated page after login/redirect instead of trusting one captured `page` reference; reconcile/cleanup an orphaned process before a new launch; treat a closed page as fatal only when no valid page remains; Re-update reuse a freshly-verified valid session; hide windows as soon as no PO action is needed; keep HUE/TCT independent; bounded restart/timeout/cleanup; accurate UI messaging) with an estimated file scope, and a mandatory test matrix (new-page login; two pages/one authenticated; closing the old login page; retry after `FAILED`; Re-update valid/expired; backend restart; HUE→TCT→HUE in succession; soak test for accumulating browser/page instances) — **not authorized for implementation**. No code changed. No credentials, cookies, tokens, or raw page content were read, logged, or stored. Production DB, imported data, `NETWORK-MANAGEMENT-001`, `Data QLML/`, and both stashes untouched. Full evidence: `docs/06_REVIEWS/Import/AUTO-IMPORT-014_CHECKPOINT_001.md`; scope and proposed design: `docs/10_TICKETS/AUTO-IMPORT-014_MANIFEST.md`.

## AUTO-IMPORT-014 — Phase 2: PO-Authorized Bounded Implementation, Technical Gate PASS

- Date: `2026-08-08`. Product Owner reviewed the Phase 1 discovery/root cause at governance commit `964428bd` and authorized bounded implementation across 6 numbered items (per-source mutex for preflight/login/session-validation/Import; polling must never close/replace client/page while a source is logging in, running a queue, Updating/Re-updating, or otherwise operation-owned; no session-cancel on a single transient auth-check false reading — bounded retry/reconciliation and error classification before cleanup; rebind to the correct authenticated page after login/redirect, support multiple pages, closing a stray page must not fail the session if a valid one remains; retry must reconcile an old instance before launching a new one, touching only confirmed-QIS-owned processes; Update/Re-update must reuse a valid session and hide the window promptly) plus a mandated 8-item test list, run in phases (A tests, B implementation, C regression, D report+checklist).
- Implemented in `backend/src/services/dkclSessionPreflightService.js`: a new per-source `SourceOperationLock`/`getSourceLock()` (HUE and TCT get independent lock instances, proven by a dedicated concurrency-timing test), wired around `interactiveAuthenticate()`'s reconciliation+launch phase and around the actual expire-and-close branch. `preflight()`'s HUE-only `entry.activeOperation === 'HUE_QUEUE_RUNNING'` exemption generalized to a source-agnostic `entry.activeOperation && entry.authenticated` check, so ANY owning operation for either source now protects the session from ever being touched by a poll. The old single-reading destructive-close branch replaced by new `probeAndMaybeExpireClient()`: checks `isAuthenticated()`/`isF13ReportReady()`, retries once after a bounded delay (`reprobeRetryDelayMs`, default 750ms) before concluding anything, defers entirely if an operation claims ownership mid-probe, and — the key error-classification step — only expires the session if a *confirmed* login form (`client.hasLoginForm()`, new) is present after the retry; an inconclusive reading (neither ready/authenticated nor a confirmed login form) is now reported as still in-progress and the session is kept, not guessed at. The TCT-only `reclaimTctOrphanedProfile` renamed to `reclaimOrphanedProfile` and its `sourceConfig.source === 'TCT'` restriction removed — HUE now gets the same before-relaunch reconciliation TCT already had; ownership safety is unchanged (`selectExactProfileRootPids()` already only ever selects PIDs whose own `--user-data-dir` matches the exact profile directory, for either source — never a personal/unrelated Chrome process).
- Implemented in `backend/src/services/dkclHueF13PortalClient.js`: detection logic factored into `_checkPageAuthenticated(page)` (byte-for-byte identical markers, only refactored to run against any candidate page); `isAuthenticated()` now falls back to new `findAuthenticatedPage()`, scanning `context.pages()` for another open, authenticated page and rebinding `this.page` to it — so a stray/duplicate page being closed no longer fails the session as long as one authenticated page remains open. New `hasLoginForm()` backs the classification step above.
- Implemented in both `backend/src/services/dkclHueF13BackfillService.js` and `backend/src/services/tctF13BackfillService.js`: the actual Import operation's use of the shared client is now wrapped in the new per-source lock (defense-in-depth around the brief window near the `activeOperation` check itself). TCT's `processQueue()`/`finishQueueIfTerminal()` now set/clear `entry.activeOperation = 'TCT_QUEUE_RUNNING'` the same way HUE's have always done — closing the "no TCT equivalent" gap the Phase 1 discovery found; this is left intentionally set while a TCT queue is `BLOCKED` (a pre-existing, TCT-specific retryable-pause state), consistent with — not contrary to — protecting a not-yet-abandoned session.
- 14 new automated test blocks added: `backend/test_dkclSessionPreflightService.js` TEST 14-20 (bounded retry recovers a transient false reading; a confirmed login form does expire, once, under the lock; an inconclusive reading keeps the session; `activeOperation` protects both HUE and TCT identically; the source lock genuinely serializes same-source callers while never blocking the other source; HUE→TCT→HUE leaves exactly one registry entry per source; an 8-cycle login/cancel soak leaves no accumulated registry entries or unclosed clients) plus a rewritten TEST 9-HUE (HUE orphan reclamation, mirroring the pre-existing TCT test); `backend/test_dkclHueF13SyncService.js` TEST 22-25 (multi-page rebind, closing a stray page not failing the session, a genuine no-authenticated-page failure, `hasLoginForm()` classification — all against the real `DkclHueF13PortalClient` class).
- Full regression: `test_dkclSessionPreflightService.js` (34 blocks total, all pass), `test_dkclHueF13SyncService.js` (135/135, up from 129), `test_dkclHueF13BackfillService.js` (39/39), `test_tctF13BackfillService.js` (all pass), `test_dkclHueBrowserBroker.js` (pass), `test_browserProfileLock.js` (all pass, file untouched) — all unchanged from baseline behavior. `oxlint` on all 4 changed source files: 6 warnings, all confirmed pre-existing on untouched lines via `git stash`/rerun/`git stash pop`. No frontend change was needed. `AUTO-IMPORT-013`'s fix (commit `f7a74d4f`) untouched and not reopened.
- `TECHNICAL GATE PASS` recorded. No `PO PASS`/`CLOSED` declared; no Product Owner runtime testing was required as a precondition of this gate. A 7-item PO Runtime Acceptance Checklist was provided (fresh HUE/TCT login+import; a poll-vs-Import race left running to completion; Re-update session reuse; retry-after-timeout reconciliation; HUE→TCT→HUE in one sitting; optional longer soak). Production DB, imported data, `NETWORK-MANAGEMENT-001`, `Data QLML/`, and both git stashes untouched; no credentials/cookies/tokens/page content read, logged, or stored at any point. Full evidence: `docs/06_REVIEWS/Import/AUTO-IMPORT-014_CHECKPOINT_002.md`; updated scope/status: `docs/10_TICKETS/AUTO-IMPORT-014_MANIFEST.md`.

## AUTO-IMPORT-014 — PO Runtime Partial Pass; TCT Re-Update `DUPLICATE_DATES` Delta Root-Caused And Fixed

- Date: `2026-08-08`. Product Owner runtime-tested Phase 2 (commit `0c5d02c3`) on the real machine: HUE login/Import/Update stable; HUE Re-update reuses the existing session; TCT login/Import/Update succeeded; HUE → TCT → HUE in one sitting showed no lost session, no duplicate window, no polling error — all `PASS`. One `FAIL`: TCT, selecting exactly one date (`2026-08-07`) and clicking "Re-Update (1)", returned `"Duplicate refresh_dates are not allowed"` (`DUPLICATE_DATES`).
- Payload trace: the checkbox/`"Update lại"` button both call `toggleTctSelectedDate(date, status)`, which calls `setTctSelectedDates((current) => { ...; if (status === 'COMPLETE') setTctRefreshDates(...); return ...; })` — a `setState` call nested as a side effect inside another `setState`'s functional updater. `frontend/src/main.jsx` wraps the app in `<React.StrictMode>`, which intentionally invokes functional updaters twice in development specifically to surface impure/side-effecting updaters like this one. The outer computation (`tctSelectedDates`, gated by `current.includes(date)`) is idempotent, so the double-invocation is invisible there — it stayed correct at exactly one entry, matching the "(1)" label. The nested `setTctRefreshDates((refCurrent) => [...refCurrent, date].sort())` call, however, is a real, separately-dispatched update each time the outer body runs; React applies queued functional updates in order, each seeing the prior result, so the two StrictMode-triggered dispatches compounded `[] → ['2026-08-07'] → ['2026-08-07','2026-08-07']` — a genuine duplicate produced by a single correctly-registered click. This is frontend-created duplication, not a backend normalization defect and not stale/merged queue state; the backend's `normalizeOptionalDates()` behaved exactly as designed given the payload it received.
- TCT vs HUE comparison: both call paths are structurally identical and equally exposed — HUE's shared `toggleDateSelection()` helper (`hueSelectionHelpers.js`) has the exact same unconditional-append pattern nested inside the exact same kind of `setState`-in-`setState`-updater side effect. HUE not failing in this PO test round is circumstantial (timing of which double-invocation happened to fire), not a structural difference; the fix was therefore applied to both.
- Fix, frontend-only, minimal: `toggleDateSelection()` (shared, HUE) and `toggleTctSelectedDate()` (TCT-local, `DataImportCenter.jsx`) both now guard the refresh-dates append with a membership check before appending; `handleStartTctBackfillQueue()`'s actual submission additionally de-duplicates via `Array.from(new Set(...))` immediately before sending, as a second safety net. Backend `normalizeOptionalDates()` in `tctF13BackfillService.js` is unchanged — deliberately left exactly as strict as before, per explicit instruction not to weaken validation; a new test confirms a genuine duplicate array is still correctly rejected.
- Regression coverage added: `frontend/src/pages/dataImportHueSelection.test.js` (exactly-one-date; three distinct dates with no collision; a direct simulation of the StrictMode double-invocation proving the fix is idempotent; re-clicking an already-selected date toggles cleanly off; a stale/leftover refresh-dates entry from an earlier cycle is preserved, not duplicated, by a later unrelated selection); `frontend/src/pages/dataImportTctScan.test.js` (static-source assertions for the guard and the de-dup submission code; one pre-existing assertion updated to match); `backend/test_tctF13BackfillService.js` TEST 4B (the exact PO-reported single-date scenario now succeeds; three distinct refreshed dates succeed; a genuine duplicate in `refresh_dates` — not just `dates` — is still rejected with `DUPLICATE_DATES`).
- Validation: all touched/adjacent frontend and backend suites pass (`dataImportHueSelection.test.js`, `dataImportTctScan.test.js`, `dataImportWave3Ui.test.js`, `importDashboardReconciliation.test.js`, `test_tctF13BackfillService.js`, plus unaffected confirmation runs of `test_dkclSessionPreflightService.js` and `test_dkclHueF13SyncService.js` since this delta never touched session-lifecycle code); `oxlint` clean vs. baseline. Scope: 5 files (1 frontend production file plus `hueSelectionHelpers.js`, 3 test files) — no backend production code, no `NETWORK-MANAGEMENT-001`/Module QLML, no production DB/imported-data change, `Data QLML/` and both stashes untouched, no credentials/cookies/tokens/page content touched.
- Recorded as `AUTO-IMPORT-014: PO RUNTIME PARTIAL PASS / TCT RE-UPDATE FAIL` (now fixed, `TECHNICAL GATE PASS` for this delta) — **not** `PO PASS`, **not** `CLOSED`. Requesting only a targeted Product Owner recheck: TCT Re-Update for `2026-08-07`; the rest of the original checklist already passed and is not being re-requested. Full evidence: `docs/06_REVIEWS/Import/AUTO-IMPORT-014_CHECKPOINT_003.md`; updated scope/status: `docs/10_TICKETS/AUTO-IMPORT-014_MANIFEST.md`.

## AUTO-IMPORT-014 — COMPLETED / PO RUNTIME PASS / CLOSED

- Date: `2026-08-08`. Product Owner confirmed the final targeted recheck against commit `6159b8b7`: TCT Re-Update for `2026-08-07` succeeded, no `DUPLICATE_DATES` error. While the operation was `RUNNING`, the TCT Chrome window was visibly shown; immediately after completion, the Chrome windows auto-hid correctly. The Product Owner explicitly accepts this behavior — the window is not required to hide while the task is still `RUNNING`, only on completion, which it does. No lost session, no leftover/duplicate window, no new error. All other runtime items (HUE login/Import/Update, HUE Re-update session reuse, TCT login/Import/Update, HUE → TCT → HUE sequence) had already passed in the prior round and were not re-tested.
- The PO Runtime Acceptance Checklist's window-hide criterion (originally "the window hides promptly") is corrected to explicitly state: the window may remain visible for the duration of an active `RUNNING` operation; it is only required to auto-hide once the operation completes. This matches the code exactly as already implemented in Phase 2 (per-item hide calls around each import, and the interactive-login background task's own hide step once the session reaches `F13_READY`) — no code change was needed to align the documented criterion with the already-correct, now PO-confirmed behavior.
- Governance-only closure: no code, test, or data changed. `NETWORK-MANAGEMENT-001` / Module QLML not touched, remains `PAUSED` exactly as previously recorded. Production DB, `Data QLML/`, and both git stashes untouched throughout. No credentials, cookies, tokens, or raw page content read, logged, or stored at any point across the entire ticket (Phases 1-3).
- `AUTO-IMPORT-014` is closed. The systemic HUE/TCT session-lifecycle race identified in Phase 1 discovery, the bounded implementation in Phase 2 (commit `0d959eb`), and the TCT Re-Update `DUPLICATE_DATES` delta fixed in Phase 3 (commit `8ea547e`) are all confirmed working end-to-end by the Product Owner on the real machine. Full evidence: `docs/06_REVIEWS/Import/AUTO-IMPORT-014_CHECKPOINT_004.md`; final ticket status: `docs/10_TICKETS/AUTO-IMPORT-014_MANIFEST.md`.

## NETWORK-MANAGEMENT-001 — 5-Point ĐTC2 Discovery Delta PO Confirmed Closed

- Date: `2026-08-10`. Delta-only, read-only discovery traced the 5 audited "Tạm dừng" điểm (`536101, 536102, 537200, 534630, 534989`) to their raw sources: all 5 present in `Mang_diem_phuc_vu_kem_du_lieu_ban_do.xlsx` sheet `Huế` as officially registered service points (2 of 5 also in sheet `TMS- NODE`); absent from `2026.08. Mang DTC2.xlsx` entirely (ĐTC2 route geometry for these 5 sourced from the HTML reference's `MAIL_ROUTES` array instead, coordinates matching exactly); DB-traced to Import batch `network_import_log.id=10` (`bosung_5diem_tamdung.xlsx`).
- Product Owner confirmed: all 5 are real service points (`điểm phục vụ thật`), status `Tạm dừng` — no code, coordinate, or classification change made. The 3 mã (536102, 537200, 534630) with an ĐTC2 route label differing from their official `Tên ĐPV` are accepted as an operational label, not a defect. The `TMS- NODE` gap for those same 3 mã is explicitly not to be investigated further.
- Documentation-only — no product code, schema, or data changed at any point in this delta. `Data QLML/` and both stashes untouched. Closes this discovery delta only. Full evidence: `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-001_CHECKPOINT_001.md` Section 24; `docs/10_TICKETS/NETWORK-MANAGEMENT-001_MANIFEST.md` Section 34.

## NETWORK-MANAGEMENT-001 — Sơ đồ tuyến phát 29-Column Standardization Discovery, PO Decision: Not Now

- Date: `2026-08-10`. Delta-only, read-only discovery for standardizing Sơ đồ tuyến phát Import/Export to the real 29-column BatchFile structure: mapped all 29 source columns (11 already persisted to `network_delivery_point`, 17 currently read-through-and-discarded); confirmed the raw-file archive mechanism (`fileArchive.js` / `network_import_archive`, no retention limit) already meets the "preserve original file for audit" requirement; found Export currently produces its own 11-column report, not the original 29-column structure; measured `network_delivery_point` at 287,759 rows / ~56.2 MB with a ~22-36 MB/month growth estimate if a full 29-column detail table were added; confirmed the snapshot/rollback mechanism is generic per-table and would extend without redesign if a detail table were added later.
- Product Owner decision: do not persist all 29 columns now. Keep `network_delivery_point` exactly as-is (11 business fields, unchanged) — sufficient for the map and current features. The raw BatchFile continues to be archived in full for audit (existing mechanism, unchanged). KPI-relevant fields will be added later on a per-need basis as concrete requirements arise, not as one full 29-column detail table now, to avoid unnecessary SQLite growth. No backfill for the 2 already-Imported months (05/2026, 06/2026). No migration created.
- Documentation-only — no product code, schema, or data changed at any point in this delta. `Data QLML/` and both stashes untouched. Closes this discovery delta only. Full evidence: `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-001_CHECKPOINT_001.md` Section 25; `docs/10_TICKETS/NETWORK-MANAGEMENT-001_MANIFEST.md` Section 35.

## NETWORK-MANAGEMENT-001 — Program Final Closure: COMPLETED / PO FINAL PASS / CLOSED

- Date: `2026-08-10`. Product Owner confirmed having reviewed the complete evidence set accumulated across the full 4-phase `NETWORK-MANAGEMENT-001` program and explicitly authorized closing the ticket in full. This closure synthesized already-recorded evidence — no new Product Owner runtime/UI recheck was requested or performed.
- Program summary: Phase 1 (Nền tảng) `PO Gate 1 PASS` (2026-08-05); Phase 2 (Ba bản đồ) `PO Gate 2 PASS` (2026-08-05, "PO ROUTE VISUAL RECHECK PASS — LEGEND AND ROAD ROUTES ACCEPTED"); Phase 3 (Import) `PO Gate 3 PASS` (2026-08-06, baseline `7da98a79eb8`, after a same-day remediation of 3 runtime-recheck defects plus a further ĐTC2 journey-visual/arrow round); Phase 4 (Nghiệm thu) — Sơ đồ tuyến phát data-contract audit + remediation (raw-BatchFile-native Import, "Biển số" removed, period cross-check, file archive), two independent recheck-fail/fix cycles against the real May 2026 BatchFile (sheet-detection bug then text-cell type-coercion bug, each root-caused and fixed same day), `Sơ đồ tuyến phát Import PO PASS` (2026-08-07: real unmodified May BatchFile, 144,289 valid rows — 144,284 new / 5 duplicate / 0 error, Confirm succeeded via `network_import_log.id=17`, period 05/2026 live), manifest §6's full acceptance checklist (admin-only enforcement across all 3 modules; "Tạm dừng" point handling + the 5 audited points; cross-module regression; data-integrity reconfirmation) reaching `Technical PASS` (2026-08-08, read-only verification, no code changed, no defect found), and 2 closed Product Owner-confirmed discovery deltas — the 5-point ĐTC2 source-classification discovery (all 5 confirmed real service points, status `Tạm dừng`, no code/data change) and the Sơ đồ tuyến phát 29-column standardization discovery (Product Owner explicitly decided not to persist all 29 columns now, no migration created) — both `2026-08-10`.
- Product Owner FINAL PASS (`2026-08-10`) covers manifest §6 checklist items 2 (admin-only), 3-4 ("Tạm dừng" handling), and 5 (cross-module regression) — previously the only items awaiting a live Product Owner UI check — together with everything already PO-accepted above. This constitutes `PO Gate 4 PASS` and program-wide `PO FINAL PASS`.
- Documentation-only closure — no product code, schema, UI, or database change was made or needed to reach this closure; the underlying product (3 authenticated Leaflet map modules, Import/Export/History/Rollback, the live May 2026 data) was already in its final PO-accepted state before this round. `Data QLML/` and both pre-existing git stashes (`stash@{0}`, `stash@{1}`) confirmed untouched throughout the program's entire lifecycle.
- State after closure: no active ticket. `AWAITING PO DIRECTION` for any next scope — the Product Owner-named future "Bản đồ tổng thể mạng lưới" module remains noted only, not authorized, not self-activated. Full evidence: `docs/10_TICKETS/NETWORK-MANAGEMENT-001_MANIFEST.md` Section 36; `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-001_CHECKPOINT_001.md` Section 26.

## NETWORK-MANAGEMENT-002 — Activation, Discovery + Planning Complete

- Date: `2026-08-10`. Product Owner explicitly authorized a new, independent ticket — Bản đồ tích hợp Điểm phục vụ + Đường thư cấp 2 — activated the same day `NETWORK-MANAGEMENT-001` closed `COMPLETED / PO FINAL PASS / CLOSED`; that program is not reopened, not touched. Locked scope, given directly in the activation prompt: exactly one integrated read-only map screen; data read directly from the two existing source modules (Mạng điểm phục vụ, Mạng đường thư cấp 2), no data duplication; independently toggleable layers for Điểm phục vụ and ĐTC2; preserve the two source screens' already-Product-Owner-approved marker/popup/legend/routing/interaction behavior verbatim; no new database table; no dedicated Import/Export/History/Rollback for this screen; no behavior change to the two original screens; the F1.3 Shipment Detail/Evidence screens not folded into this ticket.
- Claude Code performed read-only, delta-only discovery only in this round — no product code, schema, or database change made. Findings: both source screens already read via existing `admin`+`viewer`-readable, unfiltered API endpoints (`GET /api/network-map/service-points`, `GET /api/network-map/level2-routes`) through `NetworkMapClient.js`'s existing methods, so zero backend/schema/migration work is needed to build the new screen; both source Map components (`ServicePointsMap.jsx` ~16 KB, `Level2RoutesMap.jsx` ~34 KB) are self-contained, each owning its own `<MapContainer>` and all marker/popup/polyline/legend JSX in one file, with no existing reusable "layer-only" component; shared pure modules (`mapStyles.js`, `roadRoutingService.js`, `routeJourneyGeometry.js`) already exist and are directly reusable regardless of implementation approach; the ĐTC2 layer's full approved interaction (real road routing on the 28-route overview plus the complete selected-route journey-visual mode — outbound/turnaround/return classification, spiderfy, arrows, hover/click sync) is the ticket's main implementation-complexity item, not optional under the "keep approved routing/interaction" lock since route selection is normal ĐTC2 usage; nav/routing wiring (`App.jsx`, `navigation/appNavigation.jsx`) follows an established, simple pattern for a 4th `network-map` screen; a latent, currently-dead `VIEWER_ALLOWED_PATH_PREFIXES` constant was noted for defensive consistency, not an active blocker; no dedicated component/visual automated tests exist for any of the 3 current map screens, so this ticket's test plan follows the same established precedent (real-browser technical verification + PO UI acceptance, automated tests reserved for extracted pure logic).
- Two implementation options identified and recorded in the manifest (Section 8): Option A extracts shared layer components (touches the two original, already-PO-approved files, requires proven behavior-equivalence); Option B leaves the two original files completely untouched (some JSX duplication, zero regression risk to the originals). This is a technical implementation choice, not a business-rule decision — Claude Code recommends and will default to Option B absent further Product Owner/CTO direction.
- Documentation-only round — no product code, schema, UI, or database change made. `Data QLML/` and both stashes confirmed untouched. Full evidence: `docs/10_TICKETS/NETWORK-MANAGEMENT-002_MANIFEST.md`; `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-002_CHECKPOINT_001.md`.

## NETWORK-MANAGEMENT-002 — Implementation Delivered, READY FOR PO CHECK

- Date: `2026-08-11`. Product Owner/CTO approved implementation via Option B (manifest §8, zero-touch to the two original source screens) and authorized building it. Delivered exactly the locked scope: new `frontend/src/features/networkMap/IntegratedMap.jsx` renders Mạng điểm phục vụ + Mạng đường thư cấp 2 on one shared `<MapContainer>` at `/network-map/integrated` (`admin`+`viewer` read, both layers default open), re-implementing the two source screens' full already-Product-Owner-approved marker/popup/legend/routing/journey-visual behavior — including ĐTC2 route selection, outbound/turnaround/return direction, same-coordinate spiderfy, and direction arrows — on the same shared pure modules (`mapStyles.js`, `roadRoutingService.js`, `routeJourneyGeometry.js`) those screens already use; new `IntegratedMapPage.jsx` fetches both datasets via the existing `NetworkMapClient` methods; `App.jsx` (+1 route) and `navigation/appNavigation.jsx` (+1 nav entry) wired following the established 3-screen pattern.
- `ServicePointsMap.jsx`, `Level2RoutesMap.jsx`, their Page wrappers, every backend file, `schema.sql`, and `auth/roles.js` confirmed untouched via `git diff --name-only` — `auth/roles.js` deliberately left alone per explicit instruction not to edit its dead `VIEWER_ALLOWED_PATH_PREFIXES` configuration defensively.
- Validation: `oxlint` clean on all changed files (full-repo `oxlint` shows only pre-existing, unrelated warnings); `vite build` succeeds (688 modules); `node --test` on the 3 relevant frontend suites (`networkMapRemediation.test.js`, `routeJourneyGeometry.test.js`, `NetworkMapClient.test.js`) — 53/53 pass, unchanged. Real-browser verification as `admin`: `/network-map/integrated` loads real data (156 điểm phục vụ, 28 hành trình); all 4 layer-toggle states (both on / only Điểm phục vụ / only ĐTC2 / both off) confirmed independently; ĐTC2 route selection on Tuyến 6 reproduces the exact known-good outbound (BCVH Phú Lộc→Lộc Thủy→Thừa Lưu)/turnaround (Lăng Cô)/return (Thừa Lưu→BCVH Phú Lộc) behavior with the same-coordinate Thừa Lưu revisit spiderfied, mode toggle and Quick Info Badge rendering correctly; deselect ("✕ Bỏ chọn") reverts cleanly to the 28-route overview; `ServicePointsPage`/`Level2RoutesPage` re-verified live and render identically to before (156/156 điểm, 28 hành trình/1435 km, same legend counts, admin Import sections unaffected). Live viewer-role check not performed — no viewer plaintext credential is available or appropriate to obtain (same precedent as the closed `NETWORK-MANAGEMENT-001` Phase 4 verification round) — role gating confirmed instead by static code (`ProtectedRoute allowedRoles={[ROLE_ADMIN, ROLE_VIEWER]}`, byte-identical to the other 3 network-map routes), flagged as a Product Owner checklist item.
- Data-integrity check: `network_service_point` (156), `network_level2_route` (28), `network_level2_route_stop` (148), `network_delivery_point` (287,759) all unchanged before/after — this feature performs zero database writes. `fact_f13` moved (682,833, was 673,781 at the last recorded checkpoint) — unrelated background F1.3 activity in this live environment, not touched by this read-only ticket. May/June BatchFile SHA-256 checksums unchanged; `Data QLML/` and both stashes (`stash@{0}`, `stash@{1}`) confirmed untouched.
- State: `READY FOR PO CHECK`. Claude Code updated governance accordingly and does not self-award PO PASS or declare this ticket closed. A concrete, ordered Product Owner runtime checklist was provided in the corresponding chat report. Full evidence: `docs/10_TICKETS/NETWORK-MANAGEMENT-002_MANIFEST.md` Section 14; `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-002_CHECKPOINT_001.md` Section 12.

## NETWORK-MANAGEMENT-002 — PO Runtime Fail (Marker Density) + Remediation, READY FOR PO RECHECK

- Date: `2026-08-11`. Product Owner reported a runtime FAIL on the first delivered version of `/network-map/integrated`: with both layers on, 156 Điểm phục vụ markers + 28 ĐTC2 routes overlapped too heavily to read; the Điểm phục vụ layer only had a whole-layer on/off toggle, no way to reduce marker density by Loại điểm.
- Remediation, delta-only, scoped to `frontend/src/features/networkMap/IntegratedMap.jsx` only (confirmed via `git diff --name-only` — no other file touched): replaced the single-select "click a category to filter" legend behavior with independent per-Loại-điểm checkboxes, all default checked. The category list is derived from real data (`Object.keys` of counts built by running every point through the existing shared `normalizeLoaiDiem()`), never a separately hand-typed array. New `visibleCategories` state is seeded once per newly-seen category and otherwise never reset, so toggling the whole Điểm phục vụ layer off and back on restores the exact same per-category selection from earlier in the session. Marker filtering and the legend's `visible/total` badges share one `passesNonCategoryFilters()` helper, so the counts shown can never drift from what's actually rendered. Combines correctly with the existing (unchanged) status filter, "Hiện điểm Tạm dừng" toggle, search, and the ĐTC2 layer/route-selection.
- Validation: `oxlint` clean, `vite build` succeeds, 53/53 relevant frontend tests pass unchanged. Real-browser re-verification as `admin`: all categories on (default) — 151/156, matches the pre-remediation baseline exactly (5 "Ngừng hoạt động" hidden by the unchanged default rule); only 1 category checked (Văn hoá xã) — 102/156, exact match; multiple categories checked (Văn hoá xã + Giao dịch + Văn phòng) — 144/156 = 102+35+7 exactly; all categories unchecked — 0/156, zero markers; combined with status filter "Ngừng hoạt động" + 1 category checked (Giao dịch) — 2/156, exact match to that category's Tạm dừng rows; category filters confirmed unaffected by toggling the ĐTC2 layer off then back on; ĐTC2 route selection (Tuyến 6) re-verified working correctly — same known-good outbound/turnaround(Lăng Cô)/return/spiderfy behavior — while category filters are active, and deselect reverts cleanly. `git diff --name-only` confirms only `IntegratedMap.jsx` changed — `ServicePointsMap.jsx`, `Level2RoutesMap.jsx`, their Pages, and every backend/schema/migration file remain untouched.
- Data-integrity: `network_service_point`(156)/`network_level2_route`(28)/`network_level2_route_stop`(148)/`network_delivery_point`(287,759) unchanged (zero writes, this feature is read-only). `Data QLML/` and both stashes (`stash@{0}`, `stash@{1}`) confirmed untouched.
- State: `READY FOR PO RECHECK`. Claude Code does not self-award PO PASS and does not declare this ticket closed. A concrete, ordered Product Owner recheck checklist was provided in the corresponding chat report. Full evidence: `docs/10_TICKETS/NETWORK-MANAGEMENT-002_MANIFEST.md` Section 15; `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-002_CHECKPOINT_001.md` Section 13.

## NETWORK-MANAGEMENT-002 — PO PASS — Closure

- Date: `2026-08-11`. Product Owner performed the runtime recheck of `/network-map/integrated` and explicitly confirmed `PO PASS`: the per-Loại-điểm checkbox filter (previous remediation round) resolved the reported marker/route overlap density issue. Product Owner explicitly authorized closing `NETWORK-MANAGEMENT-002` in full.
- Final delivered scope, unchanged from the locked scope given at activation: one integrated read-only map screen at `/network-map/integrated` (`admin`+`viewer`); Điểm phục vụ and Đường thư cấp 2 layers independently toggleable, both default on; Điểm phục vụ further filterable per Loại điểm via independent checkboxes derived from real data (default all-on, selection persists across the layer's own on/off toggle); full ĐTC2 routing/journey-visual behavior preserved (route selection, outbound/turnaround/return, spiderfy, direction arrows); no data duplication, no new database table, no dedicated Import/Export/History/Rollback for this screen. The two original screens (`ServicePointsMap.jsx`/`ServicePointsPage.jsx`, `Level2RoutesMap.jsx`/`Level2RoutesPage.jsx`) were never modified across the entire ticket — discovery, implementation, and remediation rounds all confirmed via `git diff --name-only`.
- Documentation-only closure — no product code, schema, or database change made or needed in this round. `Data QLML/` and both pre-existing git stashes (`stash@{0}`, `stash@{1}`) confirmed untouched throughout the ticket's entire lifecycle.
- State: no active ticket. `AWAITING PO DIRECTION` for any next scope. `NETWORK-MANAGEMENT-001` remains separately `COMPLETED / PO FINAL PASS / CLOSED` (2026-08-10), unaffected. The Product Owner-named future "Bản đồ tổng thể mạng lưới" module remains noted only, not authorized, not self-activated by this closure. Full evidence: `docs/10_TICKETS/NETWORK-MANAGEMENT-002_MANIFEST.md` Section 16; `docs/06_REVIEWS/Shared/NETWORK-MANAGEMENT-002_CHECKPOINT_001.md` Section 14.

## F13-STANDARDIZATION-001 — Evidence / Chi tiết bưu gửi Discovery Delta

- Date: `2026-08-10`. Delta-only discovery, opened within the existing `F13-STANDARDIZATION-001` program group rather than a new ticket naming series, scoped strictly to the Evidence/Chi tiết bưu gửi (shipment detail) screen. Discovery and governance only — no product code changed. Does not reopen `NETWORK-MANAGEMENT-001`/`NETWORK-MANAGEMENT-002`, does not audit the whole repository. `Data QLML/`, `.claude/`, and both pre-existing git stashes untouched.
- Findings, confirmed by direct file read: the nav-visible `/f13/evidence` route (`F1.3 Quality Management → Evidence`, `admin`-only) still renders `<PlaceholderPage title="Evidence List" />`, unchanged since the `2026-08-04` `F13-DATABASE-PRODUCT-OPPORTUNITY-AUDIT-PLAN` audit; a real, working shipment-detail implementation already exists at `/f13/ranking/shipment` (`ShipmentPerformancePage.jsx`, "Shipment Performance Center") but is orphaned from navigation (`appNavigation.jsx` does not list it), reachable only by direct URL; both screens are backed by the exact same single backend contract (`GET /f13/evidence-list` → `DashboardController.getEvidence` → `f13DashboardService.getEvidenceList`), and `F13DashboardClient.js`'s `getShipmentEvidenceList()` is confirmed still a direct pass-through alias of `getEvidenceList()`; a third, narrower consumer of the same contract is `RouteViolationEvidencePage.jsx` (route-violation drill-down, `/f13/ranking/route/violations`).
- This exact convergence is already recorded, unresolved, in three prior artifacts: this program's own Phase 1 ("Keep Evidence as the official shipment detail screen") and Phase 3 ("Complete Evidence as the single lookup for detailed shipment verification"), both `PLANNED / NOT ACTIVE`; the `2026-08-04` `F13-DATABASE-PRODUCT-OPPORTUNITY-AUDIT-PLAN` closure's outstanding "MERGE confirmation (Evidence → Shipment Ranking)", never Product Owner-decided since; and the deferred `F13-SHIPMENT-001` (`stash@{0}`, deferred `2026-07-28`). A fourth, named-but-never-created candidate, `F13-SURFACE-CLEANUP-PLAN`, also covers this exact scope in `PROJECT_SNAPSHOT.md`'s next-direction candidates.
- No implementation performed or authorized. Product Owner decision requested on: (1) confirming or amending the MERGE direction; (2) which of the four converging planning artifacts should carry implementation scope, to avoid a fifth competing plan; (3) whether to authorize implementation now.
- State: `DISCOVERY COMPLETE / AWAITING PO DECISION`. Full evidence: `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 17; `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` Section 12.

## F13-STANDARDIZATION-001 — Evidence / Chi tiết bưu gửi Implementation, READY FOR PO CHECK

- Date: `2026-08-11`, commit `a66fa57`. Product Owner issued a 12-point remediation decision authorizing implementation, kept within this existing program group per explicit instruction (no `F13-SHIPMENT-001` reactivation, no `F13-SURFACE-CLEANUP-PLAN` created).
- Delivered: `ShipmentPerformancePage.jsx` becomes the canonical component at `/f13/evidence` (`admin`+`viewer`, replacing `PlaceholderPage`); `/f13/ranking/shipment` redirects to `/f13/evidence` preserving the full query string (new `LegacyShipmentRedirect`), not deleted; real BCVH/date defaults (`getDashboardMeta()`) and a real BCVH-dependent Tuyến selector (`getRouteRanking(..., route_type=all)`) replace every previous fake fallback (`2026-06-23`/`BC_HUE01`/`R_HUE01_01`); "Tất cả tuyến" enabled via a backward-compatible backend relaxation (`FactBuuGuiRepository.getEvidenceListFacts` drops `ma_tuyen` only when route is absent/`'all'`); single-day analysis contract reused verbatim from Route Ranking's `resolveDefaultRouteDate`; the implicit `pageSize=1000` cap fixed by a new `fetchAllEvidenceRows()` helper walking every backend page (server pagination was already correct — the cap was frontend-only), with a bounded safety ceiling that surfaces rather than silently drops an over-ceiling condition; full drill-down context (`from_date`, `bcvh_id`, `bcvh_name`, `route_id`, `route_name`, `shipment_id`) preserved; `/evidence-list` opened `admin`-only → `admin`+`viewer`.
- Untouched, confirmed via `git diff --name-only`: `RoutePerformancePage.jsx`, `BcvhRankingPage.jsx`, `DashboardPage.jsx`, `RouteViolationEvidencePage.jsx`, `frontend/src/auth/roles.js` (including the still-dead `VIEWER_ALLOWED_PATH_PREFIXES`, left untouched per the `NETWORK-MANAGEMENT-002` precedent), all `NETWORK-MANAGEMENT-001`/`002` files, `Data QLML/`, both stashes, `.claude/`.
- Validation: backend targeted suite 16/16 pass (2 new tests); full backend sweep 107/111 pass, the 4 failures confirmed pre-existing on the baseline via `git stash` (unrelated live-KPI-database/timeline tests, untouched by this diff); frontend 25/25 pass (15 new tests: `shipmentPerformanceData.test.js` pagination-helper tests + new `ShipmentPerformancePage.contract.test.js`); `oxlint` clean; `vite build` succeeds (688 modules).
- Runtime: not exercised by Claude Code — no usable plaintext credential (admin or viewer) exists in this workspace (only a viewer password hash), same precedent as `NETWORK-MANAGEMENT-001` Phase 4. An 11-item PO/Antigravity runtime checklist is recorded instead. `network_service_point`/`network_level2_route`/`network_level2_route_stop`/`network_delivery_point`/`fact_f13` unchanged — zero database writes performed.
- State: `READY FOR PO CHECK`. Claude Code does not self-award PO PASS. Full evidence: `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 18; `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` Section 13.

## F13-STANDARDIZATION-001 — Evidence / Chi tiết bưu gửi, PO RUNTIME CHECK PASS, Closure (delta only)

- Date: `2026-08-11`. Implementation commit `a66fa57d`; governance baseline before the check `ca170c40`.
- Product Owner ran the recheck and reported `8/9 mục PASS`: `/f13/evidence` canonical Evidence screen; legacy `/f13/ranking/shipment` redirect preserves query context; single-day analysis contract; real BCVH list; real Tuyến list; "Tất cả tuyến"; `shipment_id` sync; loading/empty/error + UI/UX.
- The 9th item (>1,000-row live dataset) is `NOT TESTABLE`, not `FAIL` — the real production dataset's largest matching set is `318` records, below the cap this delta fixed. `fetchAllEvidenceRows()`'s page-walking behavior (incl. the over-ceiling `truncated: true` warning) has independent automated test PASS (5 tests), not represented here as a live-runtime PASS.
- Admin+Viewer access recorded per actual evidence only: the PO report does not evidence a live dual-role login, so this is recorded as technical verification (`ProtectedRoute allowedRoles`) + automated routing test PASS (`App.role-routing.test.js`), not a live dual-role runtime PASS.
- Backend test status carried forward precisely: `107/111` at commit `a66fa57d`, same 4 pre-existing failures confirmed via `git stash` — never described as "full backend PASS".
- Scope of this closure: the Evidence/Chi tiết bưu gửi delta only. Does **not** close the `F13-STANDARDIZATION-001` program — Phase 0 remains partial/not separately closed, Phases 1-4 remain `PLANNED / NOT ACTIVE`, unaffected.
- No next ticket activated. `F13-SHIPMENT-001` (`stash@{0}`) remains `DEFERRED / PRESERVED`; `F13-SURFACE-CLEANUP-PLAN` remains not created. `NETWORK-MANAGEMENT-001`/`002` not reopened. Documentation-only closure; `Data QLML/`, `.claude/`, both stashes confirmed untouched.
- State: no active ticket, `AWAITING PO DIRECTION`. Full evidence: `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 19; `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` Section 14.

## F13-STANDARDIZATION-001 — Evidence Product-Value Audit

- Date: `2026-08-11`. Opened because the prior `PO RUNTIME CHECK PASS` (commit `a66fa57d`) proved only technical function, not product value. Discovery/planning only — no product code changed.
- Traced `Tuyến Ranking → current shipment detail → /f13/evidence` end to end.
- **Central finding**: the frozen `EVIDENCE_CENTER_INFORMATION_ARCHITECTURE.md`/`EVIDENCE_CENTER_WIDGET_SPECIFICATION.md` explicitly forbid Evidence Center from duplicating Shipment Performance Center or carrying Recommendation content. The Product Owner-accepted canonical `/f13/evidence` today runs `ShipmentPerformancePage.jsx` — the Shipment Performance Center component itself, including a `ShipmentRecommendation` widget — exactly what the frozen docs forbid. Escalated to Product Owner (Decision 1), not resolved unilaterally.
- Tuyến Ranking's "Mở chi tiết bưu gửi vi phạm" button still targets a separate, already-`PO PASS`-closed screen (`RouteViolationEvidencePage.jsx`, `/f13/ranking/route/violations`, closed `2026-08-04`) built and closed before Evidence's canonicalization (`2026-08-11`) — never reconciled. That screen has violation-reason classification Evidence lacks entirely, but also has Evidence's already-fixed 1,000-row pagination cap, still unfixed there. A `date` vs `from_date`/`to_date` URL-param mismatch between the two screens would silently corrupt the analysis date if merged without translation — flagged pre-emptively.
- All 7 `Shipment*` widgets audited: only `ShipmentEvidenceSummary` is substantively real; `ShipmentExecutiveBrief` real but redundant; `ShipmentImpactOverview`/`ShipmentTimeline`/`ShipmentDrilldown` decorative or fully redundant; `ShipmentRootCause` never surfaces `violation_reason` (the one thing it should); `ShipmentRecommendation` just echoes the delay number with no real threshold logic, and independently violates the frozen Evidence boundary.
- Denominators between Tuyến Ranking and Evidence confirmed identical by construction (same repository query) given identical date/BCVH/route — code trace, not re-measured live.
- Proposed target flow: Tuyến Ranking → violation list (merge onto `RouteViolationEvidencePage.jsx`'s real violation-reason structure + Evidence's route selector/viewer access/pagination fix) → select one shipment → real single-shipment cause/timeline panel → honest hand-off to Action Center. Full KEEP/REMOVE/MERGE/REDESIGN table, no-code wireframe, and 8 acceptance criteria recorded in the dedicated checkpoint.
- 5 Product Owner decisions requested: (1) frozen-architecture path (amend the frozen docs vs. build toward them separately); (2) merge direction; (3) `ShipmentRecommendation` remove-or-redesign; (4) final route/URL; (5) sequencing/authorization.
- No implementation performed or authorized. `F13-SHIPMENT-001` not opened; Dashboard/BCVH Ranking/`Data QLML/` not touched; `NETWORK-MANAGEMENT-001`/`002` not reopened; `.claude/` and both stashes confirmed untouched.
- State: `AUDIT COMPLETE / AWAITING PO DECISION`. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-PRODUCT-VALUE-AUDIT_CHECKPOINT_001.md`; `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 20; `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` Section 15.

## F13-STANDARDIZATION-001 — Evidence Consolidation Plan

- Date: `2026-08-11`, baseline `e2c32178`. Planning only — no product code changed, no frozen document edited.
- **Product Owner decision** on the audit above: keep `/f13/evidence` as the single shared violation-detail screen; Tuyến Ranking must lead into it rather than maintaining a separate shipment-detail flow; streamline Evidence per the audit; remove Recommendation in this scope; planning (not executing) a controlled amendment of the frozen architecture documents is permitted.
- **Three new defects found while planning.** (F-1, blocking) `f13DashboardService.getEvidenceList()`'s mapper returns only 6 fields and discards `ma_tuyen`/`ten_tuyen`/`ma_bcvh`/`ten_bcvh`, though the repository's `SELECT *` already returns them — so in "Tất cả tuyến" mode every row silently falls back to the URL parameter, every shipment displays "Tất cả tuyến" as its route, and route-name search matches nothing; the previous round's headline capability cannot attribute a violation to a route. (F-2, latent crash) `RoutePerformancePage.jsx:262` references an out-of-scope `row`, a `ReferenceError` whenever `route.failed` is nullish, masked only because the backend always populates it. (F-3, baseline correction) the true full frontend suite is **256 pass / 13 fail of 269**, not the "25/25" reported in earlier rounds for a narrow subset; all 13 proven pre-existing via `git diff --name-only b83900af HEAD` over the route/dashboard/pages directories returning empty; one of them sits directly in the path of plan Phase 3.
- Plan contents: exact URL/data/reconciliation contract (Tuyến Ranking sends `from_date` and `to_date` with the same resolved analysis day, so Evidence can never silently fall back to the newest imported day); no-code wireframe with three regions (context/filter, violation list with server-sourced group tabs, evidence detail panel) plus desktop/mobile/loading/empty/error behaviour; KEEP/REMOVE/MERGE/REDESIGN disposition for every widget (removing Impact Overview, Recommendation, Drilldown, and the stale "shell" disclaimer); the old `/f13/ranking/route/violations` becomes a translating redirect (`date` → both `from_date` and `to_date`) at `admin`+`viewer` so old bookmarks land on the correct day, component retired only after acceptance; 14 acceptance criteria (AC-1..AC-5 count reconciliation, AC-6..AC-9 date-parameter protection, AC-10 requiring a real dual-role login); 8 frozen/architecture documents listed as needing amendment, **none edited**, each requiring separate explicit approval; 4-phase implementation plan with test plan and file scope.
- No implementation authorized. `F13-SHIPMENT-001` not opened; Dashboard/BCVH Ranking/`Data QLML/`/`NETWORK-MANAGEMENT` untouched and unexpanded; `.claude/` and both stashes untouched.
- State: `PLAN COMPLETE / AWAITING PO APPROVAL`. Full plan: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md`; `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 21; `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` Section 16.

## F13-STANDARDIZATION-001 — Evidence Consolidation Phase 1 Implementation

- Date: `2026-08-11`, backend-only, additive-only. Implementation commit `b147df7c` (plan approved at `34f42c57`).
- Product Owner approved the plan and authorized Phase 1 only, plus 5 decisions locked for Phase 2/3 (not implemented this round): screen name "Evidence — Chi tiết bưu gửi vi phạm"; arriving from Tuyến Ranking keeps the clicked violation group, total click opens "Tất cả không đạt"; no Action Center button until a real hand-off exists; frozen-document amendment approved in principle, requires its own separate governance delta before Phase 2; the 3 violation groups may only be asserted to sum to the total after proving mutual exclusion + exhaustiveness over the unique `ma_bg` set.
- **Implementation**: `backend/src/services/f13DashboardService.js`'s `getEvidenceList()` mapper now returns `ma_tuyen`/`ten_tuyen`/`ma_bcvh`/`ten_bcvh` (fixes F-1 — the repository's `SELECT *` already returned them, the mapper discarded them, so "Tất cả tuyến" mode could not attribute any row to its real route). **No frontend file touched** — both existing consumers already prefer a real API value over their own URL-parameter fallback via `||`, confirmed by code read; the display/search fix took effect with zero additional frontend change.
- 4 new backend tests: 2 prove route/BCVH pass-through (incl. a direct regression guard that two rows in "Tất cả tuyến" mode resolve two different real routes, never a shared fallback); 2 prove `violation_reason` classification is a true partition (mutually exclusive, exhaustive over `ma_bg`) before trusting the numeric summary, implementing the Product Owner's exact reconciliation instruction.
- Validation, reported precisely per explicit instruction not to overstate scope: targeted evidence-list suite **20/20 pass**; full backend sweep **111/115 pass** (same 4 pre-existing failures, unchanged, unrelated); full frontend sweep re-run despite zero frontend files touched: **256/269 pass**, identical to the plan's F-3 baseline — reported as the true full-suite figure, never narrowed to a targeted subset.
- No Phase 2-4 work performed; no frozen document edited; `F13-SHIPMENT-001` not opened; Dashboard/BCVH Ranking/`Data QLML/`/`NETWORK-MANAGEMENT` untouched and unexpanded; `.claude/` and both stashes confirmed untouched.
- State: `PHASE 1 IMPLEMENTED / READY FOR PO CHECK`. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 12; `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 22; `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` Section 17.

## F13-STANDARDIZATION-001 — Evidence Consolidation Phase 1 Remediation

- Date: `2026-08-11`, frontend-only, no widget consolidation, no frozen document.
- Product Owner ran Phase 1 and reported 2 additional defects.
- **DEFECT A** — Vietnamese IME input (UniKey/EVKey) corrupted the shared `GlobalFilterBar` search box. Root cause: every keystroke's `onChange` synchronously called `setSearchParams`, a React Router navigation heavy enough to interrupt in-progress IME composition (e.g. "phía" → "pịa"); no `compositionstart`/`compositionend` handling existed. Fixed in the one shared component where the `<input>` lives: new `frontend/src/components/shared/searchCommitController.js`, a composition-aware debounced commit controller — never commits mid-composition, commits the final value immediately on `compositionend`, debounces plain typing/paste/delete. Diacritic-insensitive search fallback also added (`stripVietnameseDiacritics`/`matchesSearchQuery`) — exact match tried first, never breaks route-code search.
- **DEFECT B** — empty state didn't distinguish "route has no violations" from "keyword no match" (reported case: Tuyến 535790 - Hương Phong, BCVH A Lưới, 2026-08-10). Re-verified via a direct, read-only query against the real operational database *before any code change*: Tuyến `53579015` has exactly 2 real shipments that day, both `Đạt`, zero `Không đạt` — **the filter was correct, real data, not a defect**. Dropdown contract verified correct by construction (routes only appear if they had real activity that day). Empty state rebuilt into 3 keyword-prioritized branches (keyword unmatched / specific route has no violations / whole context empty) instead of one generic message with an always-shown "chọn 'Tất cả tuyến'" suggestion.
- 24 new tests across 4 files, covering IME composition, paste, delete, fast typing, and all 3 empty-state branches. Full frontend sweep **280/293 pass** (same 13 pre-existing failures, unchanged); backend untouched, sanity re-run **111/115** unchanged; `oxlint` clean.
- No Phase 2-4 work, no widget consolidation, no frozen document edited. `F13-SHIPMENT-001` not opened; Dashboard/BCVH Ranking/`Data QLML/`/`NETWORK-MANAGEMENT` untouched; `.claude/` and both stashes confirmed untouched; the DEFECT B database query was read-only, zero rows written.
- State: `PHASE 1 REMEDIATION IMPLEMENTED / READY FOR PO RECHECK`. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 13; `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 23; `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` Section 18.

## F13-STANDARDIZATION-001 — Evidence Consolidation, PO Finding Locked Into Phase 2

- Date: `2026-08-12`, documentation only — no product code, route, component, schema, or frozen document changed.
- Product Owner confirmed search now filters correctly, but presentation misleads: keyword "hồng th" matched 9 rows; `ShipmentExecutiveBrief` auto-displayed one representative shipment/route; other matching routes were never shown; "Evidence Runtime" KPI kept showing the pre-search total (`30`) with no results list near the search box.
- 10-point contract locked into the not-yet-started Phase 2: no auto-selection on search; explicit "Tìm thấy [n] thuộc [m] tuyến cho '[keyword]'" summary; results grouped by real route (`[mã] - [tên tuyến]` + count, expandable); every matching/near-matching route shown; detail panel updates only on explicit selection; pre-search/post-search/selected counts kept distinct; Tuyến dropdown stays independent of search; explicit 0/1/n states + clear-keyword control + desktop/mobile; reconciliation by real `ma_bg`/`ma_tuyen`, never route-name text; no interim patch to `ShipmentExecutiveBrief` (disposition already locked).
- 9 new acceptance criteria (AC-15 to AC-23) added; wireframe (Section 4) and widget disposition (Section 5) updated to match.
- No implementation performed. Phase 2 remains blocked on its own prerequisite (frozen-document governance delta) regardless. `F13-SHIPMENT-001` not opened; Dashboard/BCVH Ranking/`Data QLML/`/`NETWORK-MANAGEMENT` untouched; `.claude/` and both stashes confirmed untouched.
- State: `FINDING LOCKED INTO PHASE 2 SCOPE / NOT IMPLEMENTED`. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 14; `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 24; `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` Section 19.

## F13-STANDARDIZATION-001 — Date-Filter Cross-Module Remediation

- Product Owner discovered Operation Dashboard's "Bảng điều hành BCVH" table silently discarded `from_date` and only ever queried a single exact day (`to_date`), contradicting its own page's correctly-ranged KPI cards; a bounded, read-only diagnosis was performed first and accepted, then a bounded fix authorized (2026-08-13).
- Contract: Operation Dashboard's BCVH table now genuinely aggregates `ngay_do_kiem BETWEEN from_date AND to_date` (inclusive); BCVH Ranking keeps its single-evaluation-day contract, its request now explicitly sending `from_date === to_date`; Tuyến Ranking and Evidence untouched.
- Fixed purely by parameter wiring, no branching on caller/page identity: `DashboardController.getBcvh` forwards both dates and validates `from_date <= to_date` (`400 INVALID_RANGE`); `f13DashboardService.getBcvhRanking(fromDate, toDate, ...)`; `FactBuuGuiRepository.getBcvhRanking` uses `ngay_do_kiem BETWEEN ? AND ?`, new `getFactBetween` added; `BcvhRankingPage.jsx` request now sends `from_date: toDate`.
- Live-database verification (not mocked) reproduced the PO's exact reported evidence: BCVH Thuận Hóa `533140` single day `2026-08-11` → `1,820/753/986`; range `2026-08-01`–`2026-08-11` → `18,895/10,179/7,841`. The 81-count gap confirmed as pre-existing `danh_gia_2026 IS NULL` rows (same unclassified category already modeled elsewhere) — no metric/formula changed.
- 12 new tests, all passing; full backend sweep 209/213 and full frontend sweep 283/296, both re-verified against a freshly-confirmed true baseline (200/204 backend, 280/293 frontend) with identical pre-existing failures by name — zero regressions.
- Evidence Consolidation Phase 1 Remediation separately received its own PO recheck PASS this same day (2026-08-13); its formal closure stays sequenced behind this date-filter remediation's own recheck, per explicit PO instruction.
- State: `DATE-FILTER REMEDIATION IMPLEMENTED / READY FOR PO RECHECK`. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 16; `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 25; `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` Section 20.

## F13-STANDARDIZATION-001 — Date-Filter Remediation Closed, Evidence Consolidation Phase 1 Formally Closed, Frozen-Document Governance Delta Executed

- Product Owner ran the date-filter fix live (backend restarted) and confirmed `PO DATE-FILTER RUNTIME RECHECK PASS` (2026-08-13): Operation Dashboard's BCVH table aggregates correctly across the selected range; range updates correctly on filter change; the earlier to_date-only observation was an unrestarted backend, not a residual defect at commit `0a15ddf4`; a BCVH-filtered Sản lượng widget correctly omits national rank in that context (confirmed intended, not data loss). Date-filter remediation closed: `CLOSED / PO DATE-FILTER RUNTIME RECHECK PASS`.
- Evidence Consolidation Phase 1 formally closed: `PHASE 1 CLOSED / PO PASS`. Phase 1 (F-1 backend fix) and its remediation (DEFECT A Vietnamese IME search, DEFECT B empty-state distinction) both already had Product Owner runtime PASS; formal closure had been sequenced behind the date-filter recheck only (per 2026-08-12 PO instruction), now satisfied.
- Frozen-document governance delta executed (approved in principle 2026-08-11, executed 2026-08-13): all 8 documents amended, each gaining a `## 0. GOVERNANCE AMENDMENT NOTICE` section, original content preserved below as historical record — see the "Controlled amendment record (2026-08-13)" note under Frozen Documents above for the full list and disposition (AMENDED vs SUPERSEDED) of each.
- Search-result-presentation finding (2026-08-12, AC-15..AC-23) kept exactly as locked, unchanged — no Phase 2 implementation performed this round.
- Both stated Phase 2 prerequisites (PO recheck, frozen-document delta) are now satisfied, but Phase 2 implementation is not self-authorized by this governance-only round — it requires its own separate, explicit Product Owner instruction.
- Documentation-only. No product code, schema, route, or test file touched. `F13-SHIPMENT-001` not opened; `Data QLML/`, `NETWORK-MANAGEMENT` untouched; `.claude/` and both stashes confirmed untouched.
- State: `F13-STANDARDIZATION-001 — Date-Filter Remediation CLOSED / PO PASS; Evidence Consolidation Phase 1 CLOSED / PO PASS; frozen-document governance delta EXECUTED; Phase 2 NOT IMPLEMENTED / NOT YET AUTHORIZED`. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Sections 17-19; `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Sections 26-28; `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` Sections 20-22.

## F13-STANDARDIZATION-001 — Evidence Consolidation Phase 2 Implemented

- Product Owner authorized Evidence Consolidation Phase 2 implementation (2026-08-13, baseline `457329e2`): the locked three-region layout, approved widget disposition, `ShipmentExecutiveBrief` removal, and the search-result-presentation contract (AC-15..AC-23), all in one round.
- `ShipmentPerformancePage.jsx` rewritten: violation group tabs reused from `routeViolationEvidenceData.js`; `reason` scopes the evidence fetch server-side; conditional Tuyến column; keyword-only search-result summary (exact AC-16 wording); three visibly distinct KPI counts (AC-19); header-merged BCVH/Tuyến/Ngày badges (AC-23); auto-select-first-row effect removed entirely — selection is now only ever explicit (AC-15).
- New `groupRowsByRoute` groups by real `ma_tuyen`, never route-name text, never truncates to the first match (AC-17/18/22). New `ShipmentEvidenceDetail.jsx` consolidates the old Timeline+RootCause widgets into one evidence-detail panel.
- Deleted: `ShipmentImpactOverview.jsx`, `ShipmentRecommendation.jsx`, `ShipmentDrilldown.jsx`, `ShipmentExecutiveBrief.jsx`, `ShipmentTimeline.jsx`, `ShipmentRootCause.jsx`, `ShipmentShellShared.jsx` (last one a disclosed minor deviation — deleted as dead code once unused, not reworked as the plan literally listed).
- No metric/formula/date-contract/data-source change. Pre-existing DEFECT A/B empty-state logic and single-day `analysisDate` resolution left byte-for-byte unchanged, confirmed by 15 prior Phase 1/remediation tests passing unmodified.
- Live-database proof (service layer, not mocked): real context (2026-07-27, BCVH `533140`, 1,573 rows) — `reason=delayed_cash` returns exactly 217 rows matching `violation_summary.delayed_cash_count` exactly; 32 distinct real routes present in "Tất cả tuyến" mode.
- 19 new tests, all passing; full frontend sweep 302/315 (same 13 pre-existing failures by name, zero regressions); `oxlint` clean; `vite build` succeeds. Desktop/mobile responsiveness verified only via build success, not a live screenshot (no usable login credential in this workspace) — disclosed as a limitation. Mobile uses responsive column-hiding/stacking rather than a full-screen tap-sheet — a disclosed simplification.
- No backend file touched. Operation Dashboard, BCVH Ranking, Tuyến Ranking, Pareto/RCA, Network Management confirmed untouched. `F13-SHIPMENT-001` not opened; `Data QLML/`, `.claude/`, both stashes confirmed untouched.
- State: `PHASE 2 IMPLEMENTED / READY FOR PO RUNTIME RECHECK`. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 20 (AC-15..AC-23 → implementation/test mapping table); `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 29; `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` Section 23.

## F13-STANDARDIZATION-001 — Evidence Consolidation Phase 2 Runtime Recheck FAIL, Search-Result Remediation

- Product Owner's first Phase 2 runtime recheck FAILED (2026-08-13, tested at commit b3de0ea6/857f9b55): typing a keyword only showed one route, violating AC-17/AC-18.
- Diagnosis: pure-function tests of matchesSearchQuery/groupRowsByRoute/fetchAllEvidenceRows against real data proved them correct in isolation; a temporary, unsaved jsdom + Vite SSR harness (never committed) was then used to actually mount the real component with real backend data, reproducing the exact defect.
- Root cause: the evidence fetch was scoped server-side to the active violation-reason tab (default "Chậm nộp tiền," ~14% of a typical day's rows); a real 1,573-row/8-route context reduced to exactly 1 row/1 route under that default tab.
- Fix: the fetch now always pulls every reason group in one request; reason-tab scoping became a client-side filter; while a keyword is active, matching intentionally spans every reason group (each row still shows its own Lý do vi phạm badge); `contextTotal` re-derived from the tab-scoped subset.
- Reconciled against real data: default-tab "HCC" search restored to 8 groups/208 rows (was 1/1); a specific route selected stays correctly scoped (44 rows, matches DB exactly); clearing search restores the tab-scoped 217, not the broad 1,573.
- 14 new tests (mapped to the PO's C.1-13 list), all passing; full frontend sweep 316/329 (same 13 pre-existing failures, zero regressions); `oxlint` clean; `vite build` succeeds.
- Bounded to Phase 2 search-result presentation only. No metric/date-contract/schema change; no backend file touched; no other module expanded. **No governance closure performed** — Phase 2 remains open, per explicit PO instruction.
- State: `PHASE 2 SEARCH-RESULT REMEDIATION IMPLEMENTED / READY FOR PO RECHECK`. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 21; `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 30; `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` Section 24.

## F13-STANDARDIZATION-001 — Evidence Consolidation Phase 2 Search-Result Remediation: PO Runtime PASS

- Product Owner confirmed `SEARCH-RESULT REMEDIATION: PO RUNTIME PASS` (2026-08-13, tested at commit `e2ae87ac`/`1a6c490b`) against the real BCVH Thuận Hóa `533140`/`2026-07-27`/"Tất cả tuyến" context.
- All 8 checked items confirmed: search "HCC" returns 208 bưu gửi across 8 real routes (matches the diagnosis reconciliation exactly); every matching route group listed for selection; no auto-selection of route or shipment; detail panel updates only on explicit selection; groups distinguished by real mã tuyến + tên tuyến, no duplicate/similar-named routes lost; route-filtered search (An Cựu) stays correctly scoped at 1 tuyến/44 bưu gửi; clearing the keyword on "Chậm nộp tiền" restores exactly 217 bưu gửi; Tuyến dropdown independence, Vietnamese/IME input, and 0/1/n states all correct.
- Documentation-only round recording the PASS. No product code touched.
- Explicit instruction: this PASS covers the search-result defect only — it does not constitute Phase 2 closure and does not authorize Phase 3/4. Evidence Consolidation Phase 2 as a whole remains open, awaiting a full-screen Product Owner acceptance pass over the complete AC-1..AC-23 set.
- `F13-SHIPMENT-001` not opened; no module outside Evidence Consolidation touched; `.claude/`, `Data QLML/`, both stashes confirmed untouched.
- State: `SEARCH-RESULT REMEDIATION: PO RUNTIME PASS`; Phase 2 remains `OPEN / AWAITING FULL-SCREEN PO ACCEPTANCE`. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 21; `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 30; `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` Section 24.

## F13-STANDARDIZATION-001 — Evidence Consolidation Phase 2 Closed: PO Full-Screen Runtime Acceptance PASS

- Product Owner tested the complete merged Evidence screen against the full AC-1..AC-23 set (2026-08-13, baseline `88c4bfd0`) and confirmed 11 checked items: merged three-region layout/zones; conditional Tuyến column; no auto-selection; detail panel updates only on explicit selection; identity/kết quả/nhóm vi phạm/timeline correct; the >3.0h delayed-cash rule statement correct; Action Center hand-off state honestly reflects real scope; `ShipmentExecutiveBrief`/`ShipmentImpactOverview`/`ShipmentRecommendation`/`ShipmentDrilldown` correctly removed; violation-reason tabs/search/clear-keyword/Tuyến dropdown all correct; desktop/mobile both usable, no acceptance-blocking defect.
- "AC-1 through AC-23, in full: PO RUNTIME PASS." "PO EVIDENCE CONSOLIDATION PHASE 2: FULL-SCREEN PASS."
- Product Owner authorized governance-only closure: no product code change, no Phase 3/4 self-activation, `F13-SHIPMENT-001` not opened.
- This closes Evidence Consolidation Phase 2 in full, including the search-result-presentation remediation already passed. Does not close Phase 3 (rewire Tuyến Ranking's drill-down button) or Phase 4 (retire `RouteViolationEvidencePage.jsx`), both remain `PLANNED / NOT ACTIVE`; nor the `F13-STANDARDIZATION-001` program itself, which remains open.
- Documentation-only round. No product code, schema, route, or test file touched. `F13-SHIPMENT-001` not opened; `Data QLML/`, `NETWORK-MANAGEMENT`, and every other module untouched; `.claude/`, both stashes confirmed untouched.
- State: `PHASE 2 CLOSED / PO FULL-SCREEN RUNTIME PASS`. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 22; `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 31; `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` Section 25.

## F13-STANDARDIZATION-001 — Phase 3 Implemented: Rewire Tuyến Ranking + Redirect Old Path

- Product Owner authorized Phase 3 only (2026-08-13, baseline `b6177b88`): rewire the drill-down button to `/f13/evidence` with full context preservation, a translating redirect for the old path, the F-2 fix, explicitly not Phase 4.
- `routeViolationEvidenceData.js`'s `buildViolationEvidenceLink()` rewired to target `/f13/evidence` with `from_date`/`to_date` both set to the clicked row's `analysisDate`, `reason` defaulting to `delayed_cash`; new `translateLegacyViolationsSearch()` converts the old `/f13/ranking/route/violations` dialect (`date`) into the new one for the redirect.
- `App.jsx`: new `LegacyRouteViolationsRedirect` (mirrors the accepted `LegacyShipmentRedirect` pattern), widened to `admin`+`viewer`.
- `RoutePerformancePage.jsx`: F-2 latent `ReferenceError` fixed (`route.total_failed`, not the out-of-scope `row`); drill-down button label reconciled to "Xem bưu gửi vi phạm," resolving one pre-existing failing test (F-3) as a disclosed, planned consequence.
- Residual disclosed, not fixed this round: `ShipmentPerformancePage.jsx` does not yet render a "Quay lại Tuyến Ranking" link from `return_to` — the parameter is correctly carried end-to-end, just not consumed on the destination screen; out of Phase 3's declared file scope.
- 21 new/updated tests, all passing (including a real behavioral F-2 test); full frontend sweep 325/337 (12 pre-existing failures, down from 13, disclosed not hidden); `oxlint` clean; `vite build` succeeds.
- `RouteViolationEvidencePage.jsx` and its tests untouched (Phase 4 not performed). No backend file touched; `F13-SHIPMENT-001` not opened; Phase 2 not reopened; `.claude/`, `Data QLML/`, both stashes confirmed untouched.
- State: `PHASE 3 IMPLEMENTED / READY FOR PO RUNTIME RECHECK`. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 23; `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 32; `docs/06_REVIEWS/Shared/F13-STANDARDIZATION-001_CHECKPOINT_001.md` Section 26.

## F13-STANDARDIZATION-001 — Phase 3 Return-Journey Remediation Implemented

- Product Owner runtime recheck found the Phase 3 forward navigation correct but the return journey incomplete: `return_to` was carried end-to-end to `/f13/evidence` but never consumed there, making the drill-down operationally one-way (2026-08-14, baseline `c7fed615`).
- `routeViolationEvidenceData.js`: new `isValidReturnTo()` rejects empty/external/protocol-shaped values and requires a recognized Route Ranking query key; `buildBackToRouteRankingLink()` (pre-existing, previously unconsumed) now routes through it, falling back to the bare `/f13/ranking/route` path on any invalid input.
- `ShipmentPerformancePage.jsx`: reads `return_to`, renders "← Quay lại Tuyến Ranking" only when valid, links via `buildBackToRouteRankingLink()` (never `navigate(-1)`), shown in all 4 render branches so refresh preserves it.
- Restores date, BCVH, search, route_type, only_failed — all already URL-backed on `RoutePerformancePage.jsx`. Diagnosed and disclosed, not fixed: selected-row highlight and page number are component-only state there, not restorable without a URL-contract expansion out of this remediation's authorized scope.
- 7 new/updated tests, targeted sweep 77/77 pass; full frontend sweep 331/343 (12 pre-existing failures, unchanged by name — zero new regressions); `oxlint` clean; `vite build` succeeds.
- `RoutePerformancePage.jsx`, `App.jsx`, `RouteViolationEvidencePage.jsx` untouched; no backend touched; `F13-SHIPMENT-001` not opened; Phase 2 not reopened; `.claude/`, `Data QLML/`, both stashes confirmed untouched.
- State: `PHASE 3 RETURN-JOURNEY REMEDIATION IMPLEMENTED / READY FOR PO RECHECK`. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 24; `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 33.

## F13-STANDARDIZATION-001 — Phase 3 Closed: PO Runtime Pass (Return Journey)

- Product Owner performed the runtime recheck at authoritative HEAD `29373803` (implementation commits `c99ac789`, `29373803`) and confirmed all 8 checked items: back-link shown only with a valid `return_to`; restores `from_date`/`to_date`/`bcvh_id`/`bcvh_name`/`search`/`route_type`/`only_failed` without re-selecting filters; survives an Evidence refresh; no back action on direct-open; loading/error/empty/success states all preserve it; invalid/external `return_to` falls back safely to `/f13/ranking/route`; the round trip loses no supported context. "PHASE 3: PO RUNTIME PASS."
- Residual explicitly accepted as non-blocking: `selectedRouteId`/`currentPage` remain `RoutePerformancePage.jsx` component-only state, not URL-backed; expanding the URL contract for `route_id`/`page` remains unauthorized and deferred, not a new ticket.
- Governance-only closure round: no product code changed. Phase 3 (Sections 32-33: rewire Tuyến Ranking drill-down + return-journey remediation) is now `CLOSED / PO RUNTIME PASS` in full.
- Phase 4 (retire `RouteViolationEvidencePage.jsx`) remains `PLANNED / NOT ACTIVE`, not self-activated by this closure. `F13-SHIPMENT-001` not opened; `.claude/`, `Data QLML/`, both stashes confirmed untouched.
- State: `PHASE 3 CLOSED / PO RUNTIME PASS`. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 24; `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 33.

## F13-STANDARDIZATION-001 — Phase 4 Implemented: Retire RouteViolationEvidencePage.jsx

- Product Owner authorized Phase 4 (2026-08-15, baseline `f312fa36`): retire the legacy `RouteViolationEvidencePage.jsx` surface, bounded dependency discovery required before deletion.
- Discovery found zero live imports/routes referencing the component or its test file — `App.jsx` had already dropped the import in Phase 3, and the legacy `/f13/ranking/route/violations` redirect was already implemented independently via `translateLegacyViolationsSearch`. Shared helper module `routeViolationEvidenceData.js` (still used by `ShipmentPerformancePage.jsx`/`RoutePerformancePage.jsx`) is untouched.
- Deleted: `RouteViolationEvidencePage.jsx`, `RouteViolationEvidencePage.smoke.test.js`. Added `RouteViolationEvidencePage.retired.test.js` (4 tests). One `App.jsx` comment reworded (no route/import/behavior change).
- Full frontend sweep 331/343 (12 pre-existing failures, unchanged by name, zero new regressions); `oxlint` clean; `vite build` succeeds — 679 modules transformed, identical to the pre-deletion build, and the built bundle contains zero occurrences of the deleted component's name, proving it was already unreachable from the build graph.
- Both stashes confirmed unaffected via `git stash show --stat` (read-only check, neither applied/popped); `.claude/`, `Data QLML/` untouched; `F13-SHIPMENT-001`/`F13-SURFACE-CLEANUP-PLAN` not opened; Phase 2/3 not reopened.
- State: `PHASE 4 IMPLEMENTED / READY FOR PO RECHECK`. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 25; `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 34.

## F13-STANDARDIZATION-001 — Phase 4 Closed: PO Runtime Pass (Evidence Consolidation Delta Complete)

- Product Owner performed the runtime recheck at authoritative HEAD `f3dbe1b9` and confirmed: Tuyến Ranking → Evidence works correctly; "Quay lại Tuyến Ranking" works correctly; the legacy `/f13/ranking/route/violations` URL auto-redirects to Evidence; direct Evidence access and its existing functionality remain normal; deleting `RouteViolationEvidencePage.jsx` caused no runtime regression. `PHASE 4 RUNTIME RECHECK PASS`.
- Governance-only closure round: no product code, test, route, or schema changed. Phase 4 (manifest Section 34) is now `CLOSED / PO RUNTIME PASS` in full.
- With this closure, the entire Evidence-consolidation delta this program has tracked since the product-value audit (manifest Sections 20-34: audit → consolidation plan → Phase 1 → Phase 2 → Phase 3 → Phase 4) is complete and closed end-to-end. No phase of that delta remains open.
- The original 5-phase `F13-STANDARDIZATION-001` program plan (Section 6: Phase 0 partial, Phases 1-4 Chuẩn hóa/Điều hành/Pareto/Regression) remains a separate, larger scope, still `PLANNED / NOT ACTIVE`, not authorized or closed by this.
- Authoritative state: **NO ACTIVE TICKET / AWAITING PO DIRECTION.** `F13-SHIPMENT-001` (`stash@{0}`) and `F13-SURFACE-CLEANUP-PLAN` remain unauthorized, not self-activated by this closure. `.claude/`, `Data QLML/`, both stashes confirmed untouched.
- State: `PHASE 4 CLOSED / PO RUNTIME PASS`. Full evidence: `docs/06_REVIEWS/Shared/F13-EVIDENCE-CONSOLIDATION-PLAN_CHECKPOINT_001.md` Section 25; `docs/10_TICKETS/F13-STANDARDIZATION-001_MANIFEST.md` Section 34.

## F41-MODULE-PLAN — Activated and Planned: F4.1 Module Overall Plan (Planning Only)

- Product Owner authorized activating `F41-MODULE-PLAN` from `NO ACTIVE TICKET` (2026-08-17, baseline `c2f4bdd7`), planning/documentation only, and locked seven F4.1 decisions: metric column `Đánh giá (thời gian Có TMS PTC 8 giờ)`; authoritative result `2.863/4.695 = 60,98%` with all 4.695 rows in the denominator; analysis date from the file name as in F1.3; scope Dashboard + BCVH Ranking + Evidence, no Tuyến Ranking; `Chậm nộp tiền` and acceptance identical to F1.3 with nothing further inferred; `531120` still stored and counted in the module total but hidden from BCVH Ranking; multi-indicator Import supporting Huế and TCT.
- Read-only inventory of the real source file `Data DKCL/F4.1/Incoming/HUE/F4.1-2026.08.01.xlsx` (SHA-256 `dcaae8e1…`, 862.532 bytes, 42 columns, 4.695 data rows) reproduced the locked figure exactly — `Đạt 2.863 / Không đạt 1.581 / trống 251` → `60,98%` — with a per-BCVH breakdown recorded as the acceptance baseline. The file was not created, moved, renamed, or modified.
- Delta-only survey findings: no indicator abstraction exists (`danh_gia_2026` hardwired in 20 backend files, 138 occurrences); the F1.3 ranking rate divides by `sl_bg_ptc`, which applied to F4.1 gives `64,29%` and contradicts the locked `60,98%`, so the F4.1 contract divides by total rows; `canonicalBcvhUnits.js` already freezes exactly the 6 real BCVH, so hiding `531120` needs no new rule; `excelParser.js` is not reusable and `extractDateFromFilename` is regex-locked to `^F1\.3-`; `importPipeline.js` hardcodes `../Data DKCL/F1.3` and `importWatcher.js` watches only that tree with `ignoreInitial: false` while an F4.1 file is already staged (risk `R-1`); the F4.1 source has no route column, so "no Tuyến Ranking" is data-enforced.
- Plan delivered: 11-item data contract, 5-phase sequence (SSOT package → Nền tảng dữ liệu → Import đa chỉ tiêu → Dashboard + BCVH Ranking → Evidence + nghiệm thu), UI/nav plan, reconciliation plan, test plan, risks `R-1..R-7`, PO gates 0-4.
- Escalated rather than guessed: no F4.1 TCT source exists anywhere in the workspace and the DKCL portal report identity for F4.1 is unknown, so no TCT parser, target table, or sync behavior was designed (`Q-1`, `Q-2` of five open questions).
- Documentation only: no product code, schema, database, or Import behavior changed; `.claude/`, `Data QLML/`, `Data DKCL/`, and both stashes untouched. No phase self-activated.
- State: `PLAN COMPLETE / AWAITING PO APPROVAL`. Full evidence: `docs/06_REVIEWS/Shared/F41-MODULE-PLAN_CHECKPOINT_001.md`; `docs/10_TICKETS/F41-MODULE-PLAN_MANIFEST.md`.
